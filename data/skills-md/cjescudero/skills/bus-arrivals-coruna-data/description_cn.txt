咨询拉科鲁尼亚巴士的法律法规，并使用 HTTP/HTTPS 直接作为 iTranvias API 的单独数据格式（在 MCP 和 HTML 中）。 Usar cuando el usuario pida (1) llegadas de una parada, (2) llegada de un bus concreto en una parada concreta, o (3) proximas llegadas de una linea en una parada。
