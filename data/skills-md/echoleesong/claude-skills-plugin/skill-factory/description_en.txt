Automated factory that turns GitHub repositories into standardized AI Skills. Use this tool when a user provides a GitHub URL and wants to "package", "wrap", or "create a Skill". It automatically fetches repository metadata, generates a standard directory structure, and injects the extension metadata required for lifecycle management.
