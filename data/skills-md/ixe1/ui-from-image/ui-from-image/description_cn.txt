将一个或多个网页/应用 UI 截图转换为高保真响应式实现，可作为独立页面，也可在现有前端基础上进行适配。优先确保视口精确匹配、字体与图标审查、明确的间距和颜色对应、浏览器截图对比，并通过反复细化直至不出现明显不符。对缺失的资产，使用提供的文件处理；若可用，使用直接的 image_gen 生成缺失的栅格资产，最好生成可提取的资产表以供多个相关资产使用；否则使用占位符，并提供单独的代码块，其中包含每个剩余资产的独立生成提示。
