在存储库上运行计划的机械维护时使用 - 死代码删除、无用测试修剪、分层违规修复、重复统一、逻辑错误修复、逻辑简化或过度抽象扁平化。通常從調度程序（cron、CI 調度或代理工作台的時間觸發器）作為“/cadence:maintenance-routine <archetype>”呼叫。每次呼叫一個原型；打開 PR 並且從不合併。不適用於審查 PR（使用 pr-review），不適用於使用者直接要求的臨時重構。
