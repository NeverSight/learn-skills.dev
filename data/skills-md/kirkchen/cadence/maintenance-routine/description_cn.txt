在存储库上运行计划的机械维护时使用 - 死代码删除、无用测试修剪、分层违规修复、重复统一、逻辑错误修复、逻辑简化或过度抽象扁平化。通常从调度程序（cron、CI 调度或代理工作台的时间触发器）作为“/cadence:maintenance-routine <archetype>”调用。每次调用一个原型；打开 PR 并且从不合并。不适用于审查 PR（使用 pr-review），不适用于用户直接要求的临时重构。
