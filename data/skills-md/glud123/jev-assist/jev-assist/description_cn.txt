在搜索未阅读过的代码库之前，你必须使用此工具；当问题询问哪些文件共享单一模式无法匹配的属性时——比如仍然执行 X 的文件、尚未迁移到 Y 的文件、违反了任何 linter 未编码的约定（`drift` 会逐个文件进行判断），或是变更将落在哪儿（`rerank` 将文件池相对于任务进行排序）。`gate` 在你提交之前会对暂存的 diff 进行身份验证、PII、数据丢失和隐藏错误风险的评估。`validate` 在此测量召回率。对任何语言、过大以至于无法逐个阅读的文件池，都会进行一次判断。只需一次 `node` 调用，零安装。
