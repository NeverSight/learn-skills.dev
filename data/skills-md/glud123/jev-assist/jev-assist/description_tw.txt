在搜尋未閱讀過的程式碼庫之前，必須先使用此工具，特別是當問題詢問哪些檔案共享單一模式無法匹配的屬性——仍在執行 X 的檔案、尚未遷移到 Y 的檔案、破壞了 linters 未編碼的慣例（`drift` 會逐一判斷每個檔案），或變更會落在哪裡（`rerank` 依任務為檔案池排序）。`gate` 會在你提交之前審查暫存的差異，評估授權、PII、資料遺失與被吞掉的錯誤風險。`validate` 在此測量召回率。對於過大無法完整閱讀的檔案池，對每個檔案皆作一次判斷，支援任何語言。只需一次 `node` 呼叫，無需安裝。
