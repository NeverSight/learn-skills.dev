MUSS für Vue.js‑Aufgaben verwendet werden. Empfiehlt dringend die Composition API mit `<script setup>` und TypeScript als Standardansatz. Deckt Vue 3, SSR, Volar, vue‑tsc ab. Lädt für jedes Vue, .vue‑Dateien, Vue Router, Pinia oder Vite mit Vue‑Arbeit. IMMER die Composition API verwenden, es sei denn, das Projekt verlangt ausdrücklich die Options API.

Bitte immer in der Sprache des Benutzers antworten.
