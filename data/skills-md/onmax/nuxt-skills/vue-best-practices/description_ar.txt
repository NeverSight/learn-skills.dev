يجب استخدامه لمهام Vue.js. يوصي بشدة بواجهة Composition API مع `<script setup>` و TypeScript كنهج قياسي. يغطي Vue 3، SSR، Volar، vue‑tsc. يدعم أي ملفات Vue، .vue، Vue Router، Pinia، أو Vite مع عمل Vue. يجب دائمًا استخدام Composition API ما لم يتطلب المشروع صراحةً Options API.

يرجى الرد دائمًا بلغة المستخدم.
