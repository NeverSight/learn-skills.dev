Vue 3 における Core Regle フォームバリデーション — `useRegle` でのセットアップ、リアクティブな `r$` オブジェクト、バリデーションプロパティ、エラー表示、修飾子。`v-model` を `r$.$value` にバインドする、あるいは `$error`／`$errors`／`$invalid`／`$validate`／`$reset` を参照する際に使用します。バリデーションルールは regle‑rules を、詳細なパターンは regle‑advanced を参照してください。
