Vue 3에서 Core Regle 폼 검증 — `useRegle` 로 설정하고, 반응형 `r$` 객체, 검증 속성, 오류 표시 및 모디파이어를 사용합니다. Regle 폼을 추가하거나 편집할 때, `v-model` 을 `r$.$value`에 바인딩하거나 `$error`/`$errors`/`$invalid`/`$validate`/`$reset`을 읽을 때 사용합니다. 검증 규칙은 regle-rules를, 고급 패턴은 regle-advanced를 확인하세요.
