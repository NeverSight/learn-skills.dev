Vue 3中的核心Regle表單驗證—使用'useRegle`、反應式`r $`物件、驗證屬性、顯示錯誤和修飾符進行設置。在新增或編輯Regle表單、將`v-model`綁定到`r $. $ value` ，或讀取`$ error`/`$ errors`/`$ invalid`/`$ validate`/`$ reset`時使用。有關驗證規則，請參閱regle-rules ；有關進階模式，請參閱regle-advanced。
