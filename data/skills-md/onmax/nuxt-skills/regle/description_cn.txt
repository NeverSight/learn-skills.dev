Vue 3中的核心Regle表单验证—使用“useRegle”、反应式“r $”对象、验证属性、显示错误和修饰符进行设置。在添加或编辑Regle表单、将“v-model”绑定到“r $. $ value”或读取“$ error”/“$ errors”/“$ invalid”/“$ validate”/“$ reset”时使用。有关验证规则，请参阅regle-rules ；有关高级模式，请参阅regle-advanced。
