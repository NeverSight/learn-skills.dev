backends de terminal jms-cli (SSH/WebSocket): BackendType y connect(), semántica ejecutiva de SSH (fecha límite general, recv_exit_status, stderr nunca mezclado en stdout), protocolo de marcador ejecutivo WS (doble marcador __JMSDONE + código de salida __JMSRC, marcos binarios, drenaje después de cada ejecución), keepalive (transporte SSH 30s/marcos de texto PING a nivel de aplicación WS cada 30s, nunca Código de operación WS 0x9), PTY interactivo (Ctrl+] para salir, sincronización de cambio de tamaño de SIGWINCH) y agregar nuevos backends (register_backend / AbstractTerminal / _AUTO_SEQUENCE). Úselo cuando el tiempo de ejecución del exec se agote o se cuelgue, el análisis de salida de WS se comporte mal, las sesiones caigan al latir, inicie sesiones interactivas, agregue un protocolo de fondo o escriba código de ejecución (). CLI primero (jms exec/jms login); utilice la API de la biblioteca connect() para terminales integrados.
