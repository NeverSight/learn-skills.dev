jms-cli terminal backends (SSH / WebSocket): BackendType and connect(), SSH exec semantics (overall deadline, recv_exit_status, stderr never mixed into stdout), WS exec marker protocol (double __JMSDONE marker + __JMSRC exit code, binary frames, drain after each execute), keepalive (SSH transport 30s / WS application-level PING text frames every 30s, never WS opcode 0x9), interactive PTY (Ctrl+] to exit, SIGWINCH resize sync), and adding new backends (register_backend / AbstractTerminal / _AUTO_SEQUENCE). Use whenever exec times out or hangs, WS output parsing misbehaves, sessions drop on heartbeat, login interactive sessions, adding a protocol backend, or writing execute() code. CLI first (jms exec / jms login); use the connect() library API for embedded terminals.
