jms-cli 終端後端（SSH / WebSocket）：BackendType 和 connect()、SSH exec 語意（總體截止時間、recv_exit_status、stderr 從未混合到 stdout）、WS exec 標記協定（雙 __JMSDONE 標記 + __JMS 退出秒/WS 應用程式層級 PING 文字幀，從不 WS 操作碼） 0x9），互動式 PTY（Ctrl+] 退出，SIGWINCH 調整大小同步），以及新增後端（register_backend / AbstractTerminal / _AUTO_SEQUENCE）。每當 exec 逾時或掛起、WS 輸出解析行為不當、會話隨心跳下降、登入互動式會話、新增協定後端或編寫execute() 程式碼時使用。 CLI 首先（jms exec / jms 登入）；使用嵌入式終端機的 connect() 函式庫 API。
