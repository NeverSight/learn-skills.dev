jms-cli 终端后端（SSH / WebSocket）：BackendType 和 connect()、SSH exec 语义（总体截止时间、recv_exit_status、stderr 从未混合到 stdout）、WS exec 标记协议（双 __JMSDONE 标记 + __JMSRC 退出代码、二进制帧、每次执行后耗尽）、keepalive（每 30 秒传输 30 秒/WS 应用程序级 PING 文本帧，从不 WS 操作码） 0x9），交互式 PTY（Ctrl+] 退出，SIGWINCH 调整大小同步），以及添加新后端（register_backend / AbstractTerminal / _AUTO_SEQUENCE）。每当 exec 超时或挂起、WS 输出解析行为不当、会话随心跳下降、登录交互式会话、添加协议后端或编写execute() 代码时使用。 CLI 首先（jms exec / jms 登录）；使用嵌入式终端的 connect() 库 API。
