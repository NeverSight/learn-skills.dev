JumpServer v4 인증 및 연결 토큰: 이중 인증(REST Bearer + Django 양식 로그인 jms_sessionid 쿠키), MFA/TOTP 처리(코드 및 오류 필드 모두에서 mfa_required 감지, 챌린지 엔드포인트, otp_secret의 자동 TOTP 대 삽입된 otp_prompt 콜백), 연결 토큰 생성(SSH web_cli/SFTP web_sftp, KoKo 포트 2222, JMS-{id} 계정), HTTP 재시도 및 오류 분류(401 - AuthError, APIError with status_code). 사용자가 로그인 실패, MFA 프롬프트, WebSocket 거부(쿠키 누락), 연결 토큰 오류, 401/APIError를 보고하거나 "로그인할 수 없음"이라고만 말하는 경우에도 로그인하거나 토큰을 생성하는 코드가 필요할 때마다 사용합니다. CLI 먼저(jms config add가 자격 증명의 유효성을 검사함) 프로그래밍 세션에는 JMSSession / create_connection_token을 사용하십시오.
