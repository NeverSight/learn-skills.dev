jms-cli JumpServer 서버 구성 관리: config.yaml(스키마 v1.0) 읽기/쓰기, platformdirs 구성 경로, AES-256-GCM 자격 증명 암호화(enc:v1:), `jms config add/list/remove/set-default` 명령 및 load_config/add_server/remove_server/set_default_server 라이브러리 API. 사용자가 서버 별칭을 추가 또는 검사하고, 구성 오류(구성을 찾을 수 없음, 호스트/사용자 이름/비밀번호 누락, 암호 해독 실패)를 수정하고, config.yaml을 직접 편집 또는 생성하고, 암호화된 자격 증명을 이해 또는 처리하거나, "config"라고 말하지 않더라도 --config 및 JMS_CONFIG 경로 동작을 해결해야 할 때마다 사용하세요. CLI 먼저; 프로그래밍 방식으로 액세스하려면 라이브러리 API를 사용하세요.
