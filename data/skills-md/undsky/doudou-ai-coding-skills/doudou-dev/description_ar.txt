إدارة عبر المنصات (Windows، macOS، Linux) لتدفق عمل تطوير وبناء doudou‑ai‑coding. تشمل وضعين أساسيين: (1) وضع التطوير/التصحيح (يُفعل بواسطة كلمات مفتاحية مثل “debug / develop / start / run / run locally / dev”)، والذي يشغّل doudou‑eggjs (npm run dev) وdoudou‑vue3 (pnpm dev)، ويوفر كشف المنفذ والبيئة، تثبيت الاعتمادات تلقائيًا، تسجيل متزامن، وإغلاق سلس؛ (2) وضع بناء الإنتاج (يُفعل بواسطة كلمات مفتاحية مثل “package / build / compile artifact / production build / build / build:prod”)، والذي يُشغّل doudou‑eggjs (npm run build، أي rec build ‑o dist) وdoudou‑vue3 (pnpm run build:prod، أي vite build) لإنشاء قطع الإصدار.
