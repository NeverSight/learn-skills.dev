إنشاء اختبارات تكامل لنقاط نهاية واجهة برمجة التطبيقات (API) مع تدفقات قاعدة البيانات، بما في ذلك إعداد أدوات الاختبار والتركيبات والإعداد/التفكيك وبذر قاعدة البيانات واستراتيجيات صديقة لـ CI. يُستخدم في "اختبار التكامل" أو "اختبارات واجهة برمجة التطبيقات" أو "اختبارات قاعدة البيانات" أو "أداة الاختبار".
