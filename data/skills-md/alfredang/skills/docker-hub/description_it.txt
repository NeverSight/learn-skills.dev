Crea e invia immagini Docker a Docker Hub (tertiaryinfotech). Genera automaticamente Dockerfile se mancante rilevando il tipo di progetto. Da utilizzare durante il push di immagini Docker, l'esecuzione di "push to docker hub", "docker push" o qualsiasi attività di distribuzione di Docker Hub.
