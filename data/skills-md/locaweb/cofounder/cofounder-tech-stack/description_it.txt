Architettura full‑stack Go + React con sviluppo locale iterativo. Usa questa competenza quando crei lo scheletro di una nuova app, aggiungi funzionalità, correggi bug, esegui il ciclo di sviluppo locale, o quando l’utente chiede di “eseguire l’app localmente”, “eseguire l’app sul mio computer”, “avviare l’app”, “rodar o programa”, “rodar o app”, “executar localmente” o qualsiasi richiesta equivalente per avviare l’applicazione nel loro ambiente locale. Copre la struttura del progetto, le migrazioni del database, la generazione di codice sqlc, Supabase/Postgres locale via Podman, le estensioni di Postgres (pgmq, pg_cron, pgroonga, pgvector, pg_jsonschema, LISTEN/NOTIFY), il prerendering delle pagine pubbliche per SEO e crawler AI, e il ciclo di feedback scrivi‑test‑ripeti.
