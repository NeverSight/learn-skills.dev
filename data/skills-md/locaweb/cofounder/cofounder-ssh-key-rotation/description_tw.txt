當使用者要求「旋轉 SSH 金鑰」、「重新產生 SSH 金鑰」、「更換 SSH 金鑰」或「續期 SSH 金鑰」，或是代理需要對已部署的虛擬機進行故障排除（檢查日誌、除錯、資料庫存取）但發現預期的 SSH 金鑰檔案在本機磁碟上遺失（例如 `~/.ssh/<repo-name>` 不存在）時，應使用此技能。亦在使用者提及「遺失 SSH 金鑰」、「找不到 SSH 金鑰」、「無法 SSH 連線到伺服器」、「SSH 權限被拒絕」、「搬到新電腦」或「在其他機器上克隆了儲存庫」時使用。

請始終以使用者的語言回覆。
