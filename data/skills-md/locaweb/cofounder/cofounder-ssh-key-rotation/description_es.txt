Esta habilidad debe usarse cuando el usuario solicite "rotar claves SSH", "regenerar claves SSH", "reemplazar claves SSH", "renovar claves SSH", o cuando el agente necesite acceso SSH a una VM desplegada para solucionar problemas (registros, depuración, acceso a la base de datos) pero descubra que el archivo de clave SSH esperado falta en el disco local (p. ej., `~/.ssh/<repo-name>` no existe). También utilícela cuando el usuario mencione "clave SSH perdida", "clave SSH no encontrada", "no puedo conectar por SSH al servidor", "permiso denegado SSH", "cambiado a una nueva computadora" o "repositorio clonado en otra máquina".

Por favor, responda siempre en el idioma del usuario.
