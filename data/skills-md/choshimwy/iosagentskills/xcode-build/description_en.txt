Xcode build and configuration skills. Used when dealing with Xcode project configuration, Build Settings, build scripts, archiving, IPA export, CI/CD setup, command-line builds (xcodebuild), certificate management. Covers build optimization, Scheme configuration, compiler options, code signing, and automated build workflows.
