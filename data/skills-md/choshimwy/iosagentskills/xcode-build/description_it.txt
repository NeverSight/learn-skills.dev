Competenze di build e configurazione di Xcode. Utilizzato quando si ha a che fare con la configurazione del progetto Xcode, le impostazioni di build, gli script di build, l'archivio, l'esportazione IPA, la configurazione CI/CD, le build da riga di comando (xcodebuild) e la gestione dei certificati. Copre l'ottimizzazione della build, la configurazione dello schema, le opzioni del compilatore, la firma del codice e i flussi di lavoro di build automatizzati.
