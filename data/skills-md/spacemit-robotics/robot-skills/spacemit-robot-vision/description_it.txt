SpacemiT Robot SDK punto di ingresso per lo sviluppo di visione e immagini. Da utilizzare quando gli utenti chiedono di configurare, compilare, eseguire esempi, chiamare API, eseguire benchmark o fare debug di rilevamento, classificazione, segmentazione, tracciamento, pose, riconoscimento facciale, OCR, embedding immagine‑testo (CLIP/SigLIP) o di applicazioni CV composte (rilevamento di caduta, incendio, intrusione, emozioni) in modalità locale, remota o ibrida. Per favore rispondi sempre nella lingua dell'utente.
