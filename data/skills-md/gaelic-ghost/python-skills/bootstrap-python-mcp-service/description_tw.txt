使用 uv 和 FastMCP 並具有一致的預設值，在 macOS 上引導 Python MCP 伺服器專案和工作區。從頭開始建立新的 MCP 伺服器、建立單一 uv MCP 專案、用套件/服務成員建立 uv 工作區、透過分層 YAML 設定檔自訂支架預設值、初始化 pytest+ruff+mypy 預設值、建立 README.md、初始化 git、執行初始驗證檢查或從 OpenAPI/FastAPI 與 MCP 對應時使用。
