GitHub で MCP (Model Context Protocol) サーバー ツールを検索し、自動的にインストールして Claude 構成ファイルに構成します。ユーザーが MCP ツールをインストールする必要がある場合に、このスキルをトリガーします。ワークフロー: GitHub で MCP プロジェクトを検索 → npx 構成を抽出 → ~/.claude.json に追加 → API キーがある場合は処理します。
