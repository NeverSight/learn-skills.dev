在 Scry 中编写语义向量——将概念嵌入为 @handles、按余弦距离搜索、使用向量代数消除偏差以及诊断信号丢失。当任务涉及以下内容时使用：语义搜索、嵌入、向量、余弦距离、<=>、“X 但不是 Y”、去偏差、嵌入此概念、@handle、振动代数、概念向量。不适用于：word2vec 训练、微调嵌入、本地向量数据库（FAISS、Pinecone、Chroma）或普通关键字/SQL 搜索（使用 scry）。
