在 Scry 中編寫語義向量——將概念嵌入為 @handles、按餘弦距離搜尋、使用向量代數消除偏差以及診斷訊號丟失。當任務涉及以下內容時使用：語意搜尋、嵌入、向量、餘弦距離、<=>、「X 但不是 Y」、去偏差、嵌入此概念、@handle、振動代數、概念向量。不適用於：word2vec 訓練、微調嵌入、本地向量資料庫（FAISS、Pinecone、Chroma）或普通關鍵字/SQL 搜尋（使用 scry）。
