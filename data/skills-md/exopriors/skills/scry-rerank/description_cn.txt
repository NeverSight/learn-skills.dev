LLM 支持的通过成对比较对候选集进行多属性重新排序。支持规范属性（清晰度、技术深度、洞察力）、自定义提示、模型层选择和 TopK 配置。当任务涉及：重新排名、按清晰度排名、按洞察力排名、按深度排名、最佳项目、质量等级、LLM 判断、成对比较、多属性排名、从 sql 或列表重新排名时使用。不适用于：简单的 SQL 排序（ORDER BY date/upvotes/score - 使用 scry），或语义搜索和嵌入代数（使用 scry-vectors）。
