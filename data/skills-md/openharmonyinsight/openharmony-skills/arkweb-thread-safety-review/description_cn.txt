在扫描或审查 OpenHarmony ArkWeb/Chromium C++ 单文件时，必须使用，以检测涉及 PostTask、BindOnce、base::Unretained(this)、WeakPtr、scoped_refptr、mojo::Connector、GPU/Audio/DrDC 线程、NDK/UI 回调、NWeb、WebContents、Profile、BrowserContext、NavigationController 的线程安全违规。强制控制误报，并仅输出针对 standard_rule1‑standard_rule10 的严格 JSON 架构违例。

请始终使用用户的语言作答。
