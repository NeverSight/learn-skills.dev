Guida per aggiungere e mantenere i test di interoperabilità ArkTS ↔ JS/TS in ArkCompiler.

Scenario di attivazione: quando si creano nuovi casi di test di interoperabilità ArkTS‑JS/TS (Interop) nella directory `plugins/ets/tests/interop_js/tests/`, si esegue il debug o la manutenzione dei test Interop esistenti, o si scrive un runner C++ (runner GTest) o file di dichiarazione (`.d.ets`).
