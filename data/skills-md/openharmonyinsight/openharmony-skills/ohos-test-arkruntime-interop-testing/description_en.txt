Guide for adding and maintaining ArkTS ↔ JS/TS interoperability tests in ArkCompiler.

Trigger scenario: when creating new ArkTS‑JS/TS interop (Interop) test cases in the `plugins/ets/tests/interop_js/tests/` directory, debugging or maintaining existing Interop tests, or writing a C++ runner (GTest runner) or declaration files (`.d.ets`).
