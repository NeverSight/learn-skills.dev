在对 04-feature.md 执行 Phase 0 Step 0.5 Review Ready Gate 时使用，特别是当用户说“evaluate gate”“review readiness”“feature ready?”“should we generate IR”，或当 ohos‑req‑intake‑orchestration 主会话需要结构化的 Ready / Conditional Ready / Not Ready 判定而不是内联检查时使用。读取 01‑04，运行七项固定检查以及条件项检查，并返回机器可读的 JSON 摘要以及人类可读的表格，主会话可据此进行路由。不要用于特性基线生成（ohos‑req‑feature‑baseline）、价值决策记录（ohos‑req‑value‑decision）或 IR 生成（ohos‑req‑feature‑to‑ir）。

请始终使用用户的语言回复。
