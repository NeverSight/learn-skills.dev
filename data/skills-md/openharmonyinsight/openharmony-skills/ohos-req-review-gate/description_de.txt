Verwenden Sie dies, wenn Sie die Phase 0 Schritt 0.5 Review Ready Gate an einer 04‑feature.md durchführen, insbesondere wenn der Benutzer sagt „evaluate gate“, „review readiness“, „feature ready?“, „should we generate IR“ oder wenn die Hauptsitzung ohos‑req‑intake‑orchestration eine strukturierte Ready / Conditional Ready / Not Ready‑Bewertung anstelle einer Inline‑Prüfung benötigt. Liest 01‑04, führt sieben feste Prüfungen plus eine Conditional‑Items‑Prüfung durch und gibt eine maschinenlesbare JSON‑Zusammenfassung sowie eine menschenlesbare Tabelle zurück, die die Hauptsitzung weiterverarbeiten kann. Verwenden Sie dies NICHT für Feature‑Baseline‑Generierung (ohos‑req‑feature‑baseline), Wertentscheidungs‑Aufzeichnung (ohos‑req‑value‑decision) oder IR‑Generierung (ohos‑req‑feature‑to‑ir).

Bitte immer in der Sprache des Benutzers antworten.
