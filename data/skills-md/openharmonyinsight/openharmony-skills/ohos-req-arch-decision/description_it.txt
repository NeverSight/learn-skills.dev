Usa quando selezioni una direzione di soluzione OHOS nella fase 0.3, soprattutto per 03-arch-decision-record.md, il confronto delle opzioni candidate o quando la sessione principale richiede un output ADR strutturato con decisione fornita dall’utente. NON utilizzare per l’analisi di fattibilità (ohos-req-feasibility-analysis), il baseline della funzionalità (ohos-req-feature-baseline) o il gate di revisione (ohos-req-review-gate).

Per favore rispondi sempre nella lingua dell’utente.
