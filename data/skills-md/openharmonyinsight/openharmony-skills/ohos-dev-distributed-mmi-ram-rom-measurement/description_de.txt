Verwenden Sie es, wenn Sie die RAM/ROM‑Auswirkungen von Änderungen am OpenHarmony‑Multimodal‑Eingabe‑(MMI‑)Subsystem auf dem DAYU200 (RK3568) messen. Es deckt die ROM‑Messung mittels readelf‑Abschnittsanalyse und Dateigrößenvergleich, die RAM‑Messung über /proc/PID/smaps, smaps_rollup, hidumper --mem sowie die Baseline/Current‑.so‑Swap‑Methodik ab. Enthält eine theoretische Code‑Analyse auf Ebene der heap‑allokierten Datenstrukturen.

Bitte antworten Sie immer in der Sprache des Benutzers.
