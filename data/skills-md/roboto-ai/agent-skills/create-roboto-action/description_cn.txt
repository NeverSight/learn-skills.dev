从自然语言描述生成一个可工作且已验证的 Roboto Action。每当用户想创建、构建或搭建 Roboto Action，即使他们仅描述任务（如“标记所有日志中提到 X 的数据集”“从这些 rosbag 中提取视频”），而未使用 “action” 这一词，也请使用此技能。包括需求分解、解决歧义的访谈（在 `--yolo` 模式下跳过）、脚手架搭建、实现以及通过本地干运行调用进行验证。此技能不用于部署或调用已存在的 Action，也不用于一般的 Roboto SDK 问题。

请始终使用用户的语言作答。
