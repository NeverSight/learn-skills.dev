用于编写查询、设计模式、管理索引和优化性能的 PostgreSQL 16+ 参考。在以下情况下使用：(1) 编写 SQL 查询（SELECT、INSERT、UPDATE、DELETE、CTE、窗口函数、子查询），(2) 设计或更改表模式（CREATE TABLE、约束、序列、数据类型），(3) 选择或创建索引（B 树、GIN、GiST、BRIN、部分、表达式），(4) 管理事务和隔离级别（BEGIN/COMMIT、保存点、MVCC），(5) 工作使用 JSONB 数据和 jsonpath，(6) 使用 EXPLAIN ANALYZE 分析查询性能，或 (7) 使用 psql CLI。
