Integra i database PostgreSQL con le API Go Gin utilizzando GORM o sqlx. Copre modello di repository, tentativi di connessione con backoff, impaginazione di cursori/set di tasti, transazioni basate sul contesto, TLS/sslmode, migrazioni e inserimento di dipendenze. Da utilizzare quando si aggiunge il supporto del database, si creano modelli, si scrivono query, si implementano repository, si impostano migrazioni o si collegano livelli di database in un progetto Gin. Si attiva anche quando l'utente menziona GORM, sqlx, connessione al database, query SQL, pattern di repository o migrazioni di database in un contesto Go/Gin.
