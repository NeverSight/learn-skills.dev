在当前会话中以 ponytail-for-experiments 的风格执行一个受限的研究实验分支。仅在明确请求时使用，或用于已知配置的单一连贯批次运行（一个 方法 × 数据集 × 预算，或一次活动的消融分支）。首先检查该分支是否已被裁剪或已有答案，只审查未解决的配置决策，预检查环境和资源，按项目约定执行，并将指标以最终图表可直接使用的形式落入共享表中。想法仍模糊需讨论时倾向使用 exp-discuss；工作需要在各分支上分阶段裁剪时倾向使用 exp-campaign；一次性抽查时倾向使用 exp-probe。

请始终使用用户的语言作答。
