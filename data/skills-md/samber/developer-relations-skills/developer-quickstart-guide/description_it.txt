Scrive o revisiona un quickstart per sviluppatori che accompagna il lettore da zero a un successo verificato – percorso minimo, comandi da copiare‑incollare, output atteso ad ogni passo, ramificazioni di fallimento e un budget di tempo per l’esecuzione a freddo. Usalo ogni volta che l’utente menziona un quickstart, una pagina di introduzione, un documento hello‑world, “time to first success” o “time to first value”, documenti di onboarding per un’API, SDK, CLI o strumento auto‑ospitato, o si lamenta che nessuno completa la loro pagina di introduzione – anche se dicono solo “la nostra configurazione è troppo difficile”. NON usarlo per un tutorial didattico (samber/developer-relations-skills@developer-tutorial) o per una riscrittura del README (samber/developer-relations-skills@readme-optimization).

Rispondi sempre nella lingua dell’utente.
