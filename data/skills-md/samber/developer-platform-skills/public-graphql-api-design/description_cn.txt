为第三方开发者设计公共 GraphQL API——GraphQL-or-not 门、模式约定（命名、可空性、Node 接口、输入/负载类型）、Relay 游标连接分页、联合/接口错误结果类型、深度和复杂度上限作为设计决策、联邦信任边界（仅路由器对外公开），以及仅允许列入白名单的第一方流量的持久化查询策略。每当用户提及 GraphQL、公共模式、Relay 连接、查询深度限制、持久化查询、联邦或 GraphQL 与 REST 时使用——即使他们从未说“GraphQL API 设计”。仅限设计层面。不要用于 REST 表面审查——改用 samber/developer-platform-skills@public-api-design-review。
