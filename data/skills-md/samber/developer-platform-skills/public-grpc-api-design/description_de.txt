Entwerfen Sie eine öffentliche gRPC-Oberfläche für externe Entwickler – das wann‑gRPC‑überhaupt‑Tor (die meisten Plattformen halten gRPC intern und veröffentlichen REST oder transkodiertes JSON), AIP‑Proto‑Paket‑ und Versionskonventionen, buf‑Breaking‑Change‑Gates und deren google.api.http‑Blindspot, Unary‑by‑Default‑Streaming‑Entscheidungen, das google.rpc.Status‑Fehlermodell mit seinen HTTP‑Mapping‑Fallen, Transkodierung und Gateway‑Architektur (Connect‑RPC, grpc‑gateway, Envoy) sowie externe Authentifizierung und TLS. Verwenden Sie dies immer, wenn der Nutzer gRPC, Protobuf oder .proto‑Dateien, buf, Connect‑RPC, grpc‑gateway oder gRPC‑JSON‑Transkodierung an einem öffentlichen Rand erwähnt – selbst wenn er nie „gRPC API Design“ sagt. Nur die Design‑Schicht, nicht die sprachspezifische Serverimplementierung.
