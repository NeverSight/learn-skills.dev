设计 B2B SaaS 为第三方应用提供的 OAuth2 授权服务器界面——遵循 OAuth 2.1 协议基线（所有客户端使用 PKCE，不提供隐式或密码授权，精确的重定向匹配），令牌 TTL 与刷新轮换策略，作用域分类与粒度，带有部分授权和增量授权的同意屏幕设计，客户端注册姿态，以及分层的应用验证计划。每当用户提到 OAuth、“使用 X 登录”、访问令牌和刷新令牌、作用域、同意屏幕、PKCE，或第三方应用代表客户操作时，都要使用该设计——即使他们没有说“OAuth 提供者”。仅针对发行者端，不要与他人的 OAuth 集成。不要用于 API‑key 设计——请改用 samber/developer-platform-skills@api-auth-key-management。

请始终使用用户的语言进行回应。
