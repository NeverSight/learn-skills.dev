使用 zenrouter 協調器模式實現和擴展路由。當使用者要求新增路線、功能模組、佈局、重定向規則、重定向防護、嵌套協調器或以程式設計方式導航時，請使用此技能。觸發：route、router、routing、coordinator、CoordinatorModular、RouteModule、NavigationPath、IndexedStackPath、RouteLayout、RouteRedirectRule、RedirectRule、RedirectResult、parseRouteFromUri、push、Reddeep、navigate、RedirectResult、parseRouteFromUri、push、deep、replace、navigate、 link、toUri、parentLayoutKey、layoutKey、bindLayout、RouteUnique、RouteGuard、RouteRedirect、notFoundRoute、zenrouter。
