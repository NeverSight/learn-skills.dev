Avvia in modo idempotente qualsiasi repository con strumenti di sviluppo, hook e flussi di lavoro standard. Da utilizzare quando si inizia a lavorare su un nuovo repository, si esegue l'onboarding in un progetto esistente o si garantisce che un repository abbia una configurazione CI/CD corretta. Configura: hook git (lefthook), modelli di messaggi di commit, modelli PR e azioni GitHub per lint/format/type-check/build. Richiede la conferma dell'utente prima delle modifiche. Trigger: "bootstrap repo", "setup hooks", "configure CI", "rig", "standardize repo".
