Multi-perspective adversarial review. In full/lean mode, when reviewer agents are deployed they are spawned in parallel; if agents are missing or abnormal, or if spawning fails, it automatically falls back to solo mode, and uses the built-in rubric fallback when the reference file cannot be read. Trigger methods: /story-review, /审查, “审查一下”, “帮我审一下”.
