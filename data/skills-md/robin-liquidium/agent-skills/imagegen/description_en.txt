Generate images from prompts and reference images via the Codex ChatGPT subscription (no API key). Use when the task needs AI-created bitmaps and the built-in image_gen tool is unavailable or you need a scriptable CLI with explicit output paths.
