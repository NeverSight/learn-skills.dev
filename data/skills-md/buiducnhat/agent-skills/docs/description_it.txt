Inizializzare o aggiornare la documentazione del progetto. Da utilizzare quando si configurano documenti per un nuovo progetto (`--init`) o si sincronizzano documenti con la base di codice corrente e le modifiche all'architettura (`--update`).
