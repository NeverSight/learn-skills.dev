将栅格徽标（PNG/JPG/WebP/截图）转换为干净、平滑、低复杂度的 SVG，并生成独立的 JS 渲染 HTML，采用先低复杂度后高复杂度的拟合工作流并进行叠加 QA，以边缘平滑度为主要硬性门槛，在不设固定全局阈值的情况下尽可能高地优化 IoU，进行贝塞尔审计，默认使用 10 次迭代预算。仅在用户请求矢量化、追踪、重新创建、拟合或转换徽标图像为 SVG/HTML 且未要求动画时使用此方法。如果用户请求原子运动、调谐器/控制器、主动画或徽标动画展示 HTML，则改用 pixel2motion。v2：还包括闭合/自交叉可变宽度丝带拟合（中心线骨架 + 边缘捕捉）以及文字商标字体匹配协议（墨水重量比）。
