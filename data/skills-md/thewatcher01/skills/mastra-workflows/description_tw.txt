使用 Mastra 1.x (@mastra/core/workflows) 建立 DAG 工作流程。在使用有向無環圖、步驟連結、並行執行、分支、循環、錯誤處理、掛起/恢復或串流處理實現工作流程編排時使用。在涉及 createStep、createWorkflow、.then()、.parallel()、.branch()、.foreach()、.dountil()、.dowhile()、.map()、工作流程狀態管理或 Mastra 步驟組合的任務上觸發。
