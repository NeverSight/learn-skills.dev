À utiliser lorsque vous voyez des erreurs « acteur isolé », « Sendable », « course aux données », « @MainActor », ou lorsque vous demandez « pourquoi cela n'est-il pas thread-safe », « comment utiliser async/wait », « à quoi sert @MainActor », « mon application plante avec des erreurs de concurrence », « comment puis-je réparer les courses aux données » - Swift 6 modèles de concurrence stricts avec isolation des acteurs et async/await
