Da utilizzare quando QUALSIASI build iOS fallisce, si verificano arresti anomali del test, Xcode si comporta in modo anomalo o si verifica un problema ambientale prima di eseguire il debug del codice. Copre errori di compilazione, errori di compilazione, conflitti di dipendenze, problemi del simulatore, diagnostica innanzitutto dell'ambiente.
