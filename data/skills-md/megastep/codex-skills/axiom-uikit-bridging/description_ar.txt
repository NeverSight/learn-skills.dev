يُستخدم عند تغليف طرق العرض/وحدات التحكم UIKit في SwiftUI، أو تضمين SwiftUI في UIKit، أو تصحيح مشكلات التشغيل المتداخل UIKit-SwiftUI. يغطي UIViewRepresentable، UIViewControllerRepresentable، UIHostingController، UIHostingConfiguration، المنسقين، دورة الحياة، ربط الحالة، إدارة الذاكرة.
