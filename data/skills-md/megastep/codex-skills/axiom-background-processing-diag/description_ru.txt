Устранение неполадок фоновой задачи на основе симптомов — деревья решений для «задача никогда не запускается», «задача завершается раньше времени», «работает в разработке, а не в рабочей версии», «обработчик не вызывается», с анализом затрат времени для каждого пути диагностики.
