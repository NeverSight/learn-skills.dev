HTML‑in‑Canvas API（drawElementImage）を利用して、ライブDOMを canvas / WebGL / three.js に描画する全体的な手順です。「HTML in canvas」や「drawElementImage」「drawing DOM onto canvas」「input within canvas」「DOM as texture」および CanvasUI ライブラリに関するリクエストで使用されます。経験的に検証されたトラップ回避ルールと標準的なスキャフォールドコードを提供します。
