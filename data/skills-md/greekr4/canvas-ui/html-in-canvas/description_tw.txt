使用 HTML‑in‑Canvas API（drawElementImage）將即時 DOM 繪製到 canvas/WebGL/three.js 上的整體工作。用於與「HTML in canvas」、「drawElementImage」、「drawing DOM onto canvas」、「input inside canvas」、「DOM as texture」以及 CanvasUI 函式庫相關的需求。提供徹底測試過的防陷阱規則與標準骨架程式碼。
