Внутренние служебные навыки для восстановления выходных данных сайта. Используйте после любого изменения в `src/`, `_partials/` или` site.config.json`, или всякий раз, когда другой навык требует обновленных артефактов `dist/`. Запускает `scripts/build.mjs` из установленного каталога навыков.
