In Git development, use this when you need to abandon the current attempt and roll back to a historical commit. It automatically archives the current state to the archive/ branch and creates bidirectional links (source and target) in ARCHIVE.md, ensuring traceability of development decision flows. Suitable for scenarios requiring a safe rollback while preserving the context of failed attempts.
