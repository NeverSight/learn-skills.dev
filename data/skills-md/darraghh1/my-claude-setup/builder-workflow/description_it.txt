Flusso di lavoro di implementazione a livello di fase per gli agenti builder. Gestisce la lettura dei file di fase, la ricerca di riferimenti, il richiamo delle competenze del dominio, l'implementazione di tutti i passaggi e l'esecuzione della verifica (test + typecheck). Precaricato negli agenti del builder tramite skills: field — non richiamabile dall'utente.
