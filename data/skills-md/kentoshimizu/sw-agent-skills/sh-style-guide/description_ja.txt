POSIX `sh` スクリプトのスタイル、レビュー、およびリファクタリング標準。 `.sh` ファイル、`#!/usr/bin/env sh` または `#!/bin/sh` を含むファイル、または `shell: sh` を含む CI ワークフロー ブロックが作成、変更、またはレビューされ、POSIX シェルの移植性/安全性制御を強制する必要がある場合にトリガーされます。 Bash 固有、Zsh 固有、または PowerShell 固有のスタイル ルールには使用しないでください。多言語プル リクエストでは、他の該当する `*-style-guide` スキルと一緒に実行します。
