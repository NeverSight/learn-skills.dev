Bash shell 脚本的样式、审查和重构标准。当创建、修改或审查“.sh”文件、带有“#!/usr/bin/env bash”或“#!/bin/bash”的文件或带有“shell:bash”的 CI 工作流块时触发，并且必须强制执行特定于 Bash 的质量控制（引用安全性、错误处理、可移植性、可读性）。请勿用于通用 POSIX `sh`、PowerShell 或特定于语言的应用程序样式规则。在多语言拉取请求中，与其他适用的“*-style-guide”技能一起运行。
