Export the Q&A from the session verbatim into Markdown. Directly parse the Claude Code / Codex session log file stored locally without having the model repeat it. Use this when the user says “record these rounds of Q&A in a document,” “export the recent conversation,” “write it into a QA log,” or “save this Q&A.”
