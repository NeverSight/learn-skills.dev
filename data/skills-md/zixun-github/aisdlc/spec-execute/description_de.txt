Wird verwendet, wenn I2 (Implementierungsausführung) im Spec Pack von sdlc-dev mit „{FEATURE_DIR}/implementation/plan.md“ als einziger Wahrheitsquelle durchgeführt werden muss, in Batches implementiert, minimale Validierung ausgeführt, Prüfinformationen zurückgeschrieben und an Batch-Kontrollpunkten berichtet werden muss; Stoppen Sie sofort, wenn Sie auf Blocker oder klärende Elemente stoßen.
