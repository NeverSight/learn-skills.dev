Use when you need to perform I2 (Implementation Execution) in the Spec Pack of sdlc-dev, with `{FEATURE_DIR}/implementation/plan.md` as the single SSOT, implement in batches, run minimal validation, write back audit information, and report at batch checkpoints; stop immediately upon encountering blockers or clarification items.
