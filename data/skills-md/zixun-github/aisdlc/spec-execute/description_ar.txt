يُستخدم عندما تحتاج إلى تنفيذ I2 (تنفيذ التنفيذ) في حزمة المواصفات الخاصة بـ sdlc-dev، باستخدام `{FEATURE_DIR}/implementation/plan.md` باعتباره SSOT الفردي، والتنفيذ على دفعات، وتشغيل الحد الأدنى من التحقق من الصحة، وإعادة كتابة معلومات التدقيق، والإبلاغ عند نقاط التفتيش الدفعية؛ توقف فورًا عند مواجهة أدوات الحظر أو عناصر التوضيح.
