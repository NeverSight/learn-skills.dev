Da utilizzare quando è necessario eseguire I1 (piano di implementazione) nel pacchetto specifiche di sdlc-dev, trasformando i requisiti/la progettazione in `{FEATURE_DIR}/implementation/plan.md` (la singola lista di controllo autorevole e lo stato SSOT) e fornire un elenco di attività inequivocabile per la successiva esecuzione I2.
