Use when you need to execute I1 (Implementation Plan) in the Spec Pack of sdlc-dev, turning requirements/design into `{FEATURE_DIR}/implementation/plan.md` (the single source of truth for checklist and status), and provide an unambiguous task list for the subsequent I2 execution.
