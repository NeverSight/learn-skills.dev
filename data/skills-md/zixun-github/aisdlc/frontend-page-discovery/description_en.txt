Use when the frontend code needs to generate evidence-based outputs of "page inventory → feature points → business processes → business rules," and the project has non-unique routing entry points, complex dynamic routing/permissions/backend menus, and the team is prone to making assumptions or missing evidence chains.
