Create, read/write, and format Google Sheets with the gog CLI (v0.10.0). Used for commands like “make a sheet,” “put data in the spreadsheet,” “manipulate sheets,” “create a template,” “update the spreadsheet,” “write to cells,” and “read sheets.” Provides the correct usage of the gog sheets command (pipe-separated, tab creation, formatting).
