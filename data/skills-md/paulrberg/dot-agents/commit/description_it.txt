Questa abilità dovrebbe essere utilizzata quando l'utente chiede di "impegnare modifiche", "creare un messaggio di commit", "mettere in scena e eseguire il commit", "impegnare solo le modifiche della sessione" o eseguire un flusso di lavoro di commit con flag come --all, --deep o --push. Crea commit atomic git con analisi euristica, formattazione del commit convenzionale, regole di staging, analisi approfondita opzionale e push opzionale.
