强制性配方：所有从罐子向 X（Twitter）发布的 Caffeine 构建必须遵循。支持的路径是通过出站 HTTPS 使用 `x-client` mops 包（X API v2），并采用每用户的 OAuth 2.0（PKCE，无客户端密钥）。自行编写 `ic.http_request` 调用 `api.x.com` 是禁止的反模式——它绕过了 Bearer 授权、非复制的外部调用保护以及该包的空字段 JSON 处理。每当用户、规格或任何之前的任务提到发布推文、“tweet this”、X/Twitter、共享到 X 或任何等价表述时，都要加载此技能——并且在编写任何涉及 X 端点的代码之前。

请始终使用用户的语言回复。
