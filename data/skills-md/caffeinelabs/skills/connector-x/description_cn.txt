强制性方案：每个从罐子向 X（Twitter）发布的 Caffeine 构建必须遵循。支持的路径是通过外部 HTTPS 使用 `x-client` mops 包（X API v2），并采用每用户 OAuth 2.0（PKCE，无客户端密钥）。手动编写 `ic.http_request` 调用 `api.x.com` 是**禁止**的反模式——它绕过了 Bearer 认证、非复制的外部调用安全机制以及该包的空字段 JSON 处理。在用户、规范或任何先前任务提到发布推文、“tweet this”、X/Twitter、分享到 X 或任何等价表述时，请加载此技能——并且在编写任何涉及 X 接口的代码之前。
