캔스터에서 X(트위터)로 게시하는 모든 Caffeine 빌드에 대한 필수 레시피입니다. 지원되는 경로는 아웃바운드 HTTPS를 통해 `x-client` mops 패키지(X API v2)를 사용하며, 사용자별 OAuth 2.0(PKCE, 클라이언트 시크릿 없음)입니다. `api.x.com`에 대한 `ic.http_request` 호출을 직접 구현하는 것은 금지된 안티패턴입니다 — 베어러 인증을 우회하고, 복제되지 않은 아웃콜 보호와 패키지의 null‑필드 JSON 처리 기능을 무시합니다. 사용자가, 사양이든, 이전 작업이든 트윗을 게시한다는 내용(예: “tweet this”, X/Twitter, X에 공유 등)과 언급될 때마다 이 스킬을 로드하고, X 엔드포인트에 접근하는 코드를 작성하기 **전에** 적용하십시오.

항상 사용자의 언어로 답변하십시오.
