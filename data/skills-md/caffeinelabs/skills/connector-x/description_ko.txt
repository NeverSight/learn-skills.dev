MANDATORY 레시피: 캐니스터에서 X(트위터)로 게시하는 모든 Caffeine 빌드에 적용해야 합니다. 지원되는 경로는 outbound HTTPS를 통한 `x-client` mops 패키지(X API v2)이며, 사용자별 OAuth 2.0(PKCE, 클라이언트 시크릿 없음)을 사용합니다. `ic.http_request` 를 직접 호출해 `api.x.com` 으로 요청하는 행위는 FORBIDDEN 안티패턴입니다 — 베어러 인증을 우회하고, 복제되지 않은 아웃콜 방지와 패키지의 null‑field JSON 처리를 무시합니다. 사용자가, 사양이든 이전 작업이든 “tweet this”, 트윗 게시, X/Twitter, X에 공유하기 등과 같은 표현을 언급할 때마다 이 스킬을 로드하고, X 엔드포인트와 관련된 코드를 작성하기 **이전에** 적용하십시오.
