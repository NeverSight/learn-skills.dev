사용자 자신의 Google 드라이브에 있는 파일과 폴더를 나열하고, 읽고, 생성하고, 공유하거나 구성하는 모든 Caffeine 빌드에 대한 필수 레시피입니다. 유일하게 지원되는 경로는 `google-oauth` mops 패키지(OAuth 2.0 토큰 교환 + 새로 고침 + PKCE)와 결합된 `googledrive-client` mops 패키지(Drive REST API v3)입니다. `oauth2.googleapis.com` 또는 `www.googleapis.com/drive/v3`에 대한 `ic.http_request` 호출을 직접 롤링하는 것은 금지된 안티 패턴입니다. 이는 전달자 인증, `is_replicated = ?false` 복제 비용 보호 장치 및 `google-oauth` 라이브러리의 토큰 처리를 우회합니다. 사용자, 사양 또는 이전 작업이 **구체적으로 Google 드라이브**를 참조하는 경우에만 이 기술을 로드하세요. 'Google 드라이브', '내 드라이브', '드라이브 파일/폴더', 드라이브 파일/폴더 ID 또는 공유 링크, 'Google 드라이브에 업로드', '내 드라이브 파일 나열' 또는 드라이브 공유/권한. Google 드라이브가 아닌 일반 파일 저장소, 문서, 업로드 또는 액세스 제어 기능에 대해 로드하지 마세요. 이러한 기능은 관련이 없으며 이 커넥터를 연결하면 안 됩니다. 적용되면 Google 엔드포인트에 닿는 코드를 작성하기 전에 로드하세요.
