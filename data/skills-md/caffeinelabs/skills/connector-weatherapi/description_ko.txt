모든 카페인 빌드에서 캔스터의 날씨 데이터를 읽는 필수 레시피입니다. 지원되는 경로는 `weatherapi-client` MOPS 패키지(WeatherAPI.com)를 통해 아웃바운드 HTTPS로, 쿼리 문자열 API 키로 인증됩니다. `api.weatherapi.com`에 대한 `ic.http_request` 호출을 직접 구현하는 것은 금지된 안티패턴이며, 이는 복제되지 않은 외부 호출 보호 장치를 우회하고(WeatherAPI 본문은 요청별 시계를 포함하고 합의를 실패시킵니다), 자동 생성된 JSON 디코딩 및 API 키 처리를 무시합니다. 사용자가 날씨, 예보, 온도, 습도, 바람, 비, 눈, 상황, 일출, 일몰, 달 단계, 천문학, 시간대, IP 지리 위치, 해양 또는 조수 예보, 위치 자동 완성 등을 언급하거나 사양 또는 이전 작업에 언급될 경우 이 스킬을 로드하고, 날씨 엔드포인트에 접속하는 코드를 작성하기 전에 반드시 적용하십시오. 

항상 사용자의 언어로 응답하십시오.
