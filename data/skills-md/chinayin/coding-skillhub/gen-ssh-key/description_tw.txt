根据团队标准生成 SSH 密钥并返回公钥。默认为 Ed25519，RSA 4096 作为后备（绝不允许 RSA 2048）；私钥是 chmod 600 并以服务命名。每當使用者需要服務或機器的 SSH 金鑰、金鑰對、部署金鑰或登入金鑰，或提及 ssh-keygen/puttygen 時，請使用此技能 - 例如「產生 ssh 金鑰」、「建立 ssh 金鑰對」、「為服務 xxx 製作登入金鑰」、「產生部署金鑰」——即使它們沒有提及團隊標準。更喜欢 puttygen (.ppk/.pem/.pub)；退回到 ssh-keygen。
