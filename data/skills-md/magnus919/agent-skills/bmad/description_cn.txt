使用此技能运行 BMad（突破性敏捷 AI 驱动开发方法），将其作为与框架无关的控制平面协议，将人类意图转化为有界、可检查、可恢复的代理工作。将意图压缩为五字段合同（Why、Capabilities、Constraints、Non‑goals、Success signal）；将工作分类为 direct / bounded / initiative 并路由到最小安全路径；在持久化工件中携带决策；以分诊方式审查；将失败路由到产生歧义的层级；并在可观察的接受性上对自主权加门，使用机器可读状态（draft/ready‑for‑dev/in‑progress/in‑review/done/blocked）。当变更请求、功能、委托构建或多代理史诗需要意图捕获、有限实现、审查以及在任何代理框架中的可恢复性时使用。不要用于验证问题是否真实（product‑discovery）、在计划前进行赌注塑形（product‑shaping）、正式规范/门控流水线（spec‑driven‑development），或 issue 到 PR 的交付流程（neckbeard）。

请始终使用用户的语言作答。
