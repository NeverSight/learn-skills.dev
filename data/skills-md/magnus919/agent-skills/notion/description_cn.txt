在终端或代理中操作 Notion：检索页面、查询数据库、搜索页面和数据库，以及更新页面属性——使用捆绑的 notion‑cli 脚本，默认只读，并在每次创建或更新前通过 --dry‑run/--yes 确认。适用于代理需要读取 Notion 内容、回答团队维基或数据库中的问题，或进行确认编辑的情况。不要用于构建 Notion 集成或块级页面组合（即 Notion API 应用开发），也不要用于其他知识库（即它们自己的工具）。

请始终使用用户的语言回复。
