将 React 应用程序作为具名工具进行操作：检查并诊断 React/Vite 项目，设计组件边界和状态流，实现可访问的响应式 UI，并使用项目的测试验证行为。当任务明确涉及 React、JSX/TSX、React Hook、React Router、Vite React 配置或 React 构建失败时使用。不要用于与框架无关的前端策略（路由到 frontend-engineering），浏览器自动化（路由到 playwright），可访问性政策或审计（路由到 web-accessibility），或非 React 的移动应用（路由到 mobile-development）。

请始终以用户的语言作答。
