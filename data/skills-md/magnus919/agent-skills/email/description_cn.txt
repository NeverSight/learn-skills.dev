通过 Twilio SendGrid 在终端或代理上发送和诊断事务邮件：发送消息、检查投递情况（退信和垃圾邮件报告），并验证已签名的事件 Webhook 签名（ECDSA P‑256）——使用默认只读的捆绑 email‑cli 脚本，并通过 `--dry-run`/`--yes` 确认来控制每一次发送。当代理需要发送事务邮件、排查退信或垃圾邮件投诉，或确认收到的 SendGrid Webhook 真实性时使用。请勿用于营销或大批量邮件活动（那是 SendGrid 营销活动），构建邮件模板系统，或其他邮件提供商（即他们自己的工具）。
