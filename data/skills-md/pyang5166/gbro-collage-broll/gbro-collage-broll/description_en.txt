Create a high‑end editorial halftone paper‑collage / halftone paper collage B‑roll from roughly a 5‑second spoken script, opinion sentence, or abstract concept. When the user says “collage b‑roll”, “paper collage b‑roll”, “halftone collage”, “collage‑style visual matching”, “make a collage animation with this script”, “gbro‑collage‑broll”, or wishes to turn a sentence into a collage visual metaphor, this skill must be used. Enforce a three‑stage approval process: first propose only the visual metaphor; after the user confirms, generate a colored collage still frame; after the still frame is confirmed again, automatically invoke Gemini Omni Flash to generate the opening and closing frames and assemble the animation. The default video model is fixed to gemini‑omni‑flash‑preview and no longer defaults to Veo; it switches only if the user explicitly specifies another model.
