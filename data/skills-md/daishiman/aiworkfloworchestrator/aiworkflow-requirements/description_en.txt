A skill to search, reference, and update the canonical specification of AIWorkflowOrchestrator from `references/`.
Starting from resource-map / quick-reference / topic-map / keywords, it reads only the minimum required documents step by step.
Use: requirements check, design/API/IPC contract check, UI/state/security decisions, task-workflow・lessons-learned・unsynced-tasks sync.
Especially handles safeInvoke timeout, settings bypass, skill lifecycle, global nav, and rerouting of Skill Center / Workspace / Agent / Skill Creator.

Anchors:
• Specification-Driven Development / Apply: canonical spec sync / Purpose: keep implementation-spec alignment
• Progressive Disclosure / Apply: resource-map-driven read / Purpose: prevent omissions with minimal reference

Trigger:
spec check, spec update, task-workflow sync, lessons-learned sync, UI spec reflection, API/IPC contract check, security requirement check, safeInvoke, timeout, settings bypass, skill lifecycle, Skill Center, Workspace, Agent, Skill Creator, navContract, GlobalNavStrip, MobileNavBar, SkillManagementPanel, line budget reform, spec splitting, family split, generated index sharding
