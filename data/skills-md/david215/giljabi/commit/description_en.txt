Stage the working tree, screen it for secrets, and create a real Conventional Commit on the current branch — reading prose language, scope vocabulary, and attribution policy from the repo's own conventions. Use whenever a commit is wanted, including as the final step of /implement.
