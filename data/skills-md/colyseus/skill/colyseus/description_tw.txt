Colyseus 0.18 多人遊戲伺服器與客戶端：房間、Schema 狀態同步、狀態回呼、重新連線、配對、客戶端 SDK、客戶端預測。當任務或 package.json 中出現 colyseus、@colyseus/schema、@colyseus/sdk 或 colyseus.js 時使用，且在撰寫任何 Room 或 Schema 程式碼之前，當專案包含 pre-0.17 代碼，例如 getStateCallbacks()、Room<State> 泛型、onLeave(client, consented)、@filter() 或 new Server() 時也需使用。

請始終以使用者的語言回覆。
