Servidores y clientes de juegos multijugador Colyseus 0.18: salas, sincronización de estado con Schema, callbacks de estado, reconexión, matchmaking, SDKs de cliente, predicción del lado del cliente. Usar cuando colyseus, @colyseus/schema, @colyseus/sdk o colyseus.js aparezcan en la tarea o en package.json, antes de escribir cualquier código de Room o Schema, y cuando un proyecto contenga código previo a la versión 0.17 como getStateCallbacks(), genéricos Room<State>, onLeave(client, consented), @filter() o new Server().
