Colyseus 0.18 server e client per giochi multiplayer: stanze, sincronizzazione dello stato con Schema, callback di stato, riconnessione, matchmaking, SDK client, predizione lato client. Usa quando colyseus, @colyseus/schema, @colyseus/sdk o colyseus.js compaiono nel compito o nel package.json, prima di scrivere qualsiasi codice Room o Schema, e quando un progetto contiene codice pre‑0.17 come getStateCallbacks(), generici Room<State>, onLeave(client, consented), @filter() o new Server().

Per favore, rispondi sempre nella lingua dell'utente.
