Colyseus 0.18 多玩家游戏服务器和客户端：房间、Schema 状态同步、状态回调、重连、匹配、客户端 SDK、客户端预测。当任务或 package.json 中出现 colyseus、@colyseus/schema、@colyseus/sdk 或 colyseus.js 时使用，在编写任何 Room 或 Schema 代码之前，并且当项目包含 pre-0.17 代码，如 getStateCallbacks()、Room<State> 泛型、onLeave(client, consented)、@filter() 或 new Server()。
