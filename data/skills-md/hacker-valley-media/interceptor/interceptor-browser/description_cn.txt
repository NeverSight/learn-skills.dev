通过 interceptor CLI 驱动已登录的 Chrome / Brave / Safari 会话：打开/读取页面，点击，输入，检查 DOM/文本/网络，自动化丰富的浏览器编辑器和场景图，捕获 WebSocket、Beacon、BroadcastChannel 流量，记录/回放流程，拍摄 VLM 预算的截图，比较页面，并使用 --context 将流量路由到特定浏览器上下文。用于浏览器页面内容、标签页、表单、SPA 提取、请求覆写、页面通信捕获和部署检查。不适用于原生 macOS 应用、Electron 桌面应用的网页内容、操作系统对话框、浏览器界面或大规模爬取。
