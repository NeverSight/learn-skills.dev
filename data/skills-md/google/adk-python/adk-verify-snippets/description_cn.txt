检查 Markdown 文件中的每个 Python 代码块是否真的能够编译并运行，方法是将每个代码块提取到临时文件，在隔离的子进程中执行，并生成包含每个片段覆盖率的通过/失败报告。用于当用户请求验证、测试或确认 README、指南或文档页面中的代码示例时；想了解 Markdown 文件中哪些代码片段损坏或已过期时；或请求代码片段验证报告时。不要用于运行项目的测试套件（直接运行 pytest）、检查代码风格或格式（使用 `adk-style`），或编写新的可运行示例代理（使用 `adk-sample-creator`）。

请始终使用用户的语言回复。
