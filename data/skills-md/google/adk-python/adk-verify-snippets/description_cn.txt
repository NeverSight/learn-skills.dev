检查 Markdown 文件中的每个 Python 代码块是否真的能够编译并运行，方式是将每个块提取到临时文件中，在隔离的子进程中执行，并生成一个包含每个片段覆盖率的通过/失败报告。适用于用户希望验证、测试或确认 README、指南或文档页面中的代码示例；想了解 Markdown 文件中哪些片段已损坏或过时；或请求代码片段验证报告的情况。不要用于运行项目的测试套件（直接运行 pytest），也不要用于检查代码风格或格式（使用 `adk-style`），或用于创建新的可运行示例代理（使用 `adk-sample-creator`）。

请始终使用用户的语言作答。
