Banca dati di sviluppo Odoo 19 con 18 guide specializzate che coprono Azioni (ir.actions.*, lavori cron, azioni server), Controller (instradamento HTTP, endpoint, tipi di autenticazione), File di dati (record XML/CSV, scorciatoie, noupdate), Decoratori API (@api.depends, @api.constrains, @api.ondelete, @api.onchange, @api.model, @api.private), Vincoli SQL (models.Constraint che sostituisce _sql_constraints), Indici di database (models.Index), Sviluppo di moduli (manifest, wizard, report), Tipi di campo (Char, Text, Monetary, campi relazionali), Configurazione del manifest (__manifest__.py, dipendenze, pacchetti di asset), Mixins (mail.thread, mail.activity.mixin, mail.alias.mixin, utm.mixin), Metodi del modello ORM (search, CRUD, filtri di dominio, recordset, denominazione dei modelli in CamelCase), Script di migrazione (hook pre/post/end, migrazione dati), Componenti frontend OWL (hook, servizi, ciclo di vita), Ottimizzazione delle prestazioni (prevenzione N+1, operazioni batch, _read_group), Report QWeb (PDF/HTML, formati carta, codici a barre, t‑out), Sicurezza/ACL (regole di record, permessi sui campi, gruppi basati sui privilegi, @api.private), Test (TransactionCase, HttpCase, mocking, asserzioni sul conteggio delle query), Transazioni (savepoint, UniqueViolation, fallimenti di serializzazione), Traduzioni (i18n, file PO, campi traducibili), Vista XML (lista/form/ricerca, modelli di schede kanban, ereditarietà XPath, template QWeb). Da utilizzare quando si scrive, revisiona o si esegue il debug di qualsiasi codice Python o XML di Odoo 19, si creano o modificano moduli, si risolvono problemi di prestazioni o si cercano pattern API Odoo 19 e le migliori pratiche. Include authoring e revisione di asset CSS/SCSS per i componenti aggiuntivi Odoo.
