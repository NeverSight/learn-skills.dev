Odoo 19 開發知識庫，包含 18 本專門指南，涵蓋以下內容：動作（Actions） (ir.actions.*、定時工作（cron jobs）、伺服器動作（server actions）)、控制器（Controllers） (HTTP 路由、端點、認證類型)、資料檔案（Data files） (XML/CSV 記錄、快捷方式、noupdate)、API 裝飾器（API Decorators） (@api.depends、@api.constrains、@api.ondelete、@api.onchange、@api.model、@api.private)、SQL 約束（SQL Constraints） (models.Constraint 取代 _sql_constraints)、資料庫索引（Database Indexes） (models.Index)、模組開發（Module development） (manifest、嚮導、報告)、欄位類型（Field types） (Char、Text、Monetary、關聯欄位)、Manifest 設定（__manifest__.py、相依性、資產套件）、Mixin (mail.thread、mail.activity.mixin、mail.alias.mixin、utm.mixin)、ORM 模型方法（ORM Model methods） (search、CRUD、域過濾、recordsets、CamelCase 模型命名)、遷移腳本（Migration scripts） (pre/post/end 鉤子、資料遷移)、OWL 前端元件（OWL frontend components） (hooks、services、生命周期)、效能優化（Performance optimization） (防止 N+1、批次操作、_read_group)、QWeb 報表（QWeb Reports） (PDF/HTML、紙張格式、條碼、t‑out)、安全性/存取控制（Security/ACL） (record rules、欄位權限、基於特權的群組、@api.private)、測試（Testing） (TransactionCase、HttpCase、mocking、查詢計數斷言)、交易（Transactions） (savepoints、UniqueViolation、序列化失敗)、翻譯（Translations） (i18n、PO 檔、可翻譯欄位)、XML 視圖（XML Views） (list/form/search、看板卡片模板、xpath 繼承、QWeb 模板)。在編寫、審查或除錯任何 Odoo 19 Python 或 XML 程式碼、建立或修改模組、修復效能問題，或查詢 Odoo 19 API 模式與最佳實踐時使用。亦包括 Odoo 附加元件的 CSS/SCSS 資產撰寫與審查。
