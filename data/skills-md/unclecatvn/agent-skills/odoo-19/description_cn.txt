Odoo 19 开发知识库，包含 18 篇专门指南，涵盖 Action（ir.actions.*、定时任务、服务器动作）、Controller（HTTP 路由、端点、认证类型）、数据文件（XML/CSV 记录、快捷方式、noupdate）、API 装饰器（@api.depends、@api.constrains、@api.ondelete、@api.onchange、@api.model、@api.private）、SQL 约束（models.Constraint 替代 _sql_constraints）、数据库索引（models.Index）、模块开发（manifest、向导、报表）、字段类型（Char、Text、Monetary、关系字段）、manifest 配置（__manifest__.py、依赖、资产包）、Mixin（mail.thread、mail.activity.mixin、mail.alias.mixin、utm.mixin）、ORM 模型方法（search、CRUD、域过滤、recordsets、CamelCase 模型命名）、迁移脚本（pre/post/end 钩子、数据迁移）、OWL 前端组件（hooks、services、生命周期）、性能优化（防止 N+1、批量操作、_read_group）、QWeb 报表（PDF/HTML、纸张格式、条形码、t‑out）、安全/ACL（记录规则、字段权限、基于特权的组、@api.private）、测试（TransactionCase、HttpCase、mock、查询计数断言）、事务（保存点、UniqueViolation、序列化失败）、翻译（i18n、PO 文件、可翻译字段）、XML 视图（列表/表单/搜索、看板卡片模板、xpath 继承、QWeb 模板）。在编写、审查或调试任何 Odoo 19 Python 或 XML 代码、创建或修改模块、修复性能问题或查找 Odoo 19 API 规范和最佳实践时使用。还包括 Odoo 插件的 CSS/SCSS 资产编写与审查。
