AI code-writing workflow orchestrator, strictly executes in the order of brainstorming → writing-plans → executing-plans → keyblade-pro-code-rules. User confirmation is required before and after each step; the confirmation stage cannot be skipped or automatically assumed. Supports skill detection; if a required skill is missing, that step is automatically skipped and the subsequent process continues.
