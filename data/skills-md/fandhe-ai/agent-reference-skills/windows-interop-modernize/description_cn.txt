Windows 应用开发（Win32 / WPF / WinForms 互操作）的互操作与现代化参考。Win32 API、CreateWindowExW、WNDPROC、消息循环、COM（IUnknown、CoCreateInstance、ComPtr）、CsWin32、windows-rs、C++/WinRT（winrt::com_ptr、IAsyncOperation、IDL、ABI 互操作）、C#/WinRT（cswinrt、ComWrappers、AOT/裁剪、WindowNative）、XAML Islands（DesktopWindowXamlSource、DesktopChildSiteBridge、WindowsXamlManager）、WPF/WinForms 互操作（HwndHost、HwndSource、WindowsFormsHost、ElementHost）、从 UWP 迁移到 Windows App SDK / WinUI 3（命名空间映射、AppWindow、DispatcherQueue、.NET Upgrade Assistant）。
