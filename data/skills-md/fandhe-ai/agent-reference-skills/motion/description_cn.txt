Motion 动画库（前称 Framer Motion）参考文档。React 组件 motion、AnimatePresence、AnimateView、LayoutGroup、LazyMotion、MotionConfig、Reorder。hooks（钩子）useAnimate、useScroll、useSpring、useTransform、useMotionValue、useInView。原生 JS API animate、scroll、inView、hover、press、stagger、spring、timeline、motionValue。Vue 版（motion‑v）包括 v-motion 指令、useScroll 等 composable，Motion+ 限定组件（Carousel、Cursor、Ticker、Typewriter、AnimateNumber）。transitions / layout / drag / gestures / SVG / scroll 动画、可访问性、性能、bundle 优化、故障排查。
