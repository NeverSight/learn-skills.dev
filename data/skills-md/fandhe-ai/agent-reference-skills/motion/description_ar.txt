مكتبة Motion للرسوم المتحركة (الاسم السابق Framer Motion) – مرجع. مكوّنات React: motion، AnimatePresence، AnimateView، LayoutGroup، LazyMotion، MotionConfig، Reorder. الخطاطيف (hooks): useAnimate، useScroll، useSpring، useTransform، useMotionValue، useInView. واجهة برمجة التطبيقات vanilla JS: animate، scroll، inView، hover، press، stagger، spring، timeline، motionValue. نسخة Vue (motion‑v): توجيه v‑motion، composable مثل useScroll، مكوّنات مخصصة من Motion+ (Carousel، Cursor، Ticker، Typewriter، AnimateNumber). التحولات / التخطيط / السحب / الإيماءات / SVG / رسوم التمرير المتحركة، إمكانية الوصول، الأداء، تحسين حجم الحزمة، استكشاف الأخطاء وإصلاحها.
