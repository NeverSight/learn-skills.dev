cursor-agent quando si avvia la CLI di Cursor Agent ("cursor-agent") per un'installazione di prima esecuzione, una sessione interattiva, un comando one-shot, streaming, persistente, worktree, MCP, plugin, lavoratore o Bedrock.
