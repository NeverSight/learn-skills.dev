claude-code quando si avvia il CLI locale di Claude Code (`claude`) per un'installazione al primo avvio, sessione interattiva, esecuzione singola, streaming, ripresa, worktree o autenticazione, MCP o comando plugin.
