Verwenden Sie diese Fähigkeit beim Erstellen, Ausführen, Wiederanbinden, Auflisten, Stoppen oder Entfernen von Docker‑Sandboxes (dem eigenständigen `sbx`‑CLI, das KI‑Coding‑Agenten in isolierten Micro‑VMs ausführt), selbst wenn der Benutzer nur sagt, er möchte „claude in einer Sandbox ausführen“, „einen Agenten von meinem Repo isolieren“, „einem Agenten sein eigenes Git‑Clone geben“ oder „alte Sandboxes bereinigen“. Deckt `sbx run`/`sbx create` ab (einschließlich der integrierten Agenten claude, codex, cursor, devin, docker-agent, gemini, opencode, shell), Workspace‑Bind‑Mount vs. `--clone`‑Isolation, zusätzliche schreibgeschützte Workspaces, Wiederanbinden mittels `--name`, `sbx ls`/`stop`/`rm`/`prune`, `sbx exec`, `sbx cp` und `sbx ports`.

Bitte immer in der Sprache des Benutzers antworten.
