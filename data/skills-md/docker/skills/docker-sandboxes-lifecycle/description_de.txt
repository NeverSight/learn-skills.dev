Verwende diese Fähigkeit beim Erstellen, Ausführen, Wiederanbinden, Auflisten, Stoppen oder Entfernen von Docker‑Sandboxes (dem eigenständigen `sbx`‑CLI, das KI‑Coding‑Agenten in isolierten Micro‑VMs ausführt), selbst wenn der Benutzer nur sagt, er wolle „claude in einer Sandbox ausführen“, „einen Agenten von meinem Repo isolieren“, „einem Agenten ein eigenes Git‑Clone geben“ oder „alte Sandboxes aufräumen“. Deckt `sbx run`/`sbx create` ab (einschließlich der integrierten Agenten claude, codex, cursor, devin, docker-agent, gemini, opencode, shell), Workspace‑Bind‑Mount vs. `--clone`‑Isolation, zusätzliche nur‑Lese‑Workspaces, Wiederanbinden über `--name`, `sbx ls`/`stop`/`rm`/`prune`, `sbx exec`, `sbx cp` und `sbx ports`. 

Bitte antworte immer in der Sprache des Benutzers.
