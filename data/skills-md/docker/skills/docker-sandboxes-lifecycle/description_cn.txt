在创建、运行、重新附加、列出、停止或删除 Docker Sandbox（独立的 `sbx` CLI，可在隔离的微型虚拟机中运行 AI 编码代理）时使用此技能，即使用户仅说想要“在沙箱中运行 claude”“将代理与我的仓库隔离”“给代理一个自己的 git 克隆”或“清理旧的沙箱”。涵盖 `sbx run`/`sbx create`（包括内置代理 claude、codex、cursor、devin、docker-agent、gemini、opencode、shell）、工作区绑定挂载与 `--clone` 隔离、额外的只读工作区、通过 `--name` 重新附加、`sbx ls`/`stop`/`rm`/`prune`、`sbx exec`、`sbx cp` 和 `sbx ports`。

请始终使用用户的语言作答。
