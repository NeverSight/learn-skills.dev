Verwenden Sie diese Fähigkeit beim Einrichten, Initialisieren oder Dockerisieren eines Projekts, selbst wenn der Nutzer Docker nicht ausdrücklich erwähnt, aber einen Bedarf an containerisierter lokaler Entwicklung, dem Hinzufügen einer Datenbank‑ oder Cache‑Abhängigkeit oder dem Ausführen von Diensten ohne Installation auf Host‑Ebene beschreibt. Deckt die Erstellung von Dockerfile, compose.yaml und .dockerignore mit Docker‑Best‑Practices ab.

Bitte antworten Sie stets in der Sprache des Benutzers.
