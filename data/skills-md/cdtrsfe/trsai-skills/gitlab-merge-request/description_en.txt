Use this when the user requests to submit, create, or initiate an MR/merge request, especially “submit MR to master”; it pushes the current source branch and creates an MR in GitLab with the default target branch set to master and the default assignee set to pu.yuchun.
