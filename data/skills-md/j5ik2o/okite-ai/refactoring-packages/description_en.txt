Analyze the package/module structure of the existing codebase and perform refactoring. Detect and eliminate circular dependencies, split god modules, separate responsibilities, and create and execute a migration plan. Trigger: activated by requests to improve the structure of existing code such as “clean up this dependency,” “eliminate the circular dependency,” “split this module,” or “refactor.” If design guidelines are needed, refer to the package-design skill.
