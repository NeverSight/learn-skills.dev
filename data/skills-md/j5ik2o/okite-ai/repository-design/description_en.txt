Provide design rules and anti-patterns for repositories in DDD. Check aggregate-level naming conventions, method design based on CQS (Command Query Separation), and input/output type constraints. Use when detecting repository design issues during code reviews, new implementations, or refactoring. Target languages: language-agnostic (Java, Kotlin, Scala, TypeScript, Go, Rust, Python, etc.). Trigger: activated by repository-design-related requests such as “review repository design,” “repository name looks wrong,” “findById return value,” “repository returning DTO,” “created repository based on table name,” “aggregate-level repository,” “repository anti-patterns,” or “repository CQS.”
