Crea, esegui il debug e ottimizza i flussi di lavoro test.guide basati su flow.kit in "flow_definition.py" (pacchetto di flussi). Da utilizzare durante la progettazione o la modifica dei flussi con `FlowBuilder`/`add_block_with`/`Assign`, selezionando/collegando i blocchi flow.kit (trigger, test.guide, control-flow, user-code), eseguendo `main.py` per convalidare/eseguire/visualizzare o consultando `.vscode/flow-kit-snippets.code-snippets` e `flow_kit/**/*.pyi` / `site-packages/**/*.pyi` per i dettagli dell'API.
