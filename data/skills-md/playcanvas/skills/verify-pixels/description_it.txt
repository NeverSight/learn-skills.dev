Da utilizzare quando si modifica il codice di rendering PlayCanvas che non deve modificare visibilmente l'immagine renderizzata (ottimizzazione delle chiamate di disegno, istanze hardware, refactoring di shader o materiali, operazioni di noise o lightmap bake o scambi di pipeline di risorse) prima della spedizione.
