Wrap a researcher's policy for the Manifold platform, then prove it with check_compatibility, verify, and a live run of the driver (the live run is skipped, with the user's consent, on a machine that cannot run the model). Use when the model loads into the built container. For policies where the model runs on the user's own inference server (Modal, private HTTPS box), use `/wrap-remote-policy` instead.
