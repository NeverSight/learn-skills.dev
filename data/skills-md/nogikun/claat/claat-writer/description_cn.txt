根据输入的内容和上下文（项目源代码、README、配置文件、已有文档、过程笔记、日志、URL 等），编写一个 manual.md，使 Google Codelabs 的 claat 能将其转换为 HTML，并以日语的逐步指南形式编写，使中学生也能毫不犹豫地跟随。放入实际可运行的代码、真实的输出以及带有证据的截图，满足 cmd/claat-tools 的 lint 规则（七个元数据键、唯一的 `#` 标题、每个 `##` 步骤的 `Duration` 行以及相对图片的存在），并在交付前通过自然日语检查。每当用户请求创建、起草或重写 codelab / code lab / claat / 使用说明手册 / 教程 / 设置指南 / 使用文档时，你必须始终使用此方法。即使请求中未提及 claat，例如“为此项目创建流程指南”、“编写 manual.md”或“总结设置步骤”，也要以同样方式处理。将生成的 .md 转换为 HTML 的步骤由 claat‑build 技能完成。

请始终用用户的语言作答。

请始终用用户的语言作答。
