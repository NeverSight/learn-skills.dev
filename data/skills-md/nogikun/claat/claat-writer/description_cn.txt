根据输入内容和上下文（项目源代码、README、配置文件、已有文档、流程说明、日志、URL 等），生成一个 `manual.md`，让 Google Codelabs 的 claat 能将其转换为 HTML。该文档应采用日语编写，步骤清晰，连中学生也能毫不迷失。要求：

- 包含实际可运行的代码、真实的输出，以及带有合理说明的截图；
- 满足 `cmd/claat‑tools` 的 lint 规则：包含七个元数据键、仅有一个 `#` 标题、每个步骤都有 `##` 开头的 `Duration` 行，并且使用相对路径的图片；
- 完成后通过自然日语检查再交付。

无论用户请求创建、起草或改写 codelab / code lab / claat / 步骤指南 / 手册 / 教程 / 安装指南 / 使用文档，只要涉及此类需求，都必须遵循上述流程。即使请求中未提及 “claat”，如 “为该项目创建指南”、 “编写 manual.md” 或 “总结安装步骤”，也应采用同样的做法。生成的 `.md` 转换为 HTML 的工作由 claat‑build 技能完成。

请始终使用用户的语言进行回复。
