From the input content and context (project source, README, configuration files, existing documents, procedural notes, logs, URLs, etc.), produce a `manual.md` that Google Codelabs' claat can convert to HTML, written as a Japanese step‑by‑step guide that even middle‑school students can follow without getting lost. Include actual working code, real output, and screenshots with proper justification; satisfy the cmd/claat‑tools lint rules (seven metadata keys, a single `#` title, a `##` step‑specific `Duration` line, and the existence of relative images), then pass a Japanese check using natural‑japanese before delivering. Whenever a user asks for the creation, drafting, or rewriting of a codelab / code lab / claat / step‑by‑step guide / manual / tutorial / setup guide / usage document, you must always use this approach. Even if the request is phrased without mentioning “claat,” such as “Create a guide for this project,” “Write a manual.md,” or “Summarize the setup steps,” you should still apply the same process. The conversion of the generated `.md` to HTML is handled by the claat‑build skill.
