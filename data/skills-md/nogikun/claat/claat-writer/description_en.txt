From the entered content and context (project source, README, configuration files, existing documents, procedural notes, logs, URLs, etc.), write a manual.md that Google Codelabs' claat can convert to HTML, formatted as a Japanese step-by-step guide that even middle‑school students can follow without hesitation. Place actual runnable code, real output, and screenshots with evidence, satisfy the cmd/claat-tools lint rules (seven metadata keys, a single `#` title, a `##` step‑specific `Duration` line, and the existence of relative images), and pass a natural‑Japanese language check before delivering. Whenever a user requests the creation, drafting, or rewriting of a codelab / code lab / claat / instruction manual / tutorial / setup guide / usage documentation, you must always use this approach. Even when the request is phrased without mentioning claat—e.g., “Create a procedural guide for this project,” “Write a manual.md,” or “Summarize the setup steps”—apply it in the same way. The step of converting the generated .md to HTML is handled by the claat‑build skill. 

Please always respond in the user's language.
