Let AI truly understand large codebases. For projects with multiple repositories, many microservices, and years of iteration, use architecture reverse engineering + Graph RAG map + CLI multi-language AST to compress massive code into a structured knowledge base—each conclusion is traceable to code lines, and each relationship is annotated with confidence scores.

Applicable scenarios: The project has 10+ repositories or microservices, and AI cannot grasp the whole picture by reading the code directly, leading to inaccurate answers and high token costs.

Outputs: N component design documents + overall architecture overview + bridge documentation + Graph RAG maps (G1~G9) + _manifest.json + teamai extract graph (teamwiki/).

Trigger: team-wiki-codebase, code-to-knowledge, code knowledge base, architecture analysis, architecture reverse engineering  
Prerequisites: Accessible source code directories (supporting multiple repositories); the `references/` and `scripts/` directories under this skill.
