Da usare quando si lavora con la configurazione bmthd/dotfiles — cambiando il repository stesso (mise tools, install.sh, docs, le competenze che vi risiedono) e aprendo un PR, o applicando un aggiornamento disponibile a QUESTA macchina dopo che la shell ha stampato "dotfiles の更新があります". Richiamare come /dotfiles pr <modifica> o /dotfiles applicare.
