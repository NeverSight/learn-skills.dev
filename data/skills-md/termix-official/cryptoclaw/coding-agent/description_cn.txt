通过后台进程将编码任务委托给 Codex、Claude Code 或 Pi 代理。在以下情况下使用：(1) 构建/创建新功能或应用程序，(2) 查看 PR（在临时目录中生成），(3) 重构大型代码库，(4) 需要文件探索的迭代编码。不适用于：简单的单行修复（只需编辑）、读取代码（使用读取工具）、聊天中线程绑定的 ACP 线束请求（例如在 Discord 线程中生成/运行 Codex 或 Claude 代码；在运行时使用sessions_spawn：“acp”）或 ~/clawd 工作区中的任何工作（此处切勿生成代理）。需要支持 pty:true 的 bash 工具。
