全自動模式——對每個 issue 依序執行 tdd2 再 aacpd，必須在開始下一個 issue 前，完整完成當前 issue（實作+驗證+合併+推送+部署），不會批次處理，也不會在執行途中提示。若未提供 issue 編號，會列出 issues/ 目錄中剩餘的所有檔案，並一次詢問是否要全部執行。可選擇為每個 issue 使用獨立的暫時 git worktree（`worktree` 關鍵字）以確保隔離——仍然一次只處理一個 issue，且每個 issue 會在下一個開始前立即合併回 main。當使用者輸入「/autotdd」或「autotdd」時使用此模式。

請始終以使用者的語言回覆。
