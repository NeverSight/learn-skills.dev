استخدم عندما يُطلب منك إعداد أو تثبيت أو تحسين إطار جودة الوكيل في مستودع (قواعد مع بوابات، عقود بنية، اختبارات مثبتة‑حمراء، رَتّاقات، كتيبات إرشادية)، أو عندما يقول المستخدم "gap‑trap"، "gap‑trap setup"، "gap‑trap refine"، "gate the rules"، أو "make the agents' code match the rules".
