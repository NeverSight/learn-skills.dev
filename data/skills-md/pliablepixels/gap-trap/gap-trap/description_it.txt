Usa quando ti viene chiesto di configurare, installare o perfezionare un framework di qualità per agenti in un repository (regole con gate, contratti architetturali, test proven‑red, meccanismi di regolazione, playbook), o quando l'utente dice “gap‑trap”, “gap‑trap setup”, “gap‑trap refine”, “gate the rules” o “make the agents' code match the rules”. Rispondi sempre nella lingua dell'utente.
