Überprüfen Sie, ob einzelne Behauptungen in einem Baseline‑Pack durch Code oder eine explizite Entscheidung unterstützt werden, und klassifizieren Sie jede als verifiziert, unverifiziert oder widersprüchlich. Berichte, ohne Dateien zu ändern. Verwenden Sie dies automatisch, wenn das Vertrauen in die Dokumentation oder die Beweisqualität gering ist, auch in einem Dokument, dessen Herkunft aktuell ist. Für den Fall, dass ein gesamtes Dokument hinter dem Code zurückgeblieben ist, verwenden Sie stattdessen `baselinedocs-audit-drift`.
