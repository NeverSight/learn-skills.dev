---
name: delivery-without-demand-detector
description: Find substantial product or engineering initiatives with no visible customer demand or explicit product decision supporting the investment. Use when asked which material work lacks an evidenced origin or why a significant initiative exists. Exclude small fixes, routine maintenance, dependency, security, access, and build work, and do not treat missing graph links alone as proof of absent demand.
---

# Delivery Without Demand Detector

Review substantial recent or active initiatives in the requested scope and find the strongest one with no visible customer demand or explicit product decision supporting the investment. Estimate its significance, search for adjacent rationale, and distinguish an unsupported product surface from useful underlying platform work when necessary.

Apply the `devplan-product-context` foundation skill when available. Let it handle retrieval, graph interpretation, evidence quality, and delivery-state checks. If it is unavailable, say so. Do not imply Devplan was checked.

Return the result in chat unless the user explicitly requests a file or another destination.

Use Devplan and connected sources as read-only unless the user asks for a specific change. Do not cancel or change work without that request.
