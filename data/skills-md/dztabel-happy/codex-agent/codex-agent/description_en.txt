Operating the full OpenAI Codex CLI as a project manager. Includes: knowledge-base maintenance (auto-tracking Codex’s latest features), task execution (prompt design → execution → monitoring → QA → iteration → reporting), configuration management (feature flags / models / skills / MCP). Uses tmux for an interactive TUI; async wake-up via notify hooks + pane monitor. NOT for: simple one-line edits (use edit), file reading (use read), quick Q&A (answer directly).
