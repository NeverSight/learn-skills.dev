Obtener una fuente MapLibre GL JS para renderizar realmente: TileJSON `url` versus plantillas de `tiles` cableadas a mano, hacer coincidir los nombres de `capa de origen` con el esquema de mosaico, orden de capas e insertar etiquetas debajo, `promoteId` y los identificadores de características que presentan las necesidades de estado, y los fallos de CORS y glifos detrás de un mapa en blanco. Úselo cuando se configura una fuente pero no se dibuja nada, o cuando setFeatureState no hace nada.
