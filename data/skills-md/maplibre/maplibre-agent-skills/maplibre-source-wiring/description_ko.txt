실제로 렌더링할 MapLibre GL JS 소스 가져오기 — TileJSON `url` 대 직접 연결한 `tiles` 템플릿, `source-layer` 이름을 타일 스키마와 일치, 레이어 순서 및 레이블 아래 삽입, `promoteId` 및 기능 상태에 필요한 기능 ID, 빈 지도 뒤에 있는 CORS 및 문자 모양 오류. 소스가 구성되었지만 아무것도 그려지지 않거나 setFeatureState가 아무것도 수행하지 않는 경우에 사용합니다.
