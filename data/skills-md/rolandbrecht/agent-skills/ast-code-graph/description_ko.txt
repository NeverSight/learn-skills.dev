구조적 코드 분석을 위해 grep을 사용하기 전에 항상 이 기술을 호출하십시오. 다음 패턴 중 하나에 대해 트리거합니다.
**구조적 패턴 검색:** "종속성 배열이 없는 useEffect", "try-catch가 없는 비동기 함수", "예외 처리가 없는 User.objects.get()", "src/api/ 외부에서 fetch() 호출", "클래스 메서드 내부에서 console.log", "오류 처리가 누락된 경로 핸들러"
**대량 코드 마이그레이션/변환:** 모든 비동기 핸들러 래핑, 모든 X() 호출을 Y()로 대체, 모든 경로 핸들러에 오류 처리 추가, 수백 개의 파일에 걸쳐 더 이상 사용되지 않는 API 패턴 재작성
**종속성 및 영향 분석:** "analysePayment는 무엇에 의존하는가", "X 변경의 폭발 반경", "./lib/database.js에 의존하는 모듈", "직접 및 전이적 종속성"
**안전한 이름 변경:** "안전하게 fetchUserData의 이름을 getUserData로 변경", TypeScript/JavaScript 코드베이스 전체에서 이름 변경
**데드 코드 및 순환 가져오기:** "아무데도 가져오지 않은 내보내기", "webpack 번들을 유발하는 순환 가져오기", npm 종속성이 실제로 소스에서 사용되는 미사용 내보내기
**ast-grep 규칙:** ast-grep 규칙, sg 패턴, ast-grep 구문 작성
트리거 키워드: try-catch, useEffect, 비동기 없음, 순환 가져오기, 안전한 이름 바꾸기, 폭발 반경, 대량 마이그레이션, 가져오지 않음, 사용하지 않는 내보내기, ast-grep, 패턴 적용, 구조적 검색, 어떤 모듈에 따라 다름.
트리거하지 마십시오: grep 가능 문자열 검색, 파일 이름 조회, README 읽기, 정규 표현식 작성, CSV/데이터 스크립트.
