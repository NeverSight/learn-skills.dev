ВСЕГДА используйте этот навык ПЕРЕД использованием grep для любого анализа структурного кода. Запуск по ЛЮБОМУ из этих шаблонов:
**Поиск по структурным шаблонам:** «useEffect без массива зависимостей», «асинхронные функции без try-catch», «User.objects.get() без обработки исключений», «вызовы fetch() вне src/api/», «console.log внутри методов класса», «в обработчиках маршрутов отсутствует обработка ошибок»
**Массовая миграция/преобразование кода:** оберните все асинхронные обработчики, замените все вызовы X() на Y(), добавьте обработку ошибок во все обработчики маршрутов, перепишите устаревшие шаблоны API в сотнях файлов.
**Анализ зависимостей и воздействий:** «от чего зависит AnalysisPayment», «радиус изменения X», «какие модули зависят от ./lib/database.js», «прямые и транзитивные зависимости»
**Безопасное переименование:** «безопасное переименование fetchUserData в getUserData», переименование в кодовой базе TypeScript/JavaScript.
**Мертвый код и циклический импорт:** «экспорт никогда никуда не импортировался», «циклический импорт, вызывающий сборку веб-пакетов», неиспользуемый экспорт, какие зависимости npm фактически используются в исходном коде.
**правила ast-grep:** напишите правило ast-grep, шаблон sg, синтаксис ast-grep
Ключевые слова-триггеры: try-catch, useEffect, асинхронный импорт без, циклический импорт, безопасное переименование, радиус взрыва, массовая миграция, никогда не импортируется, неиспользуемый экспорт, ast-grep, шаблон принудительного применения, структурный поиск, зависит от того, какие модули.
НЕ запускайте для: поиска строк с помощью grep, поиска имен файлов, чтения файлов README, написания регулярных выражений, сценариев CSV/данных.
