構造コード分析に grep を使用する前に、必ずこのスキルを呼び出してください。次のパターンのいずれかでトリガーされます。
**構造パターン検索:** 「依存関係配列のない useEffect」、「try-catch のない非同期関数」、「例外処理のない User.objects.get()」、「src/api/ の外部での fetch() 呼び出し」、「クラス メソッド内の console.log」、「エラー処理が欠落しているルート ハンドラー」
**コードの一括移行/変換:** すべての非同期ハンドラーをラップし、すべての X() 呼び出しを Y() に置き換え、すべてのルート ハンドラーにエラー処理を追加し、数百のファイルにわたる非推奨の API パターンを書き換えます。
**依存関係と影響の分析:** 「analyzePayment は何に依存しているか」、「変化する X の爆発範囲」、「どのモジュールが ./lib/database.js に依存しているか」、「直接的および推移的な依存関係」
**安全な名前変更:** 「fetchUserData を getUserData に安全に名前変更」、TypeScript/JavaScript コードベース全体で名前を変更します。
**デッドコードと循環インポート:** 「どこにもインポートされなかったエクスポート」、「Webpack バンドルを引き起こす循環インポート」、未使用のエクスポート、npm 依存関係が実際にソースで使用されている
**ast-grep ルール:** ast-grep ルール、sg パターン、ast-grep 構文を記述します。
トリガーキーワード: try-catch、useEffect、非同期、循環インポート、安全な名前変更、ブラスト半径、一括移行、インポートされない、未使用のエクスポート、ast-grep、パターンの強制、構造検索、依存、モジュール。
grep 可能な文字列検索、ファイル名の検索、README の読み取り、正規表現の書き込み、CSV/データ スクリプトをトリガーしないでください。
