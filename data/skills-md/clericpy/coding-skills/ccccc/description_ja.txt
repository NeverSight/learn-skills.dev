最新の5つのコミットの形式を学び、会社の厳格な基準に従ってGitコミットを生成して直接作成します（呼び出しは承認を構成し、それ以上の確認は必要ありません）。不特定のIDのようなフィールドは最新の値を再利用し、コミットメッセージは改行なしで200文字以下、単一の追加は500行以下、プッシュしないでください。手動によるユーザー呼び出しのみに制限されています（手動のみ。ユーザーが明示的に要求した場合にのみ呼び出します）。
