Learn the format of the last five commits, generate and directly create a Git commit according to the company’s strict standards (calling grants authorization, no further confirmation needed): unspecified ID-like fields reuse the most recent value, commit description ≤200 characters without line breaks, a single addition ≤500 lines, never push. Only for manual user invocation (manual‑only; invoke only when the user explicitly asks).
