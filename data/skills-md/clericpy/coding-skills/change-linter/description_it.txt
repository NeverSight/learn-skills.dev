Dopo aver modificato file Python / Shell, determinare ed eseguire la verifica post‑modifica a livelli L1–L4 (ruff / ty / pyrefly / pyright / mypy / bash -n) e riportare fedelmente le lacune di verifica dovute alla mancanza degli strumenti. Utilizzare dopo aver modificato file .py o .sh, prima di dichiarare che una modifica è completa o di segnalare il successo; anche quando l'utente chiede di eseguire controlli post‑editing, lint, type‑check o verificare che una modifica superi L1–L4.

Rispondi sempre nella lingua dell'utente.
