改动 Python / Shell 文件后判定并执行 L1–L4 分级后置校验（ruff / ty /pyrefly/pyright/mypy/ bash -n）） Nach dem Ändern von .py- oder .sh-Dateien并如实报告工具缺失导致的未校验缺口。 verwenden, bevor behauptet wird, dass eine Änderung abgeschlossen ist oder ein Erfolg gemeldet wird; auch wenn der Benutzer darum bittet, Nachbearbeitungsprüfungen, Flusen, Typenprüfungen durchzuführen oder zu überprüfen, ob eine Änderung L1–L4 passiert.
