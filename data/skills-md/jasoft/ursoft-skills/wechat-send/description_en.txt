Use when you need to automatically send messages to WeChat contacts on macOS and want to bypass Chinese input-method interference by pasting from the clipboard; if necessary, reuse the GUI and OCR commands provided by localmac-ai-ocr to take screenshots, locate text, click, and verify.
