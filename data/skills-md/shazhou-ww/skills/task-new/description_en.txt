Record a repository implementation task only when the user explicitly invokes /task-new. Use active conversation context when available; never trigger from an ordinary implementation request or proposal alone.
