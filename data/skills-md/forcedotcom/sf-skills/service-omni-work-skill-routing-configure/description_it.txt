Da utilizzare per creare una regola Omni WorkSkillRouting (criteri di competenza basati sul campo): mappare un valore di campo di entità correlata a una competenza Omni richiesta (ad esempio aggiungere la competenza vocale quando Case.Origin = Phone), distribuito come un record WorkSkillRouting tramite l'API dei metadati. Idempotente tramite i file API dei metadati[].state signal; una regola per entità correlata. Trigger: aggiungere instradamento di competenze basato su campo, instradare tramite un campo Caso a una competenza, instradare tramite un campo MessagingSession a una competenza, configurare l'instradamento di competenze basato su attributi, richiedere una competenza Omni per un valore di campo. Non utilizzare per creare la competenza o associare agenti ad essa (service-omni-skills-based-routing-configure), per creare utenti agente o per configurare code.
