نقل مجموعات واجهة المستخدم القديمة في Salesforce إلى LWC الحديثة — التحقق من اكتمال تحويل Aura → LWC وهجرة صفحة الاستضافة من Lightning Out Beta إلى Lightning Out 2.0. تشغيل (TRIGGER) عند "verify Aura to LWC migration completeness"، "migrate Lightning Out Beta to LO 2.0"، "upgrade $Lightning.use() to <lightning-out-application>"، "replace hardcoded LO tokens with OAuth PKCE"، "write an LO 2.0 host page"، أو صفحة Lightning Out خارجية توقفت عن العرض؛ أيضًا ملفات `.cmp`/`.js-meta.xml` جنبًا إلى جنب مع LWC. لا تُشغل (DO NOT TRIGGER) للتحويل الأولي من Aura إلى LWC (استخدم experience-aura-lwc-migrate)، أو تحويل من React إلى LWC، أو بناء LWC جديد (استخدم experience-lwc-design-generate).
