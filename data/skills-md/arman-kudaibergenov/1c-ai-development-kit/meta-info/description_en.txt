Analysis of the structure of a 1C metadata object from XML export—attributes, tabular sections, forms, movements, types. Use it to study object structures and as a preparatory step when writing queries and code that work with objects.
