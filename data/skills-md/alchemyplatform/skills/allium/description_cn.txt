查询 Allium API 以获取钱包盈亏（当前 + 历史，按钱包和按代币）、持仓时间序列历史、Hyperliquid HyperCore 交易数据（信息、成交、订单、订单簿），以及跨 70 多条链的自定义 SQL 分析。**不适用于**代币价格、代币元数据、当前钱包余额快照、交易转账历史或 NFT 元数据——这些请使用 `alchemy-cli`（实时工作）、`alchemy-mcp`、`alchemy-api`（应用代码）或 `agentic-gateway`（无需 API 密钥）。需要在 `~/.allium/credentials` 中提供 Allium 凭证。  
请始终使用用户的语言回复。
