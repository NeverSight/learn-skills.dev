查詢 Allium API 以取得錢包盈虧（即時及歷史，依錢包與代幣分類）、持有量時間序列歷史、Hyperliquid HyperCore 交易資料（資訊、成交、訂單、委託簿），以及跨 70 多條鏈的自訂 SQL 分析。**不適用於**代幣價格、代幣元資料、即時錢包餘額快照、交易轉帳歷史或 NFT 元資料——對於這些請使用 `alchemy-cli`（即時工作）、`alchemy-mcp`、`alchemy-api`（應用程式碼）或 `agentic-gateway`（無 API 金鑰）。需要在 `~/.allium/credentials` 中提供 Allium 認證。

請始終以使用者的語言回覆。
