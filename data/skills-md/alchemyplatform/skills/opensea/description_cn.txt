查询 OpenSea 市场数据——挂单、报价、销售/事件、底价、合集统计、空投、属性——并通过官方 `@opensea/cli` 和 OpenSea REST API 在以太坊、Base、Arbitrum、Optimism、Polygon 等链上执行 Seaport 交易。包括对合集/NFT/代币/账户的搜索。**不适用于**普通 NFT 元数据读取（名称、图片、属性、所有权）、代币元数据、当前钱包余额、交易历史或实时 RPC 读取——这些请使用 `alchemy-cli`（实时）、`alchemy-mcp`、`alchemy-api`（应用代码）或 `agentic-gateway`（无需 API 密钥）。若仅进行跨链代币交换（无市场上下文），请使用 `lifi` 生态系统技能。需要 `OPENSEA_API_KEY`（可即时通过 API 获取）。

请始终使用用户的语言作答。
