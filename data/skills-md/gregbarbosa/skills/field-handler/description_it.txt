Da utilizzare quando il lavoro del progetto diventa complesso o si divide in filoni indipendenti che potrebbero progredire in parallelo in sessioni separate, ad esempio diversi progetti correlati che si muovono verso un obiettivo o un singolo sforzo che si ramifica in parti che non dipendono l'una dall'altra. Questa abilità EMERGE l'opzione e concorda SEMPRE l'ambito con l'utente prima che generi qualcosa. Richiede herdr >= 0.9.0 e HERDR_ENV=1.
