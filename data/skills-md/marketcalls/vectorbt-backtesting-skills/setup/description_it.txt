Configura l'ambiente di backtest Python. Rileva il sistema operativo, crea un ambiente virtuale, installa le dipendenze (openalgo, ta-lib, vectorbt, plotly) e crea la struttura delle cartelle di backtest.
