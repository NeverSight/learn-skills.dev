Used to hide or restore skills; supports single or batch operations; manages the parent folder path of skills; prevents hiding itself; supports multi-directory search and specified directory operations; supports one-click hiding/restoring of all skills. Use this skill when the user needs to [hide or restore skills].
