Identifica, convalida e distribuisci ottimizzazioni Node.js sicure per la produzione con il tempo di esecuzione come obiettivo principale. Da utilizzare quando gli utenti chiedono di ridurre la latenza (p50/p95/p99), migliorare la velocità effettiva e quindi ridurre la pressione di CPU/memoria/loop di eventi/FD o riprovare l'amplificazione, utilizzando flussi di lavoro un PR per miglioramento con benchmark.
