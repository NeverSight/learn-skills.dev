Batch schedule and manage GPU containers through the AutoDL private-cloud elastic-deployment API. Supports creating deployments (ReplicaSet/Job/Container), querying container status, setting replica counts, stopping/deleting deployments, etc. Used when users mention AutoDL, elastic deployment, GPU container scheduling, batch container creation, or private-cloud deployment.
