إنشاء مقاطع فيديو من كود مكوّنات React/Remotion عبر inference.sh. مرّر شفرة TSX، احصل على MP4. يدعم جميع واجهات Remotion API: useCurrentFrame، useVideoConfig، spring، interpolate، AbsoluteFill، Sequence. يمكن ضبط الدقة، وعدد الإطارات في الثانية، والمدة، والترميز. يُستخدم لـ: توليد الفيديو برمجيًا، الرسوم المتحركة، تصميم الحركة، الفيديوهات المستندة إلى البيانات، تحويل إعلانات React إلى فيديو. المحفزات: remotion، إنشاء فيديو من الكود، tsx إلى فيديو، فيديو React، فيديو برمجي، remotion render، كود إلى فيديو، فيديو متحرك، كود رسومات الحركة، فيديو رسوم متحركة React.
