نفّذ كود بايثون في بيئة آمنة معزولة عبر [inference.sh](https://inference.sh). مثبت مسبقًا: NumPy، Pandas، Matplotlib، requests، BeautifulSoup، Selenium، Playwright، MoviePy، Pillow، OpenCV، trimesh، وأكثر من 100 مكتبة أخرى. استخدمه لـ: معالجة البيانات، استخراج الويب، تعديل الصور، إنشاء الفيديو، معالجة نماذج ثلاثية الأبعاد، توليد PDF، استدعاءات API، سكريبتات الأتمتة. المشغلات: python، تنفيذ الكود، تشغيل السكريبت، استخراج الويب، تحليل البيانات، معالجة الصور، تحرير الفيديو، نماذج ثلاثية الأبعاد، الأتمتة، pandas، matplotlib.
