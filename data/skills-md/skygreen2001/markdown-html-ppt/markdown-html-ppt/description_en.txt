Convert Markdown documents or text into presentations. First transform them into interactive HTML PPTs via reveal.js, then optionally convert to PowerPoint files. Supports rich Markdown syntax (bold, italic, code, quotes, code blocks), 5 beautiful themes, smart pagination algorithm. Output formats: HTML (reveal.js) or PowerPoint (.pptx). Version: v2.4.0
