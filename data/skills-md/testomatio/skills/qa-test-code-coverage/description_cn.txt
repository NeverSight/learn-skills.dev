将测试用例（手动（markdown）和自动 e2e（Playwright、Cypress、WebdriverIO、CodeceptJS、Puppeteer、Appium）映射到源代码文件并生成由 `@testomatio/reporter --filter "coverage:..."` 使用的 `coverage.tests.yml` 映射。当用户想要仅运行受代码更改影响的测试、生成覆盖文件（手动、e2e 或两者）、在测试和源代码之间构建可跟踪性矩阵或设置更改感知回归时，请使用此技能。
