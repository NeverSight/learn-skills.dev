Habilidad de punto de entrada principal para Transloadit. Dirija hacia la habilidad derecha `integrate-*`, `transform-*` o `docs-*` y prefiera ejecutar a través de `npx -y @transloadit/node ...` (CLI) para un comportamiento determinista.
