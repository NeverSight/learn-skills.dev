Abilità principale del punto di ingresso per Transloadit. Indirizza alla competenza `integrate-*`, `transform-*` o `docs-*` corretta e preferisci l'esecuzione tramite `npx -y @transloadit/node ...` (CLI) per un comportamento deterministico.
