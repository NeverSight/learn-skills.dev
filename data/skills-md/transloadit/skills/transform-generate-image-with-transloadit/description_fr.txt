Génération d'image unique (invite -> fichier image) à l'aide de Transloadit via la CLI `transloadit`. Préférez les modèles intégrés (`builtin/generate-image@latest`) et téléchargez les sorties localement via `-o`.
