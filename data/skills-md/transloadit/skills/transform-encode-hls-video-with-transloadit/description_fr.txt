Encodage HLS unique (vidéo locale -> rendus HLS + playlist) à l'aide de Transloadit via la CLI `transloadit`. Préférez les modèles intégrés (`builtin/encode-hls-video@latest`) et téléchargez les sorties localement via `-o`.
