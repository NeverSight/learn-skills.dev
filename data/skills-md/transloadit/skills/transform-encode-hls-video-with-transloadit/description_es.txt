Codificación HLS única (video local -> representaciones HLS + lista de reproducción) usando Transloadit a través de la CLI `transloadit`. Prefiera las plantillas integradas (`builtin/encode-hls-video@latest`) y descargue los resultados localmente a través de `-o`.
