검사 우선, 사실 우선 워크플로를 통해 JSON 아티팩트를 읽기 가능한 UI로 렌더링합니다. Codex가 JSON 파일, JSON 생성 셸 명령, CLI 출력 아티팩트 또는 알 수 없는 구조화된 페이로드를 하네스 또는 터미널 기본 참조 렌더러를 통해 기본적으로 렌더링할 수 있는 선언적 UI 사양으로 변환해야 하는 경우(정렬된 배열로 인코딩된 반복 하위 레코드가 있는 경우 포함) 사용합니다.
