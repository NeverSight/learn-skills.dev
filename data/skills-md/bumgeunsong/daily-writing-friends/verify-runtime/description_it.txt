Da utilizzare quando si modifica il codice del flusso di dati (chiamate API, doppia scrittura, mutazioni, letture/scritture di database) per verificare il comportamento di runtime tramite log di sviluppo strutturati, prima di dichiarare che la modifica funziona.
