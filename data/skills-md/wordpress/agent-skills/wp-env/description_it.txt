Usa quando configuri, imposti o risolvi problemi di ambienti di sviluppo locali di WordPress con @wordpress/env (wp‑env). Si attiva quando vengono menzionati wp‑env, lo sviluppo locale di WordPress, WordPress basato su Docker o richieste di avviare/arrestare/configurare un'istanza locale di WordPress.

Rispondi sempre nella lingua dell'utente.
