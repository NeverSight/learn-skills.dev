Gestisci e ispeziona i processi in background per gli agenti utilizzando la CLI bgproc. Da utilizzare quando viene richiesto di avviare, arrestare, elencare, pulire, controllare lo stato o visualizzare i log per i processi gestiti da bgproc o quando un'attività necessita di un gestore di processi con output JSON.
