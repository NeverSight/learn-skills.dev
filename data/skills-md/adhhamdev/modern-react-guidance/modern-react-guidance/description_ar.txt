إرشادات موثوقة لتطوير React الحديث 19+ (الإجراءات، use، Compiler، انتقالات العرض، مراجع الـ Fragment، النشاط، المتصفح، useEffectEvent). يُستخدم عند كتابة، مراجعة، إعادة هيكلة أو ترحيل مكوّنات React، النماذج، جلب البيانات، واجهة المستخدم المتزامنة، أو الترقية إلى React 19+. يتم التفعيل على React، React 19، useActionState، useOptimistic، forwardRef، جلب البيانات باستخدام useEffect، React Compiler، ViewTransition، أنماط Suspense أو codemods. دائمًا يُفضَّل اتباع أحدث الأنماط الرسمية على بيانات التدريب.
