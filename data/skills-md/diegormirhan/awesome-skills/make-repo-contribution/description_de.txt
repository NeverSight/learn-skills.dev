Alle Änderungen am Code müssen den im Repository dokumentierten Richtlinien folgen. Bevor ein Issue erstellt, ein Branch angelegt, Commits erzeugt oder ein Pull‑Request (oder PR) erstellt wird, muss eine Suche durchgeführt werden, um sicherzustellen, dass die richtigen Schritte befolgt werden. Immer wenn Sie aufgefordert werden, ein Issue zu erstellen, Commit‑Nachrichten zu verfassen, Code zu pushen oder einen PR zu erstellen, nutzen Sie diese Fähigkeit, damit alles korrekt erledigt wird.

Bitte antworten Sie stets in der Sprache des Benutzers.
