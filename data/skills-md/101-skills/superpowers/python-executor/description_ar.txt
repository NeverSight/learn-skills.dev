نفّذ كود بايثون في بيئة محمية وآمنة عبر [inference.sh](https://inference.sh). مثبت مسبقًا: NumPy، Pandas، Matplotlib، requests، BeautifulSoup، Selenium، Playwright، MoviePy، Pillow، OpenCV، trimesh، وأكثر من 100 مكتبة أخرى. استخدمه لـ: معالجة البيانات، استخراج البيانات من الويب، تعديل الصور، إنشاء الفيديو، معالجة نماذج ثلاثية الأبعاد، توليد ملفات PDF، استدعاءات API، سكريبتات الأتمتة. المحفزات: python، تنفيذ الكود، تشغيل السكريبت، استخراج البيانات من الويب، تحليل البيانات، معالجة الصور، تحرير الفيديو، نماذج ثلاثية الأبعاد، الأتمتة، pandas، matplotlib.  

الرجاء دائمًا الرد بلغة المستخدم.
