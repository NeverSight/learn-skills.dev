将由代理编写的应用从代码仓库部署到用户自己的账号上的实时生产环境，使用他们选择的提供商（托管、数据库、认证、支付、邮件、域名/DNS）。用户连接账号并批准更改；支持的绑定操作通过本地 CLI 运行，并生成带有明确限制的验证证据。当用户想要发货、部署、上线、启动、发布或将其应用放到线上，或请求配置环境变量、Webhook、认证设置（注册、邮件确认、密码策略）、真实的注册→确认邮件→登录流程、密码恢复、两个用户之间的账户隔离、认证重定向、邮件 DNS 或自定义域名时使用。

请始终用用户的语言作答。
