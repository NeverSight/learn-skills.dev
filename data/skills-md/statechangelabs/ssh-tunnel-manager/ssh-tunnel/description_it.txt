Gestisci i tunnel SSH in modo dichiarativo. Da utilizzare quando l'utente desidera creare, avviare, arrestare, abilitare, disabilitare o verificare lo stato dei tunnel di port forwarding SSH. Da utilizzare anche quando l'utente menziona tunnel SSH, port forwarding, bastion host o connessione a database/servizi remoti tramite SSH.
