Rassemblez et mettez en cache les informations du cluster SLURM (partitions, GPU, mémoire, limites de QOS). Génère un document récapitulatif lisible lors de la première exécution ; renvoie le document mis en cache lors des exécutions suivantes.
