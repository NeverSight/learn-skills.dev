当用户请求“在 JavaScript 中绘制每一帧”“制作不使用图像资源的动画”“用代码/在画布上实现动画”“制作手绘风格的动画视频”“把这变成短篇动画电影”“制作用代码绘制的动画解释视频”“用我的照片制作动画故事/绘本”或“制作类似 Opus 5.5 的零资源动画”等时，应使用此技能。该技能生成一个单独的自包含 HTML 文件，帧在 HTML canvas 上计算（可寻帧、确定性），并将其渲染为 MP4，同时自行检查结果。可与配乐技能配合使用，以生成代码合成的音乐。**不适用于数据图表（使用 chart‑animation）**、**纯文本运动（使用 kinetic‑typography）**，或**3D/WebGL（使用 threejs‑animation / shader‑glsl 来自 webgl‑animation‑skills）**。

请始终使用用户的语言回复。
