Diese Fähigkeit sollte verwendet werden, wenn der Nutzer darum bittet, „jeden Frame in JavaScript zu zeichnen“, „eine Animation ohne Bildressourcen zu erstellen“, „dies in Code / auf einer Leinwand zu animieren“, „ein handgezeichnetes animiertes Video zu machen“, „dies in einen kurzen Animationsfilm zu verwandeln“, „einen animierten Erklärfilm in Code zu erstellen“, „eine animierte Geschichte / ein Bilderbuch aus meinen Fotos zu machen“ oder „eine Null‑Asset‑Animation wie die Opus 5.5‑Versionen zu erstellen“. Sie erzeugt eine einzelne, eigenständige HTML‑Datei, deren Frames auf einem HTML‑Canvas (suchbar, deterministisch) berechnet werden, rendert sie zu MP4 und prüft das Ergebnis selbstständig. Sie lässt sich mit der Soundtrack‑Fähigkeit für code‑generierte Musik kombinieren. NICHT für Diagramme aus Daten (verwende chart‑animation), reine Textbewegungen (verwende kinetic‑typography) oder 3D/WebGL (verwende threejs‑animation / shader‑glsl aus webgl‑animation‑skills).

Bitte immer in der Sprache des Nutzers antworten.
