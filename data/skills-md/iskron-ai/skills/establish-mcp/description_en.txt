Transport to the Iskron graph. By default — iskron‑bridge from the distribution: stdio → streamable‑HTTP MCP bridge (OAuth or personal token), holding the grant and binding of the session through reconstruction and never responding to harassment with silence; native http + OAuth registration — a fallback path when the bridge cannot be raised. Triggers: “connect the graph”, “raise the bridge”, “iskron tools unavailable”, “MCP does not connect”, “OAuth fails”, “log in with token”, “PAT instead of OAuth”, “connect opencode”, “what is the bridge version”, connect to the graph, raise the bridge, MCP tools missing. Composes entry and iskronify.
