Progetta architetture di caching multilivello per applicazioni web: cache-aside vs write-through vs write-behind, progettazione TTL, invalidazione della cache, pattern Redis, configurazione CDN, caching del browser e prevenzione della fuga precipitosa. Da utilizzare quando si sceglie un modello di memorizzazione nella cache, si progettano strategie di invalidamento della cache, si implementa la memorizzazione nella cache Redis, si configurano intestazioni Cache-Control o si previene la fuga precipitosa della cache. Attiva su "invalidazione cache", "cache-aside", "write-through", "TTL", "cache Redis", "caching CDN", "cache stampede", "dati obsoleti", "cache del browser". NON per la memorizzazione nella cache delle query del database all'interno di un ORM, la memorizzazione di funzioni pure o la memorizzazione nella cache a livello di CPU.
