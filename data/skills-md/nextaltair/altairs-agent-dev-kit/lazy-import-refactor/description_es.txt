Refactorice las importaciones a nivel de módulo de dependencias nativas pesadas (torch, tensorflow, jax) para que funcionen como importaciones diferidas internas. Úselo cuando la importación de una biblioteca activa SIGSEGV o un inicio lento en entornos donde el departamento nativo no puede inicializarse por completo (por ejemplo, falta el controlador CUDA, extensión triton C segmentfault, falta GPU). Predice los efectos secundarios de las pruebas a partir de los atributos del módulo eliminado.
