Synthesize durable align/map sources (and current conversation) into docs/monorail/<feature>/spec.md, then stop and ask before continuing to rail-slice. Refuse empty context — do not invent a thin spec.
