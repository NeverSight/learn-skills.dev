Forgia l'implementazione dai ticket JIRA utilizzando la generazione dinamica di abilità. Raccoglie il contesto in un'abilità caricata lentamente, quindi la richiama per l'esecuzione. Da utilizzare quando viene fornito l'URL di un ticket JIRA (formato: https://[domain].atlassian.net/browse/[TICKET-ID]) per l'elaborazione end-to-end.
