Ricerca di codice strutturale tramite ast-grep: da utilizzare quando contano la forma del codice e le relazioni tra gli elementi, non solo il testo. Ad esempio, "trova funzioni asincrone senza gestione degli errori", "refactoring foo(a, b) in foo({ a, b })". Usa Grep per semplici ricerche di nomi.
