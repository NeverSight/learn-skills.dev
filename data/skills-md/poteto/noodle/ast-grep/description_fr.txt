Recherche de code structurel via ast-grep - à utiliser lorsque la forme du code et les relations entre les éléments sont importantes, pas seulement le texte. Par exemple, "trouver des fonctions asynchrones sans gestion des erreurs", "refactoriser foo(a, b) en foo({ a, b })". Utilisez Grep pour des recherches de noms simples.
