Utility-Hooks für die React-Statusverwaltung – gruppierter Status und LocalStorage-Persistenz.

Wird verwendet, wenn: ein gruppierter Status verwaltet wird, der sich gemeinsam ändert, der Status in localStorage beibehalten wird oder der Status zwischen URL und Speicher koordiniert wird.
