TanStack Query (React Query) per operazioni asincrone, recupero dei dati, memorizzazione nella cache e gestione dello stato.

Da utilizzare per: recuperare dati dal server, gestire operazioni asincrone, memorizzare risposte nella cache, gestire mutazioni o qualsiasi operazione che trae vantaggio dalla gestione automatica dello stato e dalla memorizzazione nella cache.
