Schema TypeBox con rotte Fastify per lo sviluppo di API tipizzate — JSON Schema nativo, generazione automatica di OpenAPI. Da usare quando si costruiscono API REST Fastify con validazione dello schema e generazione di OpenAPI.
