TanStack Query (React Query) per operazioni asincrone, recupero dati, caching e gestione dello stato. Da utilizzare quando si recuperano dati dal server, si gestiscono operazioni asincrone, si memorizzano le risposte, si gestiscono le mutazioni, o per qualsiasi operazione che beneficia della gestione automatica dello stato e del caching.
