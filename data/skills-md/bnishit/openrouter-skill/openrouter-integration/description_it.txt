Connetti le app a oltre 300 modelli IA tramite l'API compatibile con OpenAI di OpenRouter: rilevamento di modelli, generazione di immagini, chat multimodale (testo, immagini, PDF), monitoraggio esatto dei costi per generazione, routing del fornitore con fallback, chiamata di strumenti con limiti di sicurezza, convalida dell'output strutturato, filtro di modelli gratuiti, modelli di avvio (Next.js, Express), flussi di lavoro delle risorse (icone, immagini OG), playbook di produzione e script di verifica. Da utilizzare quando un agente deve aggiungere o eseguire il debug dell'utilizzo di OpenRouter, creare un selettore di modelli, proxy `/api/v1/models`, `/api/v1/models/user`, `/api/v1/providers` o `/api/v1/ generation`, inviare immagini o contenuti PDF a `/api/v1/chat/completions`, generare immagini tramite OpenRouter, creare icone o flussi di immagini OG, analizzare risposte OpenRouter, ispezionare i costi di generazione a posteriori, aggiungere percorsi server Next.js o Express, convalidare output strutturati, eseguire test di fumo, verificare i documenti correnti rispetto a OpenRouter prima della codifica o collegare modello o provider di fallback in un server o un'interfaccia utente.
