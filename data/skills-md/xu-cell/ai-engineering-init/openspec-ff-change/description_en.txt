Fast-forward through OpenSpec artifact creation. Use when the user wants to quickly create all artifacts needed for implementation without stepping through each one individually.

Trigger scenarios:
- Need to quickly generate all change artifacts at once
- Skip step-by-step creation and directly generate proposal/specs/design/tasks
- Want to quickly kick off a change and immediately move to implementation phase

Trigger words: quick create, fast forward, opsx:ff, openspec ff, one-click generate, quick start, fast forward
