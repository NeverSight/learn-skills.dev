Universal Row-Level Data Permission Design Guide. Implements data isolation via AOP + MyBatis interceptor pattern, supporting department-level, self-owned, and custom permission granularities.
Trigger scenarios:
- Add row-level data filtering to business modules
- Design department-level / self-owned data isolation
- Extend custom data permission types
- Temporarily bypass data permissions to query full dataset
- Troubleshoot data permission ineffectiveness
Trigger words: data permission, row-level permission, data isolation, department permission, self permission, custom permission, DataScope, DataPermission, data filtering
Note: If the project has a dedicated skill (e.g., leniu-data-permission), use the dedicated version first.
