leniu-tengyun-core project multi-tenant data permission control specification. Based on a dual-database architecture with physical database isolation, including data source switching, tenant context, and cross-tenant operations.

Trigger scenarios:
- Use @UseSystem to switch to system database operations
- Use Executors.doInTenant/readInTenant for cross-tenant queries
- Use TenantContextHolder to obtain tenant context
- Configure dual-database data source routing
- Cross-tenant data initialization

Trigger words: multi-tenant, data permission, @UseSystem, Executors.doInTenant, readInSystem, TenantContextHolder, MERCHANT-ID, dual-database isolation, data source switching
