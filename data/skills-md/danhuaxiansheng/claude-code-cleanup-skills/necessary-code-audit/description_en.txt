Audit whether “still‑consumer code” truly needs to remain, focusing on wrappers/facades, compatibility layers, outdated public APIs, defensive branches, fallbacks, default values, configuration, duplicate sources, and speculative extension points. Use it for code that is used but not sufficiently necessary, legacy compatibility cleanup, meaningless wrapping, removal of defensive code, cleanup without compatibility requirements, deep necessity checks, and “does this still need to exist?”‑type requests; don’t treat it as a pure unused/dead‑code verification tool.
