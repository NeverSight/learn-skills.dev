营销品牌资产生成（通过 AI 的栅格图像）：标志设计（55 种风格，30 种配色，25 行业），企业形象 / CIP 模型（50 种交付物——名片、信纸、标识、包装、服装），横幅设计（22 种风格，社交/广告/网页/印刷），SVG 图标设计（15 种风格），社交照片（多平台），以及模型无关的海报提示。默认图像引擎为 Codex gpt-image-2（通过 `codex login` 的 ChatGPT 订阅），将品牌徽标作为 CIP 合成的参考图像；如不可用则回退到 Gemini Nano Banana（GEMINI_API_KEY，与 omnimedia 使用相同的密钥）。操作：设计标志，创建 CIP / 品牌形象，生成模型稿件，设计横幅，生成图标，创建社交照片，设计海报，社交媒体图片。平台：Facebook、Twitter、LinkedIn、YouTube、Instagram、Pinterest、TikTok、Threads、Google Ads。对于 HTML/网页、仪表板和幻灯片，请改用 opendesign。

请始终使用用户的语言回复。
