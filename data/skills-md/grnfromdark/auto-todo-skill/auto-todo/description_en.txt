Use when user says "autotodo", "auto-todo", or needs to convert a requirement document (from auto-requirement or any structured markdown) into a todolist.md for auto-dev pipeline consumption. Bridges requirement docs and executable task lists.
