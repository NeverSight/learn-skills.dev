Polling sullo stato del lavoro API pubblica (X-API-KEY). Sondaggio delle operazioni asincrone (struttura, diapositive, esportazioni e così via) in base all'activityId. I tipi seguono PublicApiJobController (/api/v2/jobs/{activityId}).
