Interrogation de l'état des tâches de l'API publique (X-API-KEY). Interrogez les opérations asynchrones (plan, diapositives, exportations, etc.) par ActivityId. Les types suivent PublicApiJobController (/api/v2/jobs/{activityId}).
