Operazioni della presentazione API pubblica (X-API-KEY). Genera strutture, aggiorna struttura, ottieni presentazione, genera diapositive in batch, genera trascrizione/immagine/contenuto, annulla, tema, trascrizione, sezione duplicata. I tipi seguono PublicApiSlideDeckController (/api/v2/projects/{projectId}/slide-deck/{slideDeckId}).
