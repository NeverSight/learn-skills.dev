إدارة مشروع واجهة برمجة التطبيقات العامة (X-API-KEY). إنشاء، إدراج، الحصول على، تحديث، حذف، استنساخ، التصويت على المشاريع. يدعم ترقيم الصفحات المؤشر. تتبع الأنواع PublicApiProjectController (/api/v2/projects).
