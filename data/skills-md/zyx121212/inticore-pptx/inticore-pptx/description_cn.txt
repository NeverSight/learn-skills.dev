生成经过用户批准的完整幻灯片视觉蓝图并重新制作，或直接将提供的 PNG/JPG/WebP/截图重新创建为原生可编辑的 PowerPoint PPTX，适用于 Codex、Claude Code、WorkBuddy、Cursor、Gemini CLI、OpenCode 等基于文件系统的代理。用于 内容先出视觉稿再做PPT、图片转PPT、截图转PPT、1:1 重制 PPT、100% 像素级还原、零差异 PPT、原生可编辑 PPT、editable PPTX，或在用户拒绝完整幻灯片背景图像时使用。保留已批准的文字、布局、尺寸、颜色、层次结构、表格、时间轴、图表、边框、图标、人物信息和摄影资产。对于像素完美的请求，冻结渲染器合约，并要求实际的 PPTX 渲染通过严格的每页零像素差异比较，且不嵌入完整的参考图像。
