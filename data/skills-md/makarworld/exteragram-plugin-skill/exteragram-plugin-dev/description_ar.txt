إنشاء ملحقات exteraGram (Python على BasePlugin SDK 1.4.4.3، exteraGram 12.5.1+، Python 3.11 عبر Chaquopy v16). يُستَخدم عند كتابة أو تعديل ملفات .plugin، تنفيذ الخطاطيف (on_send_message_hook، pre_request_hook، post_request_hook، on_update_hook)، إنشاء إعدادات الملحق (ui.settings: Header/ Switch/ Selector/ Input/ Text/ Custom)، إرسال الرسائل (send_text / send_photo / send_document / edit_message)، ربط طرق Xposed (MethodHook/HookFilter)، وكيل فئة Java (java_subclass/joverride/jmethod)، معالجة الملفات (FilesController)، النشرات (BulletinHelper)، الحوارات (AlertDialogBuilder)، النوايا (IntentsManager)، ملحقات Elyx المنظمة (refmap/metainfo/assets/strings)، الانعكاس (hook_utils find_class/get_private_field)، أو تصحيح خادم التطوير (127.0.0.1:42690). يغطي نطاق حسابات متعددة، تنسيق النص، تبعيات PIP، وعادات مستودع awesome-plugins (.plugin هو المصدر، docs.md، releases/، Ruff).

يرجى دائمًا الرد بلغة المستخدم.
