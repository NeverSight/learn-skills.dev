Utilizza l'endpoint _meta GraphQL su qualsiasi DB pubblico-app costruttivo per analizzare lo schema della tabella in fase di esecuzione ed eseguire il rendering di moduli CRUD completamente dinamici con zero configurazione di campi statici. Copre DynamicFormCard (crea/modifica/elimina), precompilazione FK bloccata dal contesto (defaultValues ​​+ defaultValueLabels) e il modello di record correlato O2M/M2M. Da utilizzare durante la creazione dell'interfaccia utente di creazione/modifica/eliminazione per qualsiasi tabella con provisioning costruttivo.
