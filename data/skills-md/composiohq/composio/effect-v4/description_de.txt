Schreibe, überprüfe oder aktualisiere Effect‑v4‑Code im Composio‑CLI, im cli‑keyring und im json‑schema‑to‑effect‑schema‑Paket, alles exakt auf effect@4.0.0‑rc.112 festgesetzt – Context.Service und explizite Layer, Schema.TaggedError und typisierte Wiederherstellung, die effect/unstable/cli‑Befehlsoberfläche und das vendored effect‑Source‑Oracle. Verwende dies beim Schreiben oder Überprüfen von Effect‑v4‑Code, beim Beantworten einer v4‑API‑Frage, bei der Arbeit in effect/unstable/cli, beim Definieren eines Context.Service‑Dienstes, beim Modellieren eines Fehlers mit Schema.TaggedError, beim Anheben der Effect‑Prerelease‑Pins oder beim Verifizieren einer unbekannten API gegen ts/vendor/effect. Nicht verwenden für CLI‑Befehl‑UX/Verkabelungs‑Design (verwende cli-command) oder CLI‑E2E‑Tests (verwende cli-e2e).
