uv — the 2026 standard Python package manager (replaces pip, venv, pyenv, pip-tools, pipx). Use this skill for ALL Python dependency management, virtual environments, Python version management, and Docker Python setups. NEVER use bare pip for new projects in 2026. Covers uv init, uv add, uv sync, lockfiles, pyproject.toml, Docker integration, and migration from pip/Poetry.
