Da utilizzare quando un modello Twig utilizza `reservationData` o `array<string, mixed>`, quando un modello Twig riceve variabili tramite i parametri "include", quando un Notifier chiama `$dto->__toArray()` o quando crea un nuovo modello di email transazionale `.dto.html.twig`.
