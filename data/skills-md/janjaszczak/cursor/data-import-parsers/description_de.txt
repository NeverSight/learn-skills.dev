Implementieren oder umgestalten Sie den Datenimport/die Analyse, sodass Dateien sequentiell gestreamt werden (speichersicher), Typen explizit validiert/erzwungen werden, irreparable Datensätze übersprungen werden, während sie in einer Fehler-CSV protokolliert werden (vollständige Originalspalten + Zeitstempel/Datei/Zeile/Fehler), rows_ok/rows_skipped/parse_errors-Metriken ausgegeben werden und idempotente DB-Schreibvorgänge garantiert werden.
