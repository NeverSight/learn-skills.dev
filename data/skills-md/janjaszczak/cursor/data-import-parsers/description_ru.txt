Реализуйте или реорганизуйте импорт/синтаксический анализ данных, чтобы он передавал файлы последовательно (безопасно для памяти), явно проверял/принуждал типы, пропускал невосстановимые записи, записывая их в CSV-файл с ошибкой (полные исходные столбцы + временная метка/файл/строка/ошибка), выдавал метрики rows_ok/rows_skipped/parse_errors и гарантировал идемпотентную запись в БД.
