Standard e runbook per monorepo full‑stack su Docker — auditare, scaffolding e refactoring del repository (compose dev/prod, Dockerfile multi‑stage non‑root, entrypoint con attesa del database, hot‑reload nei container, routing SPA) e distribuirlo con GitHub Actions su Vercel più un runner self‑hosted. Da utilizzare quando la richiesta riguarda docker‑compose, Dockerfile, entrypoint, deploy.yml o lo stato reale di ciò che è in esecuzione sul server. Tre modalità — analizza (audit e piano), esegui (implementazione e rimozione del legacy), ispeziona (diagnostica in sola lettura del deployment).
