نفّذ شفرة بايثون في بيئة معزولة وآمنة عبر inference.sh. مثبت مسبقًا: NumPy، Pandas، Matplotlib، requests، BeautifulSoup، Selenium، Playwright، MoviePy، Pillow، OpenCV، trimesh، وأكثر من 100 مكتبة أخرى. استخدمها لـ: معالجة البيانات، استخراج البيانات من الويب، تعديل الصور، إنشاء الفيديو، معالجة النماذج ثلاثية الأبعاد، توليد ملفات PDF، استدعاءات API، سكريبتات الأتمتة. الكلمات المفتاحية: python، execute code، run script، web scraping، data analysis، image processing، video editing، 3D models، automation، pandas، matplotlib.
