Batch wrap hexadecimal color codes in Obsidian notes (e.g., raw prompts generated by large models) with full‑width parentheses (#FFFFFF → （#FFFFFF）) to prevent # from being recognized as a tag. Scan .md files in the user‑specified directory, automatically skipping code blocks, links, frontmatter, and already wrapped items. Works on macOS / Linux. Trigger commands: /gr-ob-fix-color-tags, /gr-fix-color-tags, /转义颜色代码, “fix color tags”.
