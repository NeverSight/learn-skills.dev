قم بتصميم فيديوهات من كود مكوّنات React/Remotion عبر inference.sh. قدّم كود TSX، واحصل على ملف MP4. يدعم جميع واجهات برمجة تطبيقات Remotion: useCurrentFrame، useVideoConfig، spring، interpolate، AbsoluteFill، Sequence. يمكن ضبط الدقة، عدد الإطارات في الثانية، المدة، والترميز. يستخدم لـ: توليد فيديو برمجي، رسومات متحركة، تصميم حركة، فيديوهات مدفوعة بالبيانات، تحويل تحريكات React إلى فيديو. الكلمات المفتاحية: remotion، render video from code، tsx to video، react video، programmatic video، remotion render، code to video، animated video، motion graphics code، react animation video.
