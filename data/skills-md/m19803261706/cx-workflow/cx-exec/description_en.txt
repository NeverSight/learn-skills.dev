CX Workflow — Task Execution and Contract Verification. Triggered when the user says "execute task", "start development", "implement feature", "write code", "continue", or "next task". 5-step process: Read Task → Implement → Contract Check → Test → Commit. Uses local task-{n}.md and status.json, automatically verifying implementation matches contract. After all tasks are done, automatically triggers cx-summary.
