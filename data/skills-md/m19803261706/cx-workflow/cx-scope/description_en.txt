CX Workflow — Project Blueprint Discussion. Triggered when users mention "blueprint", "overall plan", "project scope", "scope", "project discussion", or "feature proposal". Multi-round conversations explore the project or feature plan, save results to local .claude/cx/features/{dev_id}-{feature}/scope.md, optionally sync to GitHub Issue (based on config.github_sync mode).
