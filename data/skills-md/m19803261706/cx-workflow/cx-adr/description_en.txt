CX Workflow — Architecture Decision Record. Triggered when users mention "architecture decision", "technology selection", "ADR", "why choose X", or "architecture solution comparison" (usually invoked automatically by cx-design). Records Architecture Decision Records and saves them to local .claude/cx/features/{dev_id}-{feature}/adr.md. Can be triggered manually or automatically by cx-design when an architecture decision is detected.
