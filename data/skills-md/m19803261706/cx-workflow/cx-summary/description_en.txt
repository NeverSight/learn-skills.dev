CX Workflow — Summary Release and Close-loop. Triggered manually or automatically when cx-exec is fully completed. Generates summary documents, syncs with GitHub (based on config.github_sync), intelligently updates CLAUDE.md specifications, and triggers completion notifications. Executes only when the user explicitly invokes /cx-summary or when cx-exec is fully completed. Do not auto-trigger.
