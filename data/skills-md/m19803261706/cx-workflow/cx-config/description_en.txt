CX Workflow — Configuration Management. View and modify configuration fields in config.json, such as developer_id, github_sync mode, code review switch, agent team mode, auto-formatting, etc. Trigger words: config, configuration, settings, modify config, change mode. Execute only when the user explicitly invokes /cx-config. Do not auto-trigger.
