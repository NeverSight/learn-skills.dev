Use when the user asks to create or preview a Git commit, write a commit message from current changes, validate a proposed message, or split changes into logical commits. Distinguish message-only, preview, and execution requests; follow repository commit conventions; preserve existing staging boundaries; never stage or commit unrelated changes; strictly forbid agent self-attribution, AI watermarks, or agent co-authors; and verify every created commit.
