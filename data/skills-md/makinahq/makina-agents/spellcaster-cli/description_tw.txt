以非互動模式操作 Makina 機器和 calibre 工作流程的「spellcaster」二進位文件，包括餘額、倉位、交換、橋樑操作和維護命令。每當使用者要求執行或偵錯 `spellcaster` 終端命令（作為已安裝的二進位檔案）、編寫命令標誌、設定 `~/.config/spellcaster/config.toml` 或修正與 `--machine`、`--caliber`、簽署者模式、`--dev`、安全和 RPC 設定相關的 CLI 時，請使用此錯誤。
