以非交互模式操作 Makina 机器和 calibre 工作流程的“spellcaster”二进制文件，包括余额、仓位、交换、桥操作和维护命令。每当用户要求运行或调试 `spellcaster` 终端命令（作为已安装的二进制文件）、编写命令标志、配置 `~/.config/spellcaster/config.toml` 或修复与 `--machine`、`--caliber`、签名者模式、`--dev`、安全和 RPC 设置相关的 CLI 错误时，请使用此技能。
