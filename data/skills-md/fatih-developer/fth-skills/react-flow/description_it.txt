Analizza, ripara, migra e crea scaffold codebase @xyflow/react. Da utilizzare quando gli utenti chiedono di eseguire il debug del comportamento di React Flow, correggere il cablaggio dello stato nodo/edge, migliorare l'indipendenza dal tipo o le prestazioni, aggiornare le API React Flow legacy, preservare la compatibilità persistente del grafico o generare da zero uno starter React Flow completo.
