Configura i tunnel proxy HelpMeTest per i test di sviluppo locale. Da utilizzare quando l'utente deve testare localhost, desidera sostituire gli URL di produzione con porte locali o deve instradare più servizi. Da utilizzare quando l'utente dice "imposta proxy", "test localhost", "tunnel to local" o prima di eseguire test sui server di sviluppo locali.
