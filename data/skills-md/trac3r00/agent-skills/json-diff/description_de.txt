Semantischer Unterschied zwischen zwei JSON‑Dokumenten, gemeldet per JSON‑Pfad — hinzugefügt, entfernt, geändert mit alten/neuen Werten, rekursives Durchlaufen verschachtelter Objekte und positionsbasierter Vergleich von Arrays. Verwenden Sie dies, wenn `diff` bei JSON nutzlos ist (Schlüsselneuanordnung, Formatierungsrauschen), für Config‑Drift‑Prüfungen, API‑Antwortvergleiche oder CI‑Gates für die erwartete Ausgabe.
