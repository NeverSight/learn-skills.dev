Configura un Language Server (LSP) per un linguaggio specifico affinché gli strumenti dell'editor/agent — diagnostica, vai-alla-definizione, trova-riferimenti, rinomina — funzionino. Usalo quando devi: configurare LSP, impostare LSP, installare o configurare un language server, risolvere “no LSP server configured” / “server not installed”, scegliere tra server (basedpyright vs pyright vs ty vs ruff), o collegare .codex/lsp-client.json / .opencode/lsp.json. 언어서버 설정. Instradamenti per estensione file a references/<language>/README.md per il server built‑in esatto, comandi di installazione per OS (macOS/Linux/Windows), snippet di configurazione per entrambi i file di configurazione, opzioni di inizializzazione, alternative e risoluzione dei problemi. Include script: detect‑lsp.ts (scansiona un progetto per i linguaggi + stato di installazione/configurazione di ogni server) e verify‑lsp.ts (esegue un vero roundtrip di diagnostica). Copre typescript, python, go, rust, c/c++, java, kotlin, c#/razor, swift, ruby, php, dart, elixir, zig, lua, bash, yaml, terraform, haskell, julia.
