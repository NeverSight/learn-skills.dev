從專案的 AI 會話歷史中產生 RULE.md，方式是抽取每一次人類糾正代理人的時刻——「不，永遠不要使用 jwt」；「先執行測試」；「我們使用 session 而非 token」。專案能擁有的最高效用的上下文檔案，就是根據自身錯誤歷史所建立的那個。可離線讀取 Claude Code、Codex 與 OpenCode 儲存庫（遵循與 session‑handoff 相同的慣例）。

請始終以使用者的語言回應。
