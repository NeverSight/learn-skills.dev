从分支更改或明确的文件列表中移除 AI 生成的代码味道（slop）。首先使用回归测试锁定行为，然后通过并行 `deep` 代理以每批 5 个的方式运行分类清理，随后通过质量门进行验证。涵盖 10 种 slop 类别，包括性能等价、过度复杂（对象注解、if/elif 变体链）以及超大型模块（250 行以上纯代码，需强制模块化重构）。当用户请求 “remove slop”“clean AI code”“deslop”“clean up AI-generated code”“remove AI slop”，或想要清理最近更改中的 AI 生成模式时必须使用。触发词 — “remove ai slops”“clean ai code”“deslop”“cleanup AI generated”“remove AI slop”“clean up AI-generated code”“strip slop”“ai‑slop cleanup”。
