Удалите запахи кода, генерируемого ИИ (slop), из изменений ветки или из указанного списка файлов. Сначала блокирует поведение регрессионными тестами, затем запускает категоризированную очистку через параллельные «deep»‑агенты пакетами по 5, после чего проверяет с помощью контрольных ворот качества. Охватывает 10 категорий slop, включая эквивалентность производительности, чрезмерную сложность (аннотации объектов, цепочки if/elif), и слишком большие модули (250 + чистых строк кода с обязательной модульной рефакторингой). ДОЛЖНО БЫТЬ ИСПОЛЬЗОВАНО, когда пользователь просит «remove slop», «clean AI code», «deslop», «clean up AI-generated code», «remove AI slop» или хочет очистить шаблоны, сгенерированные ИИ, из недавних изменений. Триггеры — «remove ai slops», «clean ai code», «deslop», «cleanup AI generated», «remove AI slop», «clean up AI-generated code», «strip slop», «ai-slop cleanup».
