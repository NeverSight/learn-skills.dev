Supprimez les odeurs de code générées par l’IA (slop) des changements de branche ou d’une liste de fichiers explicite. Verrouille le comportement avec des tests de régression EN PREMIER, puis exécute un nettoyage catégorisé via des agents `deep` parallèles par lots de 5, puis vérifie avec des portes de qualité. Couvre 10 catégories de slop, y compris les équivalences de performance, la complexité excessive (annotations d’objets, chaînes de variantes if/elif) et les modules surdimensionnés (plus de 250 LOC purs nécessitant une refactorisation modulaire obligatoire). DOIT ÊTRE UTILISÉ lorsque l’utilisateur demande « remove slop », « clean AI code », « deslop », « clean up AI-generated code », « remove AI slop », ou souhaite nettoyer les motifs générés par l’IA des changements récents. Déclencheurs – « remove ai slops », « clean ai code », « deslop », « cleanup AI generated », « remove AI slop », « clean up AI-generated code », « strip slop », « ai‑slop cleanup ».
