ブランチの変更や明示的なファイルリストから AI 生成コードの臭い（スロップ）を除去します。まず回帰テストで動作をロックし、次に並列 `deep` エージェントをバッチ 5 で実行してカテゴリ別クリーンアップを行い、最後に品質ゲートで検証します。パフォーマンス等価性、過剰な複雑性（オブジェクト注釈、if/elif バリアントチェーン）、サイズ過大モジュール（250 行以上の純粋 LOC で必須のモジュラーリファクタリング）など、10 のスロップカテゴリをカバーします。ユーザーが「スロップを除去」「AI コードをクリーン」「deslop」「AI 生成コードをクリーンアップ」「AI スロップを除去」などを求めたときに必ず使用してください。トリガー - 「remove ai slops」「clean ai code」「deslop」「cleanup AI generated」「remove AI slop」「clean up AI-generated code」「strip slop」「ai-slop cleanup」。
