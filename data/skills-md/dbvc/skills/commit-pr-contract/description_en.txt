Generate commit messages (title + body) and PR descriptions in Chinese, clearly separating the "long-term evidence/traceability" commit perspective from the "help reviewer understand and review" PR perspective. Applicable when the user requests "write commit message / write PR description / generate commit + PR based on changes", and must follow the `M-xxx(type): title` format and the four-part contract-style PR structure; can infer risks and review focus based on change descriptions/code diffs, and ask for clarification if needed.
