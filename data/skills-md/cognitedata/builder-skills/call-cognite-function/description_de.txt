Rufen Sie eine bereitgestellte Cognite CDF‑Funktion auf und holen Sie deren Antwort ab, mit durch Zod validierten API‑Antworten und einem optionalen React‑Query‑Hook. Verwenden Sie diese Fähigkeit, wann immer Sie eine beliebige CDF‑Funktion (nach numerischer Funktions‑ID oder externer ID) aufrufen, beliebige Eingabedaten übergeben und ein typisiertes, validiertes Ergebnis zurückerhalten – unabhängig davon, was die Funktion tut, welche Ausgabe sie hat oder welche Form ihr Ein‑ bzw. Ausgabe‑Schema hat. Auslöser: call cognite function, invoke CDF function, run CDF function, poll CDF function, CDF Functions API, cognite function result, function call nonce.
