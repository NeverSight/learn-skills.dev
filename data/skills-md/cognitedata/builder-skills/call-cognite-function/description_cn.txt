调用已部署的 Cognite CDF 函数并获取其响应，使用 Zod 验证的 API 响应以及可选的 React Query 钩子。每当需要通过数值函数 ID 或外部 ID 调用任意 CDF 函数、传入任意输入数据并获取已类型化、已验证的结果时，请使用此技能——不论函数具体执行什么操作、是否有输出，或其输入/输出的结构如何。触发词：call cognite function、invoke CDF function、run CDF function、poll CDF function、CDF Functions API、cognite function result、function call nonce。

请始终使用用户的语言回复。
