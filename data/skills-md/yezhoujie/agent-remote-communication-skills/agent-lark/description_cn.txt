将需要人工决策的事项以 Feishu/Lark 卡片的形式推送到其手机，并阻塞等待判决结果通过 stdout 返回；人类在项目的 Feishu 群组中发送的消息、图片、文件和语音笔记会作为指令进入代理会话。对于代理无法自行决定的选择（需求不一致、真实分歧、技术选型），而此时人类可能不在键盘前。何时发起请求由调用方的策略决定——此技能提供调用，而不是触发。它还可以向同一群组推送单向通知（notify）并发送文件（send-file）。
