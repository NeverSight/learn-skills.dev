This skill should be used when the user asks to "set up my computer", "install dev tools", "set up my environment", or when the pre-flight check reports NEEDS_COMPUTER_SETUP. It detects the OS, tries install methods in order of reliability, and falls back gracefully. Works on Linux, macOS, Windows (native or WSL), and locked-down corporate machines.
