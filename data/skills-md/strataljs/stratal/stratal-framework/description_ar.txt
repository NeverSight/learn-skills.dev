يُستخدم عند العمل مع @stratal/framework - المصادقة (Better Auth)، وقاعدة البيانات (ZenStack ORM)، وRBAC (Casbin)، وAuthGuard، ومصانع بيانات الاختبار. تشغيل: auth، AuthModule، AuthService، AuthContext، AuthGuard، Better Auth، قاعدة البيانات، DatabaseModule، DatabaseService، @InjectDB، ZenStack، RBAC، RbacModule، CasbinService، Casbin، Factory، Sequence، @stratal/framework، اتصال متعدد، مخططات لكل اتصال.
