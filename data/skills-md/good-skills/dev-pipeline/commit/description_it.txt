Un singolo commit git, messaggio in stile repository. Ispezione a consumo minimo di token (prima le statistiche, diff limitato). /commit o richiesta esplicita di commit. Nessun push a meno che l'utente non lo chieda.

Per favore rispondi sempre nella lingua dell'utente.
