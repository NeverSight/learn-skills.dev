在进行复用查找、超/微门控和增量检查后，将简短的自然语言请求转换为面向仓库的工程任务提示。当用户以 /promptize 开头，或请求将简短请求 promptize /展开为工程规格时使用。支持 --execute、--full、--minimal、--compress、--caveman、--save-to-file 和 --save。默认紧凑；对超低任务自动使用 minimal；Caveman 文本压缩用于代理交接。基于文件的 Promptize 复用（精确/归一化）存于 SESSION‑CACHE — 不使用 Redis。

请始终使用用户的语言作答。
