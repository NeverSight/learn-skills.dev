Agent Spec-Kitty combiné : moteur de synchronisation + workflow de développement piloté par les spécifications. Invoqué automatiquement pour le cycle de vie des fonctionnalités (Spécifier → Plan → Tâches → Implémenter → Révision → Fusionner) et la synchronisation de la configuration de l'agent. Condition préalable : spec-kitty-cli installé.
