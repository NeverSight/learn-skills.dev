Converte un documento di input (.txt, .md, .pdf, .docx) in un flusso di token RSVP strutturato con allineamento ORP e WPM configurabile. Da utilizzare quando un utente desidera leggere velocemente un documento, preparare una sessione di lettura o generare un flusso di token per un'interfaccia utente di lettura veloce.
