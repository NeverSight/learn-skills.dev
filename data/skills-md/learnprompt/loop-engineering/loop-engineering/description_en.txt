Loop Engineering (circular engineering / loop engineering) implementation scaffold, personality code name “Yu Gong”. Turn a vague sentence “I want to automatically handle X” into a self‑running Loop equipped with goal, intake, trigger, worktree, maker/checker, connectors, state, verification, and guardrails, and finally hand you a one‑click executable command—​the trigger you pull yourself.

This is the engineering realization of the fourth stage, Loop Engineering, after Prompt Engineering → Context Engineering → Harness Engineering: you no longer manually prompt the Agent round by round, but design a loop that can autonomously discover tasks, execute, verify, and iterate until the goal is achieved. The output includes a component matrix, a runtime‑neutral “Loop Spec”, two sets of directly runnable commands—Claude Code and Codex—and manifest/log state files.

Trigger phrases include but are not limited to: “create a Loop Engineering”, “turn this task into a loop”, “make the task a goal”, “design a loop engineering”, “loop engineering”, “循环工程”, “环路工程”, “let the agent run by itself”, “autonomous workflow”, “turn maintenance/triage/upgrade into an automatic loop”, “help me open a goal”, “design /goal”, “design /loop”, “run an agent that can iterate itself”, “Yu Gong”.

Even if the user merely describes “I want the agent to automatically do something every day/repeatedly and iterate itself until completion”, as long as the intent is to turn manual round‑by‑round prompting into a self‑running loop system, it should be triggered.

Do NOT use for: writing an ordinary skill from scratch (use skill‑creator), polishing/upgrading a single existing skill (use Luban), one‑off single‑round tasks (just do it directly, no loop needed), ordinary code review (use code‑review).
