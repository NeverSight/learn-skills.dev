设置并驱动 TencentDB Agent Memory（MemoryCore + MemoryHub + MemoryProxy + MemoryPanel，来自 AnEntrypoint/agent-memory）——一个持久化、跨会话的 AI 代理记忆与知识系统。Chat Memory（L0 对话 → L1 原子 → L2 场景 → L3 角色）、从以往工作中提取的可版本化技能库、覆盖文档和代码的 Wiki + CodeGraph 知识图谱，以及由人工控制的审查面板。当用户希望代理团队在会话之间以及跨多个代理框架（不仅限于当前的 Claude Code 会话）共享并累积记忆/技能/知识时使用；当他们提到 “memory hub”“team memory”“chat memory”“skill library”“wiki”“codegraph” 或请求安装/配置/排查 memory‑tencentdb 插件，或在将新代理加入已有团队的累计经验时使用。自 gm 引入 tencentdb_backend 后，该系统的格式也可以是 gm 自己的 memorize/recall/memorize‑fire/memorize‑prune 后端，针对已选择的命名空间（见 gm.config.json 的 memory.tencentdb_backend）——启用后，代理已知的动词表面保持不变，只有存储目标改变。将此技能用于独立部署（Docker Compose、面板 UI、团队审查）或当请求的不是 gm 自己的后端时使用。

请始终使用用户的语言作答。
