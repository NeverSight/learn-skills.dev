Detects "unintended diffs" before commits or PR creation and alerts when they diverge from the branch's purpose. It even suggests commands to discard unrelated changes. Use it for pre-commit scope checks, verifying no extra files are mixed into the diff, and ensuring PRs don't grow too large. Whenever you wonder, "Are any changes unrelated to this branch's purpose included?"—always use it.
