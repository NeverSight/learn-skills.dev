Erstellt UI-Tests für ein GitHub-Problem und überprüft, ob sie den Fehler reproduzieren. Iteriert, bis die Tests tatsächlich fehlschlagen (was beweist, dass sie das Problem erkennen). Verwenden Sie diese Option, wenn in der PR Tests fehlen oder Tests für ein Problem erstellt werden müssen.
