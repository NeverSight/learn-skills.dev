Enforces atomic commit workflow — breaks implementation into small, self-contained units and commits after each one. Use when implementing features, fixing bugs, or making any multi-step code changes.
