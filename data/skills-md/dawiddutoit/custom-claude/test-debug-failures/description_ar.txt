تصحيح أخطاء الاختبار المبني على الأدلة الذي يفرض التحليل المنهجي للسبب الجذري.
يُستخدم عند فشل الاختبارات، أو حدوث أخطاء pytest، أو عدم اجتياز مجموعة الاختبار،
تصحيح فشل الاختبار، أو إصلاح الاختبارات المعطلة. يمنع على أساس الافتراض
يتم إصلاحه عن طريق فرض تسلسل التشخيص المناسب. يعمل مع بايثون (.py)،
JavaScript/TypeScript (.js/.ts)، Go، ملفات اختبار Rust. يدعم بيتست،
jest، وvitest، وmocha، وgo test، وcargo test، وغيرها من الأطر.
