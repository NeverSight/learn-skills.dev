Force AI agents to automatically generate and insert VeriContext citations/claims whenever they write or edit documents (README, AGENTS.md, plans, etc.), and at the moment of task completion/commit/push run vctx_verify_workspace to confirm every claim is up-to-date. 3-layer enforcement: (1) hook reminder on document creation, (2) batch verification on task completion, (3) git hook blocking commit/push. NOT for: code changes, test writing, build tasks.
