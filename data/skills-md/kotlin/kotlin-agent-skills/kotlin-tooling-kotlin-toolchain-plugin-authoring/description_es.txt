Cargue al crear, escribir o diseñar un complemento local de Kotlin Toolchain para extender la compilación declarativa con la generación de código, el procesamiento en tiempo de compilación, la verificación personalizada o el empaquetado que module.yaml no puede expresar, o al hacer referencia a @ TaskAction, @Configurable, plugin.yaml o jvm/amper-plugin. Omitir para portar un plugin Gradle existente.
