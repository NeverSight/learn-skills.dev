Carga al migrar o convertir un proyecto completo de Gradle Kotlin (build.gradle(.kts), wrapper, libs.versions.toml, buildSrc) a la Kotlin Toolchain, incluyendo la reescritura del CI y el reemplazo de plugins de Gradle que no tienen un equivalente nativo en la Toolchain. Omitir para portar un solo plugin de Gradle o trabajo general de Toolchain una vez que Gradle haya desaparecido.
