Cargar al portar, convertir o reimplementar un solo plugin de Gradle como un plugin local de Kotlin Toolchain, o al mapear conceptos de plugins de Gradle (Task, Extension, project.version, dependsOn, propiedades -P, afterEvaluate) a equivalentes de Toolchain. Omitir al migrar un proyecto completo de Gradle o al crear un plugin desde cero.

Por favor, siempre responde en el idioma del usuario.
