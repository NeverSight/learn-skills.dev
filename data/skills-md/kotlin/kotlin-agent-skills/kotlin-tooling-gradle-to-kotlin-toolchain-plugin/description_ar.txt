حمّل عند نقل أو تحويل أو إعادة تنفيذ مكوّن إضافي واحد لـ Gradle كملحق محلي لسلسلة أدوات Kotlin، أو عند ربط مفاهيم مكوّن Gradle (Task، Extension، project.version، dependsOn، خصائص -P، afterEvaluate) بنظائر السلسلة الأداتية. تخطّ ذلك عند ترحيل مشروع Gradle كامل أو كتابة مكوّن إضافي من الصفر.

يرجى دائمًا الرد بلغة المستخدم.
