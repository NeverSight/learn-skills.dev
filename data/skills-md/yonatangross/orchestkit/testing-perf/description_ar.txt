أنماط اختبار الأداء والحمل — اختبارات تحميل k6، واختبارات إجهاد الجراد، وتحسين تنفيذ pytest (متوازي xdist، والمكونات الإضافية)، وتصنيف نوع الاختبار، وقياس الأداء. يُستخدم عند كتابة اختبارات التحميل، أو تحسين سرعة تنفيذ الاختبار، أو إعداد البنية الأساسية لـ pytest.
