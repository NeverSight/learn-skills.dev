Acceptance network canary drill: For code that has just passed acceptance, secretly embed a well‑disguised bug in a one‑time workspace, allowing the existing acceptance process to run as usual without knowing, then attribute per gate “which gate blocked, which gate missed,” calibrating the real interception capability of the acceptance network. This is limited to user‑initiated actions (“run a mine‑planting drill / canary drill / calibrate acceptance network”) and must be executed in a new session without any work context—the agent must not proactively suggest this skill, nor execute it in sessions that contain implementation or acceptance history.
