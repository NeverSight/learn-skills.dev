Crea la struttura di un nuovo pacchetto Python con pyproject.toml, sistema di build hatchling, linting ruff e supporto --version tramite importlib.metadata. Usalo quando crei un nuovo pacchetto Python, uno strumento CLI Python o un progetto Python da zero.
