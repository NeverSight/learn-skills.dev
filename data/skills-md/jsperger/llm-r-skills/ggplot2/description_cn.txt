使用 R ggplot2 包时使用，尤其是 ggplot2 4.0+ 功能。涵盖 S7 迁移（@ 属性访问）、带有 ink/paper/accent 的主题默认值、element_geom()、from_theme()、主题快捷方式 (theme_sub_*)、调色板主题、带有字典/属性的标签、离散比例改进（调色板、 Continuous.limits、minor_breaks、sec.axis）、位置美学（nudge_x/nudge_y、顺序）、facet_wrap dir/space/layout、箱线图/小提琴/标签样式、stat_manual()、stat_connect()、坐标反转。
