使用 R ggplot2 套件時使用，尤其是 ggplot2 4.0+ 功能。涵蓋 S7 遷移（@ 屬性存取）、帶有 ink/paper/accent 的主題預設值、element_geom()、from_theme()、主題快捷方式 (theme_sub_*)、調色板主題、帶有字典/屬性的標籤、離散比例改進（調色板、 Continuous.limits、minor_breaks、sec.axis）、位置美學（nudge_x/nudge_y、順序）、facet_wrap dir/space/layout、箱線圖/小提琴/標籤樣式、stat_manual()、stat_connect()、座標反轉。
