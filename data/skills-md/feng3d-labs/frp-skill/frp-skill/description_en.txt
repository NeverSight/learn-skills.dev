Deploy frp (fatedier/frp) for intranet penetration, exposing local services to the public internet. Use this skill when users need to configure frp reverse proxy, set up frpc/frps, expose local HTTP/TCP services via a public server, or perform NAT/intranet penetration. Covers Windows client and Linux server deployment.
