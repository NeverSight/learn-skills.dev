Imposta una nuova struttura di progetto con la directory delle attività e il file seed per l'area di lavoro di ricerca. Da utilizzare quando l'utente desidera avviare un nuovo progetto, creare una struttura di progetto, inizializzare una nuova idea o menzionare l'avvio di qualcosa di nuovo.
