Handles Git repository checks, diff reading, commit message generation and validation, and user-authorized Git operations; used when dealing with Git workflows, commit messages, or when existing staged content needs to be protected.
