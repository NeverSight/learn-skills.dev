將現有的第三方 SDK 追蹤程式碼（Firebase、Amplitude、Sensors Data、Mixpanel、GA4）遷移至 AE SDK（數數 / ThinkingAI / Agentic Engine / AE SDK）— 替換（切換）或與現有追蹤並行（雙寫）。觸發詞：埋點遷移、埋點切換、第三方埋點遷移、第三方埋點切換、遷移埋點到數數、把 xx SDK 切換到數數、把已有埋點用數數實現一遍、用數數替換 xx SDK、整合數數並參考已有 xx SDK 埋點、Firebase 遷移、Amplitude 遷移、Mixpanel 遷移、神策遷移、Sensors Data 遷移、GA4 遷移、gtag 遷移、雙寫、tracking migration、migrate Firebase/Amplitude/Mixpanel/Sensors Data/GA4 to AE、switch xx SDK to ThinkingAI、replace xx SDK with AE、replace logEvent with track、re-implement existing tracking with ThinkingAI、add AE tracking alongside existing analytics、dual-write。供應商無關的核心與每個供應商的適配器；重用 ae‑generate‑tracking‑plan 與 ae‑generate‑tracking‑code。
