مولّد حيوان كودكس على ران كومفي. أنشئ ملف spritesheet.webp متوافق مع كودكس + pet.json من صورة مرجعية واحدة، وضعه في `${CODEX_HOME:-$HOME/.codex}/pets/<name>/` وسيتعرف كودكس عليه كحيوان كودكس مخصص بجانب الـ 8 المدمجات. هذه الميزة تُنتج الأطلس الدقيق لحيوان كودكس الذي يتوقعه كودكس (PNG/WebP بحجم 1536×1872، 8 أعمدة × 9 صفوف، خلايا 192×208، 9 حالات حركة — ثابت، جري‑يمين، جري‑يسار، تهليل، قفز، فشل، انتظار، جري، مراجعة). تستدعي OpenAI GPT Image 2 edit مرة واحدة عبر واجهة RunComfy المحلية كـ `runcomfy run openai/gpt-image-2/edit` لإنتاج وضعية حيوان كودكس قياسية، ثم تجمع الصفوف التسعة للرسوم المتحركة برمجياً باستخدام تحولات دقيقة من ImageMagick — بلا Codex Pro، بلا `$imagegen`، بلا مفتاح OPENAI_API_KEY، فقط RUNCOMFY_TOKEN. يتم التفعيل عند استخدام "codex pet"، "create codex pet"، "make codex pet"، "hatch codex pet"، "/hatch image"، "desktop pet codex"، "codex pets"، "spritesheet.webp"، أو أي طلب صريح لبناء حيوان مخصص لـ OpenAI Codex.

يرجى دائمًا الرد بلغة المستخدم.
