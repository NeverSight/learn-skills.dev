Modifiez des images avec OpenAI GPT Image 2 (le point de terminaison `/edit` de ChatGPT Images 2.0) sur RunComfy — fourni avec les modèles d’invite documentés du modèle afin que la compétence produise un résultat plus précis que les invites naïves sur le même modèle. Documente les points forts de GPT Image Edit (préservation du langage, édition multilingue de texte dans l’image, références multiples jusqu’à 10 images, précision de mise en page/typographie), le schéma, et les cas où il faut rediriger vers Nano Banana Edit / Flux Kontext / GPT Image 2 t2i à la place. Lance `runcomfy run openai/gpt-image-2/edit` via le CLI local de RunComfy. S’active avec « gpt image edit », « gpt-image-edit », « chatgpt image edit », « edit with gpt image 2 », ou toute demande explicite d’édition avec ce modèle.

Veuillez toujours répondre dans la langue de l’utilisateur.
