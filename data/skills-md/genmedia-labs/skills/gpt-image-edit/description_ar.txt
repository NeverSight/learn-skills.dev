تحرير الصور باستخدام OpenAI GPT Image 2 (نقطة النهاية `/edit` في ChatGPT Images 2.0) على RunComfy — مدمجًا مع أنماط التوجيه الموثقة للنموذج بحيث يحصل المهارة على مخرجات أكثر حدة مقارنةً بالتوجيه السطحي على نفس النموذج. يوثّق قوة تعديل صور GPT (حفظ اللغة، تحرير النص داخل الصورة متعدد اللغات، مراجع متعددة حتى 10 صور، دقة التخطيط / الطباعة)، المخطط، ومتى يجب التحويل إلى Nano Banana Edit / Flux Kontext / GPT Image 2 t2i بدلاً من ذلك. يتم استدعاؤه عبر الأمر `runcomfy run openai/gpt-image-2/edit` في سطر أوامر RunComfy المحلي. يُفعَّل عند الكلمات “gpt image edit”، “gpt-image-edit”، “chatgpt image edit”، “edit with gpt image 2”، أو أي طلب صريح لتعديل باستخدام هذا النموذج.
