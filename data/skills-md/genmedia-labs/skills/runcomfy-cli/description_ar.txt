شغّل أي نموذج على RunComfy من سطر الأوامر. واجهة الأوامر runcomfy هي ملف تنفيذ واحد، توثيق واحد، مئات نقاط نهاية للنماذج — توليد صور، تعديل صور، توليد فيديو، صورة‑إلى‑فيديو، مزامنة شفاه، تبديل وجوه، تعديل فيديو، ملء داخل الصورة، ملء خارج الصورة، توسيع، ControlNet، إضاءة إضافية، تكبير، تدريب LoRA والمزيد. قدّم طلبًا، راقب الحالة، حمّل المخرجات. تُعلِّم هذه المهارة الوكيل كيفية التثبيت، المصادقة، اكتشاف مخططات النماذج، استدعاء النماذج، البث / الاستطلاع / بدون انتظار، البرمجة في وضع إخراج JSON، وتعامل مع الأخطاء. تُفَعَّل عند «runcomfy cli»، «install runcomfy»، «runcomfy login»، «runcomfy run»، «runcomfy whoami»، «runcomfy api»، أو أي طلب صريح لاستدعاء نموذج RunComfy من سكريبت أو طرفية. المهارات الشقيقة (ai-image-generation، ai-video-generation، image-edit، video-edit، face-swap، lipsync، image-to-video، image-inpainting، image-outpainting، video-extend، controlnet-pose، relight) كلها تُرسل عبر هذه الواجهة.
