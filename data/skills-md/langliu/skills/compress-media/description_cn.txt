使用 ImageMagick 压缩本地图像文件，使用 ffmpeg 压缩本地视频文件。当 Codex 需要减小 JPEG、PNG、WebP、HEIC、TIFF、MP4、MOV、MKV 或其他本地媒体的大小时使用；适用于批量压缩文件夹、在分享前缩放图像、自动将照片或截图转换为同一目录、同名的 WebP 文件、自动将视频压缩为同路径的 MP4/H.265、在保留原文件时将输出保存到镜像源目录结构的 “compressed” 目录、在压缩成功后检查输出大小、对输出为 0 KB 的情况进行重试，默认在报告压缩前后大小变化的同时删除或替换原文件。
