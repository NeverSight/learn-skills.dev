Use ImageMagick to compress local image files and ffmpeg to compress local video files. Use it when Codex needs to reduce the size of JPEG, PNG, WebP, HEIC, TIFF, MP4, MOV, MKV, or other local media; suitable for batch‑compressing folders, scaling images before sharing, automatically converting photos or screenshots to same‑directory, same‑name WebP files, automatically compressing videos to same‑path MP4/H.265, outputting to a “compressed” directory that mirrors the source directory structure when keeping the original files, checking the output size after successful compression, retrying 0 KB outputs, and by default deleting or replacing the original file while reporting the size change before and after compression.
