Esamina lo stato dell'elemento della configurazione di una richiesta pull GitHub utilizzando la CLI GitHub (`gh`). Da utilizzare quando l'utente chiede di verificare lo stato dell'elemento della configurazione, indagare sui controlli non riusciti o comprendere il motivo per cui una pipeline PR non riesce.
