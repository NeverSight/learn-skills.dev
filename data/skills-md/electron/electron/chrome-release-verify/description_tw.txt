Electron 發布分支的端對端 Chrome 安全性向後移植。給定 Chrome 發布部落格 URL 和分支（例如 41-x-y），確定「實際同步來源」中缺少哪些 CVE 修復，在本地寫入精選補丁，使用「esync --3」+「lint --patches」對其進行驗證，然後推送單個 PR。當被要求將 Chrome 安全版本向後移植到 N-x-y 時使用，“CVE-X 是否已經在 N-x-y 中？”，或為發布分支生成/驗證櫻桃選擇集。
