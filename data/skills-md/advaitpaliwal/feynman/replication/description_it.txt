Pianifica una replica di un documento, di un'affermazione o di un benchmark ed eseguila solo dopo una scelta esplicita dell'ambiente. Da utilizzare quando l'utente chiede di replicare i risultati, riprodurre un esperimento, verificare empiricamente un'affermazione o creare un pacchetto di replica.
