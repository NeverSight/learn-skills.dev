Utilisez une passerelle d'image compatible OpenAI pour la génération de texte en image ou d'image de référence avec une configuration locale unique. Se déclenche lorsque l'utilisateur demande à générer une image via une passerelle configurée, à utiliser une image de référence, à tester la connectivité de la passerelle, à mettre à jour les paramètres de la passerelle ou à enregistrer la sortie générée dans un chemin local spécifié.
