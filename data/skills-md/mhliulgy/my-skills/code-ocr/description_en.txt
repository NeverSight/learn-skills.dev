Use Baidu OCR High-Precision with Position to convert code screenshots into a text file while preserving the original indentation. Use cases include: (1) turning code screenshots into editable text, (2) extracting code from screenshots, (3) converting images to code text. Trigger conditions: user mentions "screenshot to text", "OCR", "recognize code", "image to text", "code screenshot to text"
