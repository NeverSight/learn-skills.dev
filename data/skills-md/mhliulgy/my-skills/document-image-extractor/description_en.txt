Extract images from Word (.docx) and PDF (.pdf) documents and save them to a specified folder. Use cases include: (1) extracting images from Word documents, (2) extracting images from PDF documents, (3) batch extracting images from multiple documents, (4) extracting all image assets from documents
