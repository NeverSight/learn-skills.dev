寫一個深色主題的 Mermaid 圖表源碼 — 根據讀者的問題選擇圖表類型（預設為 `flowchart`），套用真正的深色背景，使用 `classDef` 定義的語意色彩，根據字串長度選擇 `TD`/`LR` 方向，保持標籤簡短，圖中省略圖例，且在多次迭代中保持圖表不變。僅專注於繪圖；圖表如何提供給使用者（連結、檔案）由呼叫技能負責，例如 `explain-with-diagrams`。在需要為深色背景檢視 Mermaid 圖表時使用此指示。若需嵌入文件的圖形（在淺色與深色背景皆可閱讀），請使用 `mermaid-diagram-design`。

請始終以使用者的語言回覆。
