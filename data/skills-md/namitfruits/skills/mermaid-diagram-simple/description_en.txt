Write a dark‑mode Mermaid diagram source — choose the diagram type based on the question the reader is asking (`flowchart` by default), paint a true dark background, use semantics‑based colors with `classDef`, select `TD`/`LR` according to the length of the string, keep labels short, omit a legend in the figure, and preserve the diagram across turns. Focus only on drawing; how the diagram is delivered to the user (link, file) is the responsibility of the calling skill, e.g., `explain-with-diagrams`. Use this when you need to create a Mermaid diagram for viewing on a dark background. For a figure to embed in documentation (readable on both light and dark backgrounds), use `mermaid-diagram-design`.
