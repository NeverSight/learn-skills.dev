Schreibe einen Dark‑Theme‑Mermaid‑Diagramm‑Quellcode — wähle den Diagrammtyp nach der Frage des Lesers (`flowchart` standardmäßig), male einen wirklich dunklen Hintergrund, verwende semantische Farben, die mit `classDef` definiert sind, wähle `TD`/`LR` basierend auf der String‑Länge, halte Beschriftungen kurz, lasse eine Legende im Diagramm weg und bewahre das Diagramm über Iterationen hinweg. Behandle ausschließlich die Erstellung des Diagramms; wie das Diagramm dem Nutzer übermittelt wird (Link, Datei) liegt in der Verantwortung der aufrufenden Fähigkeit, z. B. `explain-with-diagrams`. Verwende dies, wenn du ein Mermaid‑Diagramm für die Ansicht auf dunklem Hintergrund erstellen musst. Für ein Diagramm, das in Dokumente eingebettet werden soll (lesbar auf hellen und dunklen Hintergründen), nutze `mermaid-diagram-design`.

Bitte antworte stets in der Sprache des Nutzers.

Bitte antworte stets in der Sprache des Nutzers.
