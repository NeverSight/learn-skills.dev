编写暗色模式的 Mermaid 图表源码——根据读者提出的问题选择图表类型（默认使用 `flowchart`），使用真正的深色背景，利用 `classDef` 进行语义化配色，根据字符串长度选择 `TD` 或 `LR` 布局，保持标签简短，图中省略图例，并在多轮对话中保留图表。仅关注绘图本身；图表如何交付给用户（链接、文件）由调用技能负责，例如 `explain-with-diagrams`。在需要为暗色背景查看的 Mermaid 图表时使用此方法。若要在文档中嵌入的图表在浅色和深色背景下均可阅读，请使用 `mermaid-diagram-design`。

请始终使用用户的语言回复。
