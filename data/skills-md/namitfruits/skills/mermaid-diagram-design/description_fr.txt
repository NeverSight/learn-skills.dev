Diagramme de sirène de style spec cho — « diagramme de flux à zones teintées » (diagramme de flux + sous-graphe tô nųn per vùng + classeDef palette + forme có nghųa). Chargez le bloc `` `mermaid` `` trong tài liệu, le contexte du système C1 de đɛc biệt, les modules C2, le flux utilisateur de la TR ? ? C khi vi ?t/s ? ? a b ? t k ? ? ?. Hình tų mang đų màu đđ đųc đų ųc ‹ mode lumière và mode sombre. Cěm C4Context/C4Container. Kèm `verify.py` — lint + render 2 nųn (sáng/tųi) Fichier de bouse en lecture seule, không sųa nội.
