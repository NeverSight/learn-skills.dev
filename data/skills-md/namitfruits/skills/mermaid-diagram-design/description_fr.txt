Spécification de style pour les diagrammes mermaid — « flowchart à zones teintées » (flowchart + sous‑graphe avec remplissage par zone + palette classDef + forme signifiée). Charger AVANT d’écrire ou de modifier tout bloc ```mermaid``` dans le document, en particulier C1 System Context, C2 Modules, user flow. Le diagramme doit contenir suffisamment de couleurs pour être lisible en mode clair et en mode sombre. Interdire C4Context/C4Container. Inclure `verify.py` — lint + rendu sur deux fonds (clair/sombre) pour vérification visuelle avant insertion. Lecture seule, ne pas modifier le contenu du fichier.
