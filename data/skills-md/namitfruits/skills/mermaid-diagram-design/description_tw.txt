mermaid 圖表的樣式規範 — 「區域染色流程圖」(流程圖 + 子圖以區域著色 + classDef 調色盤 + 具有意義的形狀)。在文件中撰寫/修改任何 ```mermaid``` 區塊之前先載入，尤其是 C1 系統上下文、C2 模組、使用者流程。圖形使用足夠的顏色，以便在淺色模式與深色模式下都能清晰閱讀。禁止使用 C4Context / C4Container。附帶 `verify.py` — 進行 lint 檢查並渲染兩種背景（亮色/暗色），以在貼上前目視核對。唯讀，不得修改檔案內容。
