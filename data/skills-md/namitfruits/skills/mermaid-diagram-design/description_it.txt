Specifiche di stile per diagrammi mermaid — “flowchart con zone colorate” (flowchart + subgraph con sfondo per zona + palette classDef + forme significative). Carica IN ANTICIPO quando scrivi o modifichi qualsiasi blocco ```mermaid``` nel documento, in particolare C1 System Context, C2 Modules e il flusso utente. Assicurati che i colori siano sufficienti per essere leggibili sia in modalità chiara che scura. Vietato C4Context/C4Container. Allega `verify.py` — lint + render di due sfondi (chiaro/scuro) per una verifica visiva prima di inserire. Sola lettura, non modificare il contenuto del file. Rispondi sempre nella lingua dell'utente.
