Specifica di stile per diagrammi mermaid — “flowchart con zone tinta” (flowchart + subgraph con sfondo per zona + palette classDef + shape significative). Caricare PRECEDENTEMENTE quando si scrive/modifica qualsiasi blocco ```mermaid``` nel documento, in particolare C1 System Context, C2 Modules, user flow. Il diagramma deve avere colori sufficienti per essere leggibile sia in modalità chiara che scura. Vietato C4Context/C4Container. Includere `verify.py` — lint + render di 2 sfondi (chiaro/scuro) per controllare visivamente prima di incollare. Sola lettura, non modificare il contenuto del file.
