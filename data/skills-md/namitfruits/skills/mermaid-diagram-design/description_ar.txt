مواصفات النمط لمخطط mermaid — "مخطط تدفق مصبوغ بالمناطق" (مخطط تدفق + subgraph صبغ خلفية لكل منطقة + لوحة ألوان classDef + أشكال ذات معنى). قم بتحميله مسبقًا عند كتابة/تحرير أي كتلة ```mermaid``` في المستند، خاصةً C1 System Context، C2 Modules، تدفق المستخدم. يجب أن تكون الألوان كافية للقراءة في الوضع الفاتح والداكن. يمنع استخدام C4Context/C4Container. أرفق `verify.py` — تدقيق + تصيير خلفيتين (فاتح/داكن) للمراجعة البصرية قبل اللصق. قراءة فقط، لا تُعدّل محتوى الملف.
