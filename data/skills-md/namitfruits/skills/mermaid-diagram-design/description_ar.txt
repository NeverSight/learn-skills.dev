مواصفات النمط لمخطط مِرْميد — "مخطط تدفق ملون بالمناطق" (مخطط تدفق + subgraph تلوين خلفية لكل منطقة + classDef لوحة ألوان + شكل ذو معنى). يجب تحميله مسبقًا عند كتابة/تعديل أي كتلة ```mermaid``` في المستند، خصوصًا C1 System Context، C2 Modules، تدفق المستخدم. يجب أن تكون الألوان كافية للقراءة في وضع الضوء والوضع المظلم. يُمنع استخدام C4Context/C4Container. أرفق `verify.py` — فحص lint + تصيير خلفيتين (فاتحة / داكنة) لمراجعتها بصريًا قبل اللصق. قراءة فقط، لا تعديل محتوى الملف.
