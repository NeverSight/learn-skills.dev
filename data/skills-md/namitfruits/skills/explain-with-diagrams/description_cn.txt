使用暗色主题的 Mermaid 图解释机制、错误、决策流或架构，采用语义颜色并提供可即时打开的 mermaid.live/edit 链接——分为两种模式——`easy`（默认）通过“一句话 → 隐喻 → 真实事件展开”的模式进行一对一隐喻解释，`tech`（`--tech`）则按照 `Context` · `Problem` · `Root cause` 的格式命名具体的文件/函数/字段。仅描述现象；不要提出修复方案，也不要在聊天中粘贴 Mermaid 源码。当用户说“画图”“画示意图”“mermaid”“为易懂绘制”“用图描述”，或询问“为什么 X”且答案涉及多层/分支/状态，或文字解释仍不清晰时使用。请包含一个 `mermaid-link.mjs`，用于生成离线链接并验证往返。

请始终使用用户的语言作答。
