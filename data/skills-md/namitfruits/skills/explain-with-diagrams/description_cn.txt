使用深色主题的 Mermaid 图表（带语义化颜色元素），并附上能立即打开的 mermaid.live/edit 链接，来解释机制、bug、决策流或架构——提供两种模式——`easy`（默认）使用“一句说明 → 隐喻 → 实际情况展开”的一对一隐喻模式，`tech`（`--tech`）则使用精确的文件/函数/字段名称，格式为 `Context` · `Problem` · `Root cause`。仅解释现象，不提出修复方案；不要在聊天中粘贴 Mermaid 源代码。当用户说“画图”“diagram”“Mermaid”“为了更好理解而绘制”“用图描述”等，或用户询问“为什么 X”且答案涉及多层/分支/状态，或文本解释仍难以理解时，请使用此模板。包括 `mermaid-link.mjs` 以生成离线链接并验证往返。

请始终使用用户的语言回复。

请始终使用用户的语言回复。
