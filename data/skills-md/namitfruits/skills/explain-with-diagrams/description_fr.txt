Expliquez un mécanisme, un bug, un flux de décision ou une architecture en utilisant un diagramme Mermaid à thème sombre avec des couleurs basées sur la sémantique, et incluez un lien mermaid.live/edit qui peut être ouvert immédiatement — deux modes — `easy` (par défaut) explique avec une métaphore 1‑à‑1 selon le schéma *une phrase → métaphore → comment l'événement réel se déroule*, `tech` (`--tech`) utilise les noms exacts de fichiers/fonctions/champs selon le schéma `Contexte` · `Problème` · `Cause racine`. Décrivez uniquement le phénomène, ne suggérez pas de correctifs ; ne collez pas le code source Mermaid dans le chat. Utilisez cela lorsque l'utilisateur dit « draw a diagram », « diagram‑ize », « mermaid », « draw for clarity », « describe with a diagram », lorsqu’il demande « why X » et que la réponse comporte de nombreuses couches/branches/états, ou lorsqu’une explication textuelle n’est toujours pas claire pour l’utilisateur. Incluez un `mermaid-link.mjs` qui génère un lien hors ligne et vérifie le round‑trip.

Veuillez toujours répondre dans la langue de l’utilisateur.
