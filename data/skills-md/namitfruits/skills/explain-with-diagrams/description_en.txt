Explain a mechanism, a bug, a decision flow, or an architecture using a dark‑themed Mermaid diagram with semantically colored elements, accompanied by a mermaid.live/edit link that opens immediately — two modes — `easy` (default) explains with a one‑to‑one metaphor in the pattern *one sentence → metaphor → how the real situation unfolds*, `tech` (`--tech`) uses the exact file/function/field names in the format `Context` · `Problem` · `Root cause`. Only explain the phenomenon, do not propose fixes; do not paste the Mermaid source into the chat. Use this when the user says “draw a diagram”, “diagram”, “Mermaid”, “draw for easier understanding”, “describe with a diagram”, when the user asks “why X” and the answer has many layers/branches/states, or when a textual explanation still isn’t grasped. Include `mermaid-link.mjs` to generate an offline link + verify round‑trip.

Please always respond in the user’s language.
