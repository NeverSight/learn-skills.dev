Explain a mechanism, a bug, a decision flow, or an architecture with a dark‑themed Mermaid diagram, using semantic colors and a mermaid.live/edit link that opens instantly — in two modes — `easy` (default) which explains via a one‑to‑one metaphor in the pattern *one sentence → metaphor → how the real event unfolds*, and `tech` (`--tech`) which names the exact file/function/field following the format `Context` · `Problem` · `Root cause`. Only describe the phenomenon; do not propose a fix, and do not paste the Mermaid source into the chat. Use this when the user says “draw a diagram”, “diagram it”, “mermaid”, “draw for easy understanding”, “describe with a diagram”, when the user asks “why X” and the answer involves multiple layers/branches/states, or when a textual explanation still isn’t clear to the user. Include a `mermaid-link.mjs` that generates an offline link and verifies round‑trip.
