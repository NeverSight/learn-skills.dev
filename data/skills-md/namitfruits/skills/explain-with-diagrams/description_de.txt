Erkläre einen Mechanismus, einen Bug, einen Entscheidungsfluss oder eine Architektur mit einem dark‑themed Mermaid‑Diagramm, das semantische Farben nutzt und einen mermaid.live/edit‑Link, der sofort öffnet — in zwei Modi — `easy` (Standard), das über eine Eins‑zu‑Eins‑Metapher im Muster *ein Satz → Metapher → wie das tatsächliche Ereignis abläuft* erklärt, und `tech` (`--tech`), das die genaue Datei/Funktion/Feld nach dem Format `Context` · `Problem` · `Root cause` benennt. Beschreibe ausschließlich das Phänomen; schlage keine Lösung vor und füge den Mermaid‑Quellcode nicht in den Chat ein. Verwende dies, wenn der Nutzer sagt “draw a diagram”, “diagram it”, “mermaid”, “draw for easy understanding”, “describe with a diagram”, wenn der Nutzer fragt “why X” und die Antwort mehrere Ebenen/Zweige/Zustände umfasst, oder wenn eine textuelle Erklärung dem Nutzer noch nicht klar ist. Füge ein `mermaid-link.mjs` hinzu, das einen Offline‑Link generiert und die Rundreise überprüft.
