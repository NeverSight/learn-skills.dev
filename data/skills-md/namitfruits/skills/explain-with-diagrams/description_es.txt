Explica un mecanismo, un error, un flujo de decisión o una arquitectura con un diagrama Mermaid de tema oscuro, usando colores semánticos y un enlace mermaid.live/edit que se abra al instante — en dos modos — `easy` (predeterminado) que explica mediante una metáfora uno‑a‑uno en el patrón *una oración → metáfora → cómo se desarrolla el evento real*, y `tech` (`--tech`) que nombra el archivo/función/campo exacto siguiendo el formato `Contexto` · `Problema` · `Causa raíz`. Sólo describe el fenómeno; no propongas una solución, y no pegues el código fuente de Mermaid en el chat. Utiliza esto cuando el usuario diga “draw a diagram”, “diagram it”, “mermaid”, “draw for easy understanding”, “describe with a diagram”, cuando el usuario pregunte “why X” y la respuesta implique múltiples capas/ramas/estados, o cuando una explicación textual aún no sea clara para el usuario. Incluye un `mermaid-link.mjs` que genere un enlace sin conexión y verifique el ida‑y‑vuelta.

Por favor, responde siempre en el idioma del usuario.
