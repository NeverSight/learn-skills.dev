Explica un mecanismo, un error, un flujo de decisiones o una arquitectura utilizando un diagrama Mermaid de tema oscuro con elementos coloreados semánticamente, acompañado de un enlace mermaid.live/edit que se abra inmediatamente — dos modos — `easy` (predeterminado) explica con una metáfora uno‑a‑uno siguiendo el patrón *una frase → metáfora → cómo se desarrolla la situación real*, `tech` (`--tech`) usa los nombres exactos de archivos/funciones/campos en el formato `Contexto` · `Problema` · `Causa raíz`. Sólo explica el fenómeno, no propongas soluciones; no pegues el código fuente de Mermaid en el chat. Usa esto cuando el usuario diga “dibujar un diagrama”, “diagrama”, “Mermaid”, “dibujar para entender mejor”, “describir con un diagrama”, cuando el usuario pregunte “por qué X” y la respuesta tenga muchas capas/ramas/estados, o cuando una explicación textual aún no se comprenda. Incluye `mermaid-link.mjs` para generar un enlace offline + verificar el round‑trip.

Por favor, responde siempre en el idioma del usuario.
