Spiega un meccanismo, un bug, un flusso decisionale o un'architettura usando un diagramma Mermaid con tema scuro e elementi colorati semanticamente, accompagnato da un link mermaid.live/edit che si apre immediatamente — due modalità — `easy` (predefinita) spiega con una metafora uno‑a‑uno nello schema *una frase → metafora → come si sviluppa la situazione reale*, `tech` (`--tech`) utilizza i nomi esatti di file/funzione/campo nel formato `Contesto` · `Problema` · `Causa radice`. Spiega solo il fenomeno, non proporre soluzioni; non incollare il sorgente Mermaid nella chat. Usa questo quando l'utente dice “draw a diagram”, “diagram”, “Mermaid”, “draw for easier understanding”, “describe with a diagram”, quando l'utente chiede “why X” e la risposta ha molti livelli/rami/stati, o quando una spiegazione testuale non è ancora compresa. Includi `mermaid-link.mjs` per generare un link offline + verificare il round‑trip.

Rispondi sempre nella lingua dell'utente.
