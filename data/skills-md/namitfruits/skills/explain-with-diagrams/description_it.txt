Spiega un meccanismo, un bug, un flusso decisionale o un'architettura con un diagramma Mermaid a tema scuro, usando colori semantici e un link mermaid.live/edit che si apre istantaneamente — in due modalità — `easy` (predefinita) che spiega tramite una metafora uno‑a‑uno nello schema *una frase → metafora → come si sviluppa l’evento reale*, e `tech` (`--tech`) che denomina il file/funzione/campo esatto seguendo il formato `Context` · `Problem` · `Root cause`. Descrivi solo il fenomeno; non proporre una soluzione e non incollare il codice sorgente Mermaid nella chat. Usa questo quando l'utente dice “draw a diagram”, “diagram it”, “mermaid”, “draw for easy understanding”, “describe with a diagram”, quando l'utente chiede “why X” e la risposta coinvolge più livelli/rami/stati, o quando una spiegazione testuale non è ancora chiara per l'utente. Includi un `mermaid-link.mjs` che genera un link offline e verifica il round‑trip.
