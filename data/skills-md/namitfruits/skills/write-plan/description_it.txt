Scrivi un documento di piano di implementazione (plan doc) per una feature — presentalo nella chat precedente, fissa l'ID del lavoro, costruisci il file secondo una struttura fissa di 7 sezioni (Problem · Goal · Mental model · Probe · Decisions · Design · Phases — Probe opzionale) con i simboli 🤖/👤, e spunta fino a dove hai lavorato. Usalo quando l'utente dice "lập kế hoạch", "viết plan cho X", "lưu lại plan", "tạo doc plan", "thiết kế X trước khi code", o quando una richiesta è sufficientemente grande da richiedere un piano prima di modificare il codice. Leggi le convenzioni del progetto (CLAUDE.md / AGENTS.md / cartella docs) per ottenere il **binding** — cartella, sistema di ID, documento sorgente, comando di verifica — e il **formato del documento segue questa skill**. Allegati `verify.py` — lint della struttura delle sezioni, catena `P → D → DS → phase`, copertura `DS`, spunta/stato.
