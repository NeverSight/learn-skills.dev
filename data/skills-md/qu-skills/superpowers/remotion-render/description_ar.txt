قم بتوليد مقاطع فيديو من شفرة مكوّن React/Remotion عبر inference.sh. مرّر شفرة TSX، واحصل على MP4. يدعم جميع واجهات برمجة تطبيقات Remotion: useCurrentFrame، useVideoConfig، spring، interpolate، AbsoluteFill، Sequence. دقة قابلة للتعديل، عدد الإطارات في الثانية (FPS)، المدة، الترميز. الاستخدامات: توليد الفيديو برمجيًا، الرسوم المتحركة، تصميم الحركة، الفيديوهات المدفوعة بالبيانات، تحويل تحركات React إلى فيديو. الكلمات المفتاحية: remotion، render video from code، tsx to video، react video، programmatic video، remotion render، code to video، animated video، motion graphics code، react animation video. يرجى دائمًا الرد بلغة المستخدم.
