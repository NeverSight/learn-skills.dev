قم بتنفيذ كود بايثون في بيئة محمية وآمنة عبر [inference.sh](https://inference.sh). مثبت مسبقًا: NumPy، Pandas، Matplotlib، requests، BeautifulSoup، Selenium، Playwright، MoviePy، Pillow، OpenCV، trimesh، وأكثر من 100 مكتبة أخرى. استخدم لـ: معالجة البيانات، استخراج الويب، تعديل الصور، إنشاء الفيديو، معالجة نماذج ثلاثية الأبعاد، توليد ملفات PDF، استدعاءات API، سكريبتات الأتمتة. الكلمات المفتاحية: python، execute code، run script، web scraping، data analysis، image processing، video editing، 3D models، automation، pandas، matplotlib.
