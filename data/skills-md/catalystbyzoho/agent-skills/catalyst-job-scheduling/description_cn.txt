Catalyst作业调度—作业池、即时/后台作业，以及执行作业功能、Webhook、电路、AppSail的cron （周期、OneTime、日历、CronExpression ）。触发'job scheduling'、'job pool'、'jobpool_id'、'submit a job'、'immediate job'、'cron job'、'scheduled function'、'submitJob'、'createCron'、'retry_interval'、'time_of_execution'或错误'not a job function'、'job_name必须仅包含字母数字'。替换已弃用的Cron。
