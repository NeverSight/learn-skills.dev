Elija la herramienta más eficiente en cuanto a tokens para una tarea de codificación en lugar de leer reflexivamente archivos completos, volcar la salida de comandos sin procesar en contexto o escribir más código del solicitado. Utilice esta habilidad ANTES de explorar o explicar una base de código, localizar un símbolo/definición/personas que llaman, rastrear una ruta de llamada, mapear una arquitectura, planificar o implementar una función en capas o ejecutar comandos detallados (pruebas, compilaciones, git, grep, listados de directorios) y siempre que surja el contexto/presupuesto de token, costo o "hacer que esto use menos tokens". Dirige el escenario a serena, rtk, cavernícola, cola de caballo o herramientas simples, con comandos concretos y las combinaciones a usar o evitar. Consíguelo incluso cuando el usuario no nombra una herramienta: si estás a punto de `cat`/Leer varios archivos, o a punto de generar una gran implementación, ese es el desencadenante.
