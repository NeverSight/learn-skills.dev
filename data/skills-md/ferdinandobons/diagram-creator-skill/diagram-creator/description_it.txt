Genera splendidi diagrammi di architettura pronti per la produzione come file HTML autonomi da qualsiasi input. Utilizza questa competenza ogni volta che l'utente desidera creare un diagramma, uno schema, un diagramma di flusso, un diagramma di rete, una visualizzazione di sistema, una sequenza temporale, un organigramma o qualsiasi rappresentazione visiva di concetti, tecnici o non tecnici. Si attiva quando l'utente menziona "diagramma", "schema", "architettura", "flusso", "topologia", "pipeline", "linea temporale", "visualizza", "grafico" o chiede di trasformare un file in un diagramma visivo. Accetta QUALSIASI tipo di file come input (.md, .txt, .json, .yaml, .csv, .pdf, .py, .ts, .toml, .env, .tf, .dockerfile, ecc.) — Claude rileva automaticamente il formato ed estrae la struttura pertinente nel diagramma. Anche una semplice frase come "diagramma di come funziona OAuth" o "visualizza questo file" attiva questa abilità.
