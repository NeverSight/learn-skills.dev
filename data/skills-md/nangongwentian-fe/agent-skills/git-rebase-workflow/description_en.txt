Git Rebase branch synchronization process, used to rebase the current feature branch onto the latest target branch (such as master/main), keeping the commit history clean. Suitable for scenarios where the feature branch is behind the target branch and needs to synchronize the latest code.
