[Skill Security Check] Scan the skills installed in each Agent's host directory on this machine, identify unauthorized signals such as covert traffic diversion, hidden commercial relationships, hijacking user commands, reading sensitive data, and suspicious external calls; by default, only a report is generated, and the problematic skill is isolated to a local quarantine area (recoverable, not deleted) only after the user explicitly confirms. Used when the user says “scan the skills I’ve installed,” “check if a skill has ads,” “is this skill safe,” “clean problematic skills,” or “review my skills.” | Produced by Xiao Ye
