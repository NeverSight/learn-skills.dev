Usa solo quando l'utente chiede esplicitamente TDD, un test fallito o un test di regressione, O quando il bug ha un obiettivo di test locale evidente e a basso costo. Salta quando il percorso del test è poco chiaro, costoso, pesante di integrazione, o non richiesto.

Rispondi sempre nella lingua dell'utente.
