مكتبة اختبار React وأفضل ممارسات الاختبار المستندة إلى Vitest. يُستخدم في المواقف التالية: (1) عند العمل مع ملفات .test.tsx و.test.ts و.spec.tsx و.spec.ts، (2) عند كتابة اختبارات المكونات أو إعادة هيكلتها، (3) عند العمل باستخدام كلمات رئيسية مثل 'test'، و'testing'، و'vitest'، و'RTL'، و'getByRole'، و'userEvent'، و'waitFor'، و'expect'، (4) عند كتابة اختبارات لـ المنطق غير المتزامن أو تفاعلات المستخدم
