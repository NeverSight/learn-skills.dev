Write or revise an ongoing plan: located at <docs_root>/plans/<topic>/ (see docs_root in .agents/skills/capmap-system/capmap.yaml); a ## Change Log must be maintained. After writing or revising, use a grilling approach to iteratively ask the user about items pending confirmation; once consensus is reached, mark them as confirmed/ready to proceed. New requirements for an archived plan must be opened as an incremental version (Vn). The plan’s primary status carries development/validation/implementation progress. It can be used independently.
