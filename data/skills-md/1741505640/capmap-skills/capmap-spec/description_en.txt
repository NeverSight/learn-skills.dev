Large-demand execution specifications: Once the plan is confirmed and its scale is large, write to `<docs_root>/方案/<主题>/切片/<方案stem>/<方案stem>-00-规格.md` (seams, scope, exclusions, intended dependency graph), and set the plan’s primary status to “in specification.” After others have verified the specification, hand it over to `capmap-slice`. Do not split slices, and do not begin coding.
