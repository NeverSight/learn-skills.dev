Large-demand slice access control: Compute the frontier within slice/<scheme stem>/ (for pending development slices whose dependencies have all been accepted), display and be able to repeat the recommended execution mode; start only after the user explicitly names it. Ticket submissions must include Demo steps + self‑test summary → pending acceptance; only after human confirmation is it considered accepted and the frontier is recomputed. Prohibit proactive run starts and prohibit agents from self‑approving.
