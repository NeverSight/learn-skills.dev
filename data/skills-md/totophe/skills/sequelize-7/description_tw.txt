Sequelize v7（alpha）的綜合參考，這是 TypeScript 優先的 Node.js ORM，支援 PostgreSQL、MySQL、MariaDB、SQLite、MSSQL、DB2 和 Snowflake。使用 @sequelize/core 套件時使用：使用裝飾器定義模型、編寫資料庫查詢（CRUD、關聯、原始 SQL）、管理交易、掛鉤、連接池、設定關聯或從 Sequelize v6 遷移到 v7。
