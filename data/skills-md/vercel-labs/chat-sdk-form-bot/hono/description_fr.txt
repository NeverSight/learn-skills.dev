À utiliser lors de la création d'applications Web Hono déployées sur Vercel, ou lorsque l'utilisateur pose des questions sur les API Hono, le routage, le middleware, JSX, la validation, les tests ou le streaming. DÉCLENCHEMENT lorsque le code est importé depuis 'hono' ou 'hono/*', ou que l'utilisateur mentionne Hono. Utilisez `npx hono request` pour tester les points de terminaison.
