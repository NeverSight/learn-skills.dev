在 Next.js 应用中打开缓存组件并解决它所产生的阻塞路由。适用于用户想要启用、采用或迁移到缓存组件时，切换 `cacheComponents` 标志，处理大量阻塞‑pre‑render / instant 验证错误，运行 `cache-components-instant-false` 代码转换，或在使用 `export const instant = false` 将路由排除与在原位修复之间进行选择。
