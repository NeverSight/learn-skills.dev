amis low-code framework expert and knowledge base.  
**Strict trigger conditions**: The user must explicitly mention keywords such as 'amis', 'low-code', or 'Baidu amis'.  
**Core capabilities**:  
1. Answer amis usage questions (component usage, property configuration, expressions, events, etc.)  
2. Generate amis JSON schema (CRUD, forms, cards, dialogs, etc.)  
3. Generate amis static HTML preview pages (only when the user explicitly requests amis + preview/static HTML)  
4. Debug and optimize amis configurations  

**Strictly prohibited triggers**:  
1. The user does not mention amis or low-code  
2. The user explicitly requests other frameworks (React, Vue, Angular, Vite, or other engineering projects)  
3. The user only says 'generate form' without mentioning amis  

**Keywords**: amis, low-code, Baidu amis, aisuda, amis schema, amis components, amis preview.
