Intelligently convert approved OpenSpec changes into td task issues when a user applies a change or approves implementation. Creates grouped work sessions, discovers gaps, and maintains continuity between planning and execution using the td CLI.
