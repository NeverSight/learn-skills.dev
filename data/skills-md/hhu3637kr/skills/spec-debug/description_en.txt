Diagnose and fix issues found during Spec execution. Invoked by the role spec-debugger. Trigger conditions: (1) role spec-debugger receives a bug notification from spec-tester, (2) a bug occurs after spec-executor runs or an unanticipated situation not covered in plan.md arises, (3) runtime problems, dependency, or configuration issues. Do not modify the confirmed plan.md; instead, create separate diagnostic documents (debug-xxx.md) and fix summaries (debug-xxx-fix.md). After the fix, notify spec-tester to re-verify, closing the loop.
