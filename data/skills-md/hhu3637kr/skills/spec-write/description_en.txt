Write the plan.md code implementation. Called by the role spec-writer. Trigger conditions: (1) role spec-writer needs to create design documents, API specifications, data models, architecture designs, or refactoring plans, (2) user says "create Spec"/"write design doc"/"write tech spec"/"design plan", (3) plan.md is required to be created under spec/ directory. Note: from v2.0 onward plan.md does not include the test plan section (created separately by spec-tester via spec-test), execution_mode is fixed as single-agent. If summary.md already exists under the directory, use spec-update instead of this Skill.
