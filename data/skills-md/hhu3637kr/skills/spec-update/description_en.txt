Update iterations of existing features (single-Agent mode). Trigger: changes in requirements or design for a completed feature (plan.md + summary.md already exist in the directory). Responsible for the full process from creating update-xxx.md through implementation and review. If the update is large and requires multi-role collaboration, create a new Spec and follow the spec-start process.
