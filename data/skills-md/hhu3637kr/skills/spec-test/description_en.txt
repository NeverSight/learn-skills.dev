Test plan writing and test execution. Triggered by the role spec-tester. Trigger conditions: (1) During the Spec creation phase, spec-tester needs to write test-plan.md; (2) After the implementation phase is completed, spec-tester needs to execute tests and produce test-report.md; (3) After spec-debugger fixes the bug, spec-tester needs to re-verify.
