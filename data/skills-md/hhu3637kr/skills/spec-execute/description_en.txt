Strictly follow plan.md to develop new features. Invoked by the role spec-executor. Trigger conditions: (1) the role spec-executor needs to implement code according to plan.md, (2) the user says "execute Spec" / "start implementation", (3) a plan.md exists in the spec/ directory but no summary.md. Note: from v2.0, path B (agent-teams) and test steps are removed; spec-execute has only one duty: implement code according to plan.md, produce summary.md, and archive. If the directory already contains summary.md, use spec-update.
