Use when the user explicitly requests "generate NSFC technical roadmap/technical roadmap drawing/roadmap/flowchart" or needs to convert proposal research content into a "printable, A4-readable" technical roadmap. Output editable source file (`.drawio`) and renderings embeddable in documents (`.svg`/`.png`/`.pdf`). ⚠️ Not applicable: the user only wants to modify format/size of an existing image (should use image processing skill), or only wants to polish the textual description of the technical route (should directly revise the main text).
