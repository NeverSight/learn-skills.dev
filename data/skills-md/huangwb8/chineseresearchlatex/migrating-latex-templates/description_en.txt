Used when the user explicitly requests "migrate proposal," "upgrade template," "cross-version migration," or "convert old proposal to new template." Intelligently migrates NSFC LaTeX proposals to the new template, based on a five-stage workflow (analysis → mapping → planning → execution → validation), automatically handling structural changes, content reorganization, and reference updates; supports AI-driven semantic matching and heuristic fallback, and provides isolated runs output with one-click migration script.
