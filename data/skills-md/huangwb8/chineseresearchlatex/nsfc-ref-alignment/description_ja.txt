一貫性と真実性のリスクについて、NSFC 提案本体の引用を参照と照合してチェックします (読み取り専用)。bibkey の存在、BibTeX フィールド、DOI 形式の問題を検証し、ホスト AI 用の構造化入力を生成して、「本文が実際にこの参照を引用しているかどうか」を行ごとに評価します。デフォルトでは、ユーザーが明示的に要求しない限り、提案または .bib は変更されず、監査レポートのみが出力されます。
