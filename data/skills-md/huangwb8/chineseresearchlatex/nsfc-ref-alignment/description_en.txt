Check the NSFC proposal for consistency and authenticity risks between in-text citations and references (read-only): verify whether the bibkey exists, BibTeX fields and DOI formatting issues, and generate structured input for the host AI to assess “does the main text statement actually cite this reference”; by default, output only an audit report without modifying the proposal or .bib (unless the user explicitly requests).
