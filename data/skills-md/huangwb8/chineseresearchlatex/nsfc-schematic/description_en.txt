Use this only when the user explicitly asks to “generate NSFC schematic/mechanism diagram” or needs to convert the research mechanism, algorithm architecture, or module relationships in a proposal into an “editable + embeddable” figure. Deliver an editable source file (`.drawio`) and a render-ready file (`.svg`/`.png`, optionally `.pdf`), supporting multi-round refinement.  
⚠️ Not applicable: the user only wants to polish the main text (rewrite the text directly), only wants to change the format/size of an existing image (use image-processing skills), or has not clearly requested a “schematic/mechanism diagram.”
