Utilice esto solo cuando el usuario solicite explícitamente "generar un diagrama esquemático/mecanismo NSFC" o necesite convertir el mecanismo de investigación, la arquitectura del algoritmo o las relaciones de los módulos en una propuesta en una figura "editable + integrable". Entregue un archivo fuente editable (`.drawio`) y un archivo listo para renderizar (`.svg`/`.png`, opcionalmente `.pdf`), que admita el refinamiento de múltiples rondas.  
⚠️ No aplicable: el usuario solo quiere pulir el texto principal (reescribir el texto directamente), solo quiere cambiar el formato/tamaño de una imagen existente (usar habilidades de procesamiento de imágenes) o no ha solicitado claramente un “diagrama esquemático/de mecanismo”.
