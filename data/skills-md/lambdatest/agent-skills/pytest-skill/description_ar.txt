يُنشئ اختبارات pytest على مستوى الإنتاج في لغة Python باستخدام التركيبات، والبارامترات، والعلامات، والسخرية، وأنماط المسابقة. يُستخدم عندما يذكر المستخدم "pytest"، و"conftest"، و"@pytest.fixture"، و"@pytest.mark"، و"Python test". مشغلات على: "pytest"، و"conftest"، و"Python test"، و"parametrize"، و"Python Unit test".
