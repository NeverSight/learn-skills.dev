Utilizzato quando il processo, la regola, i dati, la composizione o il comportamento dello stato di una funzione limitata richiede un modello di test o quando l'utente richiede esplicitamente l'uso di PPDCS.
