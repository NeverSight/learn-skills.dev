ABC Jenkins project release skill. Supports intelligent parameter inference and interactive triggering of Jenkins builds, automatically fetching Git branches and tags. Triggered when users request "release Jenkins", "trigger build", "deploy project", "Jenkins release", or similar. Requires environment variables JENKINS_USER and JENKINS_TOKEN.
