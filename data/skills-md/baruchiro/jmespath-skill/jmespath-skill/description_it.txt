Questa competenza dovrebbe essere utilizzata quando l'utente deve creare query JMESPath da JSON e una richiesta, eseguire il debug di una query esistente su JSON o chiedere informazioni sulla sintassi JMESPath (ad esempio fallback, valori letterali, filtri).
