Esta habilidad debe usarse cuando el usuario necesita crear consultas JMESPath a partir de JSON y una solicitud, depurar una consulta existente en JSON o preguntar sobre la sintaxis de JMESPath (por ejemplo, alternativas, literales, filtros).
