Creative pack — use only when a UI task explicitly needs personality, delight, brand expression, marketing/editorial energy, or a non-default visual direction. Skip for utility UI unless the default-safe output is the problem.
