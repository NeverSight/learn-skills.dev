يُستخدم عند إنشاء مجموعات SDK للغات متعددة من مواصفات OpenAPI واحدة، أو متغيرات SDK متعددة من مصادر مختلفة. يغطي تكوين Workflow.yaml متعدد الأهداف، وgen.yaml لكل لغة، وهيكل monorepo. مشغلات على "مجموعات SDK المتعددة"، و"SDK متعدد اللغات"، و"SDK لكل لغة"، و"SDK متعدد الأهداف"، و"SDK monorepo"، و"إنشاء مجموعات SDK لـ".
