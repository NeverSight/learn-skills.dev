Guide the LLM to correctly use the React Query and Vue Query hooks generated by the aptx code generator. Trigger when the LLM modifies parameters (such as adding enabled, staleTime, etc.) when using the generated useXxxQuery or useXxxMutation hooks in the project.
