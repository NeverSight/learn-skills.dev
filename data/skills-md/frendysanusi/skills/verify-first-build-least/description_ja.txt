実際のコードに根拠を持たせてから変更を書き、動く最小のものを作り、それを既存のコードに統合する。自分が十分に把握していないコードベースで機能や修正、チケットを実装するとき、また「まず検証しろ」「仮定しないで」「コードを先に確認」「最小限にする」「再利用できるものは何か」「既存のメソッドはあるか」「YAGNI」「少なくする」などと言われたとき、肥大化や不要な依存に不満があるときに使う。新しい依存を追加する前に標準ライブラリやネイティブプラットフォーム機能を優先する階層、主張をするために必要な証拠、決して簡略化してはならないものを網羅している。
