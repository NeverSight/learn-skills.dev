Pull-request review that verifies every claim against the code before asserting it, proves numeric findings with a runnable dry run, and triages what is worth raising. Use whenever the user asks to review a PR, review changes, look at a diff, check a branch, or says "review #N", "what's wrong with this PR", "is this mergeable", or hands you a PR URL. Also use for casual asks like "take a look at 414?". Posts findings to GitHub only when the user asks for that.
