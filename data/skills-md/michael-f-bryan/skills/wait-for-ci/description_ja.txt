GitHub Actions の実行が最小限の端末出力と信頼性の高い終了コードで終了するまで待ちます。エージェントが CI が通過するのを待つ必要がある場合 (プッシュ後、PR を開いた後、または特定の実行を検証する場合など) に使用します。ポーリング出力によるコンテキストのフラッディングを避けるために、 --exit-status および --compact を使用して gh run watch を実行することを推奨します。
