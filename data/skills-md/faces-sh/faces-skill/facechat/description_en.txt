Chat with any face in your catalog. Invoke as /facechat <alias> <message> to send a message directly (e.g. /facechat socrates How are you?), /facechat <alias> to start a conversation, or /facechat with no argument to browse your catalog and pick a face. Trigger when the user says "chat with a face", "talk to <name>", "ask <name>", or wants to have a conversation with a compiled face. Also trigger on /facechat.
