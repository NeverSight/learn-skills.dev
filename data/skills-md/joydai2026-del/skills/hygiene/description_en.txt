Monthly environment hygiene sweep for this Mac: disk, vault, Claude config, package managers. Identifies stale projects, orphaned files, vault drift, skill rot, and package cruft. ALWAYS presents findings for review BEFORE deleting anything — never auto-deletes. Use when: "run hygiene", "monthly cleanup", "environment audit", "disk cleanup", "clean up my machine", "vault hygiene", "first Monday". PROACTIVE TRIGGER (CONDITIONAL): On the first Monday of each month (scheduled via MCP scheduled-tasks). Also when the owner mentions running low on disk space, noticing vault drift, or after a major project completes.
