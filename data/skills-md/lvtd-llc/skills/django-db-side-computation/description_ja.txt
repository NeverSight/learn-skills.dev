Djangoのパフォーマンスに敏感なPythonループを、アノテーション、集約、フィルタリングされた集約、F式、関数、Subquery、OuterRef、Exists、Window式、db_default、およびGeneratedFieldを使用したデータベース側の計算に移動します。Djangoコードが行をフェッチした後にPythonで値をカウント、合計、フィルタ、フラグ、ランク、または派生する場合に使用します。
