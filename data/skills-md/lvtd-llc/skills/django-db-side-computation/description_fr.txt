Déplacez les boucles Python sensibles aux performances de Django dans le calcul côté base de données avec des annotations, des agrégats, des agrégats filtrés, des expressions F, des fonctions, Subquery, OuterRef, Exists, Window expressions, db_default et GeneratedField. À utiliser lorsque le code Django compte, additionne, filtre, marque, classe ou dérive des valeurs en Python après avoir récupéré des lignes.
