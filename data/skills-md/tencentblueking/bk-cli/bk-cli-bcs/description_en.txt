Used when you need to invoke the BCS API via `bk-cli bcs`; currently it primarily covers operations for clusters, nodes, node groups, node templates, tasks, template configurations, and virtual cluster quotas under `bk-cli bcs cluster_manager`.
