當您需要透過 `bk-cli apigateway` 探索 BlueKing API Gateway 中的所有公開網關、瀏覽特定網關的公開 API/資源清單、取得 OpenAPI/Schema、判斷如何呼叫特定介面，或在 `apigateway` 子指令與 `bk-cli api` 之間作選擇時，請使用此功能。特別是當使用者尚未知道或不確定要呼叫哪個網關或公開介面、介面名稱為何，且僅知道業務目標、關鍵字或資源名稱的片段時，您應該優先使用此技巧——先搜尋，再確認——而不是直接猜測介面。

請始終以使用者的語言回應。
