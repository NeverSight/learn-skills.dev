Use it when the task involves the general usage rules of bk-cli—especially authentication, context switching, stage/timeout, header/body, tenant, dry‑run, and verbose—or when users are unsure how to send requests between `bk-cli api`, `apigateway`, `cmdb`, and subsequent systems, why requests fail, or which inputs are shared behaviors; in such cases, this skill should be used first.
