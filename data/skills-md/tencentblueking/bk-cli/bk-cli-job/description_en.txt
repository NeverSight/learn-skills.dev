Use it when you need to use job platform–related system subcommands via `bk-cli job`. It is suitable for scenarios such as quickly executing scripts, distributing files, querying job status, and viewing execution logs.
