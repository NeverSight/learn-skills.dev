在 wonder-image/new-site 或任何基于它搭建的站点中工作。当 composer.json 依赖 `wonder-image/app` 但它不是框架包时，或当工作涉及 `custom/`、`app/Models`、`app/Resources`、`lang/`、`assets/{ASSETS_VERSION}/`、`PRODUCT.md`、`php forge credentials`、`php forge update --local`、`php forge start` 或 npm 设置时使用。涵盖页面、模型、资源、重复器、可编辑内容、翻译、路由、角色、权限、外部模块、组件覆盖以及必须复用 `wonder-image/lib` 的 UI/样式。不要用于 `wonder-image/app` 核心、`vendor/wonder-image/app` 下的直接编辑，或 `wonder-image/lib` 源码。
