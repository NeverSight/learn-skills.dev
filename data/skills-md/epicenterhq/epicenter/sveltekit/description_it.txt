Applica i pattern di SvelteKit per il routing, i moduli server, le azioni dei form, gli hook, i cookie, gli adapter e l'invalidazione. Usalo quando modifichi i file di route, gli hook, `app.d.ts` o il comportamento di SvelteKit.
