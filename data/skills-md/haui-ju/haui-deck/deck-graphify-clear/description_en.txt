Delete all Graphify memory blocks and set memory to null. Works when memory.enabled=false (cleanup). Always confirm before deleting. Does not revert .gitignore or npm scripts. Use when the user runs /deck-graphify-clear.
