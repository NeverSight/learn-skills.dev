Summarize the current session's progress and persist it to the project's progress document, or restore the last session's work context so the user can resume work immediately. Used for "save progress/record progress/update progress" or "restore progress/continue last work/resume from breakpoint" scenarios.
