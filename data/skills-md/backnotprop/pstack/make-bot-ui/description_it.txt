Usa quando costruisci un’interfaccia UI personalizzata (pagina, dashboard, pulsanti) che deve attivare un Grok Bot tramite webhook, quando l’utente deve fornire una chiave mittente del webhook, o quando esponi quell’interfaccia su Tailscale.
