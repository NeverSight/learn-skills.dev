Da utilizzare durante la distribuzione di un'applicazione Meteor 3. Trigger su meteor build, meteor deploy, Galaxy Push to Deploy, Galaxy Mode, Repository Mode, DEPLOY_HOSTNAME, Docker, Kubernetes, settings.json, METEOR_SETTINGS, MONGO_URL, MONGO_OPLOG_URL, ROOT_URL, PORT, HTTP_FORWARDED_COUNT, NODE_OPTIONS, controlli di integrità, comandi pre-distribuzione, hot code push, --architecture os.linux.x86_64, --server-only o una versione non corrispondente di Node.js distribuita. Utilizza questa competenza quando l'utente chiede informazioni sulla spedizione dell'app, sulla configurazione di produzione o sulla containerizzazione.
