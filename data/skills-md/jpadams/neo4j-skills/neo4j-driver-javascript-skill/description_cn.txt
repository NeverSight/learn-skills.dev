Neo4j JavaScript/TypeScript 驱动程序 v6 — 驱动程序生命周期、executeQuery、托管事务（executeRead/executeWrite）、session.run、整数处理、JSON 序列化、记录访问、异步/等待模式、TypeScript 类型、错误处理以及 Node.js 和浏览器的连接设置。在编写连接到 Neo4j 的 JS/TS 代码时使用 - neo4j.driver()、executeQuery、executeRead、executeWrite、session.run、neo4j.int、record.get、Integer、BookmarkManager、bolt://、neo4j+s://。不处理 Cypher 查询创作 - 使用 neo4j-cypher-skill。不涵盖驱动程序版本迁移 - 使用 neo4j-migration-skill。
