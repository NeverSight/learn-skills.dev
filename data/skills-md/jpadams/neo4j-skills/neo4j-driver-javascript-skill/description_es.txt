Neo4j JavaScript/TypeScript Driver v6 — ciclo de vida del driver, executeQuery, transacciones gestionadas (executeRead/executeWrite), session.run, manejo de Integer, serialización JSON, acceso a registros, patrones async/await, tipos TypeScript, manejo de errores y configuración de conexión para Node.js y navegador. Úselo al escribir código JS/TS que se conecta a Neo4j — neo4j.driver(), executeQuery, executeRead, executeWrite, session.run, neo4j.int, record.get, Integer, BookmarkManager, bolt://, neo4j+s://. NO maneja la creación de consultas Cypher — use neo4j-cypher-skill. NO cubre la migración de versiones del driver — use neo4j-migration-skill.
