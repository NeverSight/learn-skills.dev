محرك Neo4j JavaScript/TypeScript الإصدار 6 — دورة حياة السائق، executeQuery، المعاملات المدارة (executeRead/executeWrite)، session.run، التعامل مع Integer، تسلسل JSON، الوصول إلى السجل، أنماط async/await، أنواع TypeScript، معالجة الأخطاء، وإعداد الاتصال لـ Node.js والمتصفح. يُستخدم عند كتابة كود JS/TS يتصل بـ Neo4j — neo4j.driver()، executeQuery، executeRead، executeWrite، session.run، neo4j.int، record.get، Integer، BookmarkManager، bolt://، neo4j+s://. لا يتعامل مع تأليف استعلامات Cypher — استخدم neo4j-cypher-skill. لا يغطي ترقية نسخة السائق — استخدم neo4j-migration-skill.
