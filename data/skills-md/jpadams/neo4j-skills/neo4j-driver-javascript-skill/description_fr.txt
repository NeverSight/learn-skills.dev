Pilote Neo4j JavaScript/TypeScript v6 — cycle de vie du pilote, executeQuery, transactions gérées (executeRead/executeWrite), session.run, gestion des Integer, sérialisation JSON, accès aux enregistrements, modèles async/await, types TypeScript, gestion des erreurs et configuration de connexion pour Node.js et le navigateur. À utiliser lors de l'écriture de code JS/TS qui se connecte à Neo4j — neo4j.driver(), executeQuery, executeRead, executeWrite, session.run, neo4j.int, record.get, Integer, BookmarkManager, bolt://, neo4j+s://. Ne gère PAS la rédaction de requêtes Cypher — utilisez neo4j-cypher-skill. Ne couvre PAS la migration de version du pilote — utilisez neo4j-migration-skill.  

Veuillez toujours répondre dans la langue de l'utilisateur.
