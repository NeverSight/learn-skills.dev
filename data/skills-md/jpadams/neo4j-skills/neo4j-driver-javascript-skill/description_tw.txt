Neo4j JavaScript/TypeScript 驅動程式 v6 — 驅動程式生命週期、executeQuery、託管交易（executeRead/executeWrite）、session.run、整數處理、JSON 序列化、記錄存取、非同步/等待模式、TypeScript 類型、錯誤處理以及 Node.js 和瀏覽器的連接設定。在編寫連接到 Neo4j 的 JS/TS 程式碼時使用 - neo4j.driver()、executeQuery、executeRead、executeWrite、session.run、neo4j.int、record.get、Integer、BookmarkManager、bolt://、neo4j+s://。不處理 Cypher 查詢創作 - 使用 neo4j-cypher-skill。不涵蓋驅動程式版本遷移 - 使用 neo4j-migration-skill。
