Progetta e implementa interfacce di prodotto deliberate in una base di codice esistente o in un nuovo prototipo. Usalo quando ti viene chiesto di creare una pagina, un flusso, un componente, una landing page, una dashboard, un'esperienza web ottimizzata per mobile, una superficie di design system, o per rendere un'interfaccia più chiara, più coerente, più reattiva o più rifinita. Attiva su: progetta questa interfaccia, costruisci questa UI, migliora il design, ridisegna questa schermata, rendila intenzionale, gerarchia visiva, design d'interazione, stati responsivi, stati vuoti, stati di caricamento, design system, implementazione UI.

Per favore rispondi sempre nella lingua dell'utente.
