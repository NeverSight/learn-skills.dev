Keep-awake guard for AI agent sessions. When the user mentions /kafeined or @kafeined, hold the machine awake (no auto-sleep, idle logout, or shutdown) for the whole task, then release when done. Works on macOS (caffeinate), Windows (PowerShell), and Linux (systemd-inhibit) with no third-party installs.
