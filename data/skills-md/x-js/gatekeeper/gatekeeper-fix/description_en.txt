Gatekeeper rectification subskill (`/gatekeeper-fix`). It reads lines marked [status = pending rectification] in review.md / test.md, applies targeted fixes and writes them back, then awaits manual review. It is triggered only manually.
