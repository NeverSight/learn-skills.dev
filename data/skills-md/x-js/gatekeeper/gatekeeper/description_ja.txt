ゲートキーパー パイプライン【単一ルールソース＋中央制御ルーティング】。`/gatekeeper` コマンドでのみ手動でトリガーされます（エージェントによる自動使用は禁じられています）。まず現在のプロセスがどのステップにあるかを特定し、対応するサブスキルにディスパッチして続行します；既存のプロセスがない場合は、まず `/gatekeeper-init` を起動してください。

常にユーザーの言語で応答してください。
