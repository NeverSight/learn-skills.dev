Pipeline Gatekeeper [fuente única de regla + enrutamiento de control central]. Se activa únicamente de forma manual con el comando `/gatekeeper` (el uso automático por parte del Agente está prohibido). Primero determine en qué paso se encuentra el proceso actual, luego despache a la sub‑habilidad correspondiente para continuar; si no existe proceso, inicie primero `/gatekeeper-init`.

Por favor, responda siempre en el idioma del usuario.
