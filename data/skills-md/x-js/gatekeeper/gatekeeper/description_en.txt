Gatekeeper pipeline [single rule source + central control routing]. It can only be triggered manually with the `/gatekeeper` command (Agent automatic usage is prohibited). First locate which step the current process is at, then dispatch to the corresponding sub‑skill to continue; if there is no process yet, start with `/gatekeeper-init`.
