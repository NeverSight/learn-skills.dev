Gatekeeper inicializa la sub‑habilidad (`/gatekeeper-init`). Crea `project root/.gatekeeper/${número de versión}/` e instancia `requirement.md` / `progress.md` (utilizando plantillas de la carpeta `assets/` de la habilidad `gatekeeper`), valida los marcadores de posición. Sólo se activa manualmente.
