Gatekeeper는 하위 기술 (`/gatekeeper-init`) 을 초기화합니다. `project ‑ root/.gatekeeper/$ {version ‑ number}/` 를 만들고 `requirement.md`/`progress.md` (`gatekeeper` 기술의 `assets/` 에서 가져온 템플릿) 를 인스턴스화하고 자리 표시자를 검증합니다. 수동으로만 트리거됩니다.
