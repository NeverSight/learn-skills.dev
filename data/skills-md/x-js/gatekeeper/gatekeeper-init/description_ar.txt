يقوم Gatekeeper بتهيئة المهارة الفرعية (`/gatekeeper‑init`). ينشئ `project‑root/.gatekeeper/${versionNumber}/` ويُنشئ ملفات `requirement.md` / `progress.md` (باستخدام القوالب من مجلد `assets/` الخاص بمهارة `gatekeeper`)، ويصادق على العلامات النائبة. لا يمكن تشغيله إلا يدويًا.
