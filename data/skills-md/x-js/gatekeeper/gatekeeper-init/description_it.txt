Gatekeeper inizializza la sotto‑skill (`/gatekeeper-init`). Crea `project‑root/.gatekeeper/${versionNumber}/` e genera `requirement.md` / `progress.md` (usando i modelli dalla cartella `assets/` della skill `gatekeeper`), e valida i segnaposti. Viene attivato solo manualmente.
