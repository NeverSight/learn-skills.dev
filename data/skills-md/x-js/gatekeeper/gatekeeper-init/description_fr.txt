Gatekeeper initialise la sous-compétence (`/gatekeeper-init`). Il crée « project‑root/.gatekeeper/${version_number}/» et instancie « requirement.md » et « progress.md » (en utilisant les modèles des « assets/» de la compétence « gatekeeper »), valide les espaces réservés et est déclenché manuellement uniquement.
