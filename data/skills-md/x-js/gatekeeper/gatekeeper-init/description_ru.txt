Gatekeeper инициализирует поднавык (`/gatekeeper‑init`). Он создаёт `project_root/.gatekeeper/${version_number}/` и инстанцирует `requirement.md` и `progress.md` (используя шаблоны из `assets/` навыка `gatekeeper`), проверяет заполнители. Запускается только вручную.
