Gatekeeper initializes the sub‑skill (`/gatekeeper-init`). It creates `project-root/.gatekeeper/${version-number}/` and instantiates requirement.md and progress.md (using templates from the `gatekeeper` skill’s `assets/`), validates placeholders, and is triggered manually only.
