Gatekeeper abort subskill (`/gatekeeper-abort`). After verification and logging, the iteration directory is marked as “Aborted” and moved to `project_root/.gatekeeper/history/${version_number}/`. Triggered manually only.
