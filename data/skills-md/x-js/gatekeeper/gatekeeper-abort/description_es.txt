Subfunción de abortar de Gatekeeper (`/gatekeeper-abort`). Después de la verificación y el registro, el directorio de iteración se marca como “abortado” y se mueve a `project root/.gatekeeper/history/${version_number}/`. Solo se activa manualmente.
