Abilità di abort di Gatekeeper (`/gatekeeper-abort`). Dopo aver verificato la traccia, la directory dell'iterazione viene contrassegnata come “annullata” e spostata in `project root/.gatekeeper/history/${version number}/`. Attivata solo manualmente.
