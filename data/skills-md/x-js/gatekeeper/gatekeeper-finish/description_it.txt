Sotto‑abilità Gatekeeper finish (`/gatekeeper-finish`). Dopo che i quattro elementi di verifica dell'archivio sono completati, migra a `project root/.gatekeeper/history/${version number}/` e richiede la finalizzazione. Attivata solo manualmente.
