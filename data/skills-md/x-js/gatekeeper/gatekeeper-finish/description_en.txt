Gatekeeper completion sub-skill (`/gatekeeper-finish`). After the four archival verification items are complete, migrate to `project root/.gatekeeper/history/${version number}/` and prompt for wrap-up. Triggered manually only.
