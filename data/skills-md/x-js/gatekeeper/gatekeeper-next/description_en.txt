Gatekeeper stage advancement sub-skill (`/gatekeeper-next`). After confirming that the previous step's review has passed, execute the responsibilities of the current stage and instantiate the required template. Triggered manually only.
