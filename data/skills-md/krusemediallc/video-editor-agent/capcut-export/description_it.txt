Esporta un edit a livelli completato (filmato + schede grafiche + overlay alpha fx + didascalie + SFX/musica/VO) in una bozza CapCut per desktop dove OGNI elemento rimane modificabile — segmenti di filmato suddivisi ai confini dello zoom, clip grafiche per scheda, overlay alpha fx reale, didascalie testuali native, clip SFX per colpo e tracce separate di musica/VO. Usalo quando qualcuno dice "export to CapCut", "make this editable in CapCut", "CapCut draft", "hand the edit off to a human editor", "open my render in CapCut with layers", o menziona pyJianYingDraft / JianYing drafts / draft_content.json / draft_info.json / root_meta_info.json. Crea la bozza tramite pyJianYingDraft, poi applica una patch di schema in via di sviluppo per CapCut moderno (~181.x) e registra la bozza. NON è per il rendering del video stesso (questo è il compito della skill di editing) — consuma un edit a livelli già completato e i suoi passaggi di render.
