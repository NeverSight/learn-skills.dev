إعداد تطوير بايثون مع uv + ty + ruff + pytest + just. يُستخدم عند إنشاء مشاريع Python جديدة، أو إعداد pyproject.toml، أو تكوين الفحص، أو فحص الكتابة، أو الاختبار، أو بناء الأدوات. مشغلات على "Python project"، و"uv init"، و"pyproject.toml"، و"ruff config"، و"ty check"، و"pytest setup"، و"justfile"، و"python linting"، و"python formatting"، و"type check python".
