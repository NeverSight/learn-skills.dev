在本机上运行 CI 门禁，使其与 runner 保持一致——从工作流文件（而非 Makefile）中获取精确的命令、路径、标记和环境变量，解除会短路并隐藏后续错误的门禁步骤，固定 CI 使用的 linter 版本，构建 runner 所使用的解释器/工具链环境，并确认运行结果为绿色，而不是把红色作业解释掉。适用于本地检查通过但在 CI 中失败（或相反）的情况、lint/format 作业在未改动的文件上出现红色、为不熟悉的仓库搭建本地开发循环，或在推送预期合并的分支之前使用。
