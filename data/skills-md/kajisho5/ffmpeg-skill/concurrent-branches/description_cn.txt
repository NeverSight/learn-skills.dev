当同时针对一个存储库打开多个分支时解决冲突和合并 - 每次更改都必须触及的热点文件（注册表清单、单个版本字段、共享工具配置、一个聚合测试模块、跟踪构建输出）、union-vs-recompute-vs-rebuild 作为三种不同的正确解决方案、生成的工件决不能合并、导致每个分支冲突的非确定性序列化、git 无法向您显示的重新编号标识符，以及为什么干净的自动合并并不是一个通过测试。在变基或合并长期分支、解决冲突标记、审查合并提交、决定存储库应提交与生成什么或设置将进行并行贡献的存储库时使用。
