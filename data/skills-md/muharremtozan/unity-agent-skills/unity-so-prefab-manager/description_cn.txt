管理 Unity 6 中 ScriptableObjects（数据）和 Prefabs（逻辑/视觉）之间的结构化关系。遵循“SO-to-Mono”桥模式以确保实例独立性（例如，相同机器人的个体运行状况），同时保持干净的数据驱动架构。在以下情况下使用：(1) 创建新的单元/项目类型，(2) 将 SO 数据连接到 Prefab MonoBehaviours，(3) 解决更改一个 SO 影响所有实例的数据共享错误。
