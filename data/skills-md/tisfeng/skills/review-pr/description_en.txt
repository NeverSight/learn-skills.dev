By default, prepare the GitHub pull request on a local branch; use an isolated worktree when explicitly required, and optionally merge the latest base branch. Reuse the review core to accurately audit remote code and maintain thread state with evidence; disable remote writes when the user requests read‑only access. Suitable for GitHub PR review.
