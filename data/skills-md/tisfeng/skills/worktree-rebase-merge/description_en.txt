Complete worktree changes: create a Conventional branch for a detached checkout when needed, commit and rebase onto the target branch, then merge from the target worktree; if the source and target are the same, commit directly. The default target is the remote's default branch.
