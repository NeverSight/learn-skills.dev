Deriva mappe di altezza, normali in spazio tangente e MER (metalness, emissive, roughness) compresse da una texture albedo/base-color per i materiali PBR di Blockbench, usando PyPBR (Python) o vgpu + Dawn WebGPU (Node.js), con stima opzionale dell'altezza con Depth Anything V2 e MER inferito dall'albedo più la curvatura della normale. Utilizza dopo che esiste una texture di colore (dipinta, importata o generata con blockbench-gpt-image-textures) quando un materiale richiede canali normale, heightmap o MER, un texture_set Bedrock RTX, o quando è richiesto creare una mappa normale, bump map, height map, roughness, metalness o emissive da una texture. Si attiva su albedo a normale, mappa normale da texture, height map da albedo, mappa MER, roughness da texture, mappa metalness, mappa emissive, Depth Anything, PyPBR, vgpu, Sobel normale, normale DirectX vs OpenGL, texture_set normale, heightmap o layer metalness_emissive_roughness. Si prega di rispondere sempre nella lingua dell'utente.
