从 albedo / 基色纹理为 Blockbench PBR 材质派生高度图、切线空间法线图以及打包的 MER（金属度、发光、粗糙度）图，使用 PyPBR（Python）或 vgpu + Dawn WebGPU（Node.js），可选使用 Depth Anything V2 进行高度估计，并根据 albedo 加上法线的曲率推断 MER。仅在已有颜色纹理（绘制、导入或使用 blockbench‑gpt‑image‑textures 生成）且材料需要法线、高度图或 MER 通道、Bedrock RTX texture_set，或被要求从纹理生成法线图、凹凸贴图、高度图、粗糙度图、金属度图或发光图时使用。触发条件包括 albedo 转法线、从纹理生成法线图、从 albedo 生成高度图、MER 图、从纹理生成粗糙度图、金属度图、发光图、Depth Anything、PyPBR、vgpu、Sobel 法线、DirectX 与 OpenGL 法线、texture_set 法线、高度图或 metalness_emissive_roughness 层。
