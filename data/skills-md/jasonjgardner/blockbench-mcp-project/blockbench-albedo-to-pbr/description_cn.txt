从 albedo/基础颜色纹理为 Blockbench PBR 材质生成高度图、切线空间法线图以及打包的 MER（金属度、发光度、粗糙度）图，使用 PyPBR（Python）或 vgpu + Dawn WebGPU（Node.js），可选 Depth Anything V2 高度估计，并从 albedo 加上法线曲率推断 MER。仅在已有颜色纹理（已绘制、已导入或使用 blockbench-gpt-image-textures 生成）且材质需要法线、位图或 MER 通道、Bedrock RTX texture_set，或被要求从纹理制作法线图、凹凸图、高度图、粗糙度、金属度或发光图时使用。触发条件包括 albedo 转法线、从纹理生成法线图、从 albedo 生成高度图、MER 图、从纹理生成粗糙度、金属度图、发光图、Depth Anything、PyPBR、vgpu、Sobel 法线、DirectX 与 OpenGL 法线、texture_set 法线、位图或 metalness_emissive_roughness 层。请始终使用用户的语言进行回复。
