Previsione di serie temporali univariate zero-shot utilizzando il modello di fondazione Reverso (inferenza solo CPU NumPy/Numba). Si attiva quando gli utenti forniscono dati di serie temporali e richiedono previsioni, previsioni o estrapolazioni. Supporta Reverso Small (550.000 parametri). Trigger su "previsione", "previsione", "serie temporali", "Reverso" o quando i dati tabulari con una dimensione temporale necessitano di una stima del valore futuro.
