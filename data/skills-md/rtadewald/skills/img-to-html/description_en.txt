Recria um mock de UI (imagem) como HTML + CSS + JS estáticos (sem framework), em etapas com aprovação do usuário: wireframe ASCII tipado com plano → fundo completo → componentes com fontes → assets restantes → revisão final. Use when the user mentions img-to-html or asks to recreate a UI mock as HTML.
