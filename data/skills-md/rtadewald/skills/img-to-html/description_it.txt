Ricrea un mock di UI (immagine) come HTML + CSS + JS statici (senza framework), in fasi con approvazione dell'utente: wireframe ASCII tipizzato con piano → sfondo completo → componenti con font → asset rimanenti → revisione finale. Usa quando l'utente menziona img-to-html o chiede di ricreare un mock di UI in HTML.
