Recrea un mock de UI (imagen) como HTML + CSS + JS estáticos (sin framework), en etapas con aprobación del usuario: wireframe ASCII tipado con plan → fondo completo → componentes con fuentes → recursos restantes → revisión final. Usa esto cuando el usuario mencione img-to-html o solicite recrear un mock de UI como HTML.
