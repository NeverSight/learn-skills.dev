審核元件 API 的一致性、重大變更、TypeScript 覆蓋率與合約遵循性，遍及整個元件庫。當有人說以下語句時觸發：component prop review、verify component types are exported、component API audit、check our component interfaces、are our props consistent、API consistency check、prop naming review、breaking change detection，或任何關於檢查元件 API 是否結構健全且在整個庫中保持一致的說法。

請始終以使用者的語言回覆。
