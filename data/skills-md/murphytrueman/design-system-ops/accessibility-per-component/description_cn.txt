对特定的设计系统组件进行可访问性审计。当有人说以下任意词语时触发：accessibility check（可访问性检查）、a11y audit（可访问性审计）、WCAG compliance（WCAG 合规）、is this accessible（这是否可访问）、check accessibility（检查可访问性）、does this meet WCAG（是否符合 WCAG）、screen reader support（屏幕阅读器支持）、keyboard navigation check（键盘导航检查），或任何关于审计特定组件可访问性的内容。

请始终使用用户的语言作答。
