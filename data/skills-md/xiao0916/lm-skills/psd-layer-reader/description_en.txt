Read and export the Photoshop (.psd) layer tree as JSON, including layer metadata (name, type, visibility, bbox) and detailed text style information. Always use this skill when the user needs to analyze PSD structure, locate specific layers (such as pop-ups or buttons), or prepare data required for HTML/CSS restoration. Even with complex nested structures or when precise text restoration (font, color, spacing) is needed, this tool provides structured support.
