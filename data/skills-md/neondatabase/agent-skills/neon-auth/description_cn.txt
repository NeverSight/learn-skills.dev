为新应用添加身份验证。用于 “add auth”、 “add login”、Neon Auth（托管的 Better Auth）、身份路由、注册、登录、密码重置、邮件 OTP、魔法链接、组织、电话 OTP、OAuth、Passkeys、多因素认证（MFA）、受信任域、无效域，以及 @neondatabase/auth。若不存在已有身份，则默认使用托管的 Better Auth。继续使用 Better Auth、Clerk、Supabase Auth 或其他 IdP。用户要求从 Supabase Auth 迁移：使用托管的 Better Auth。所需插件超出托管支持范围：在 Neon Function 或现有应用主机上自行管理 Better Auth。也用于 @neondatabase/neon-js 中的身份验证 API。

请始终使用用户的语言回复。
