Raspa el repositorio anfitrión donde se encuentra esta habilidad y construye/mantén el paquete de conocimientos `.lokf/` como un sidecar, cumpliendo con el esquema del Formato de Conocimiento Abierto Vinculado (LOKF) (un perfil semántico de OKF). Úsalo cuando: crees o actualices archivos de conceptos bajo .lokf/knowledge/; agregues relaciones tipadas (isPartOf/dependsOn/derivedFrom/about/references/...); elijas una clase LOKF (Service/Metric/Dataset/Table/Policy/Playbook/GlossaryTerm/...); establezcas base_iri/context/id para que el frontmatter se expanda a JSON‑LD/RDF; valides el paquete con JSON Schema y SHACL mediante la herramienta lokf; conviertas/servir el paquete como un grafo; audites .lokf/ para verificar su corrección, huecos o errores; prepares un cambio LOKF para revisión de un mantenedor humano; o ejecutes la tarea programada de LLM‑librarian que mantiene .lokf/ preciso (regla de Karpathy).

Por favor, responde siempre en el idioma del usuario.
