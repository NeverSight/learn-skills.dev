Durchsuche das Host-Repository, in dem diese Fähigkeit liegt, und erstelle/pflege das `.lokf/` Wissens‑Bundle als Sidecar, konform mit dem Linked Open Knowledge Format (LOKF)-Schema (ein semantisches Profil von OKF). Verwende es, wenn: Konzeptdateien unter .lokf/knowledge/ erstellt oder aktualisiert werden; typisierte Beziehungen (isPartOf/dependsOn/derivedFrom/about/references/…) hinzugefügt werden; eine LOKF‑Klasse (Service/Metric/Dataset/Table/Policy/Playbook/GlossaryTerm/…) gewählt wird; base_iri/context/id gesetzt werden, sodass Frontmatter zu JSON‑LD/RDF expandiert; das Bundle mit JSON‑Schema und SHACL über das lokf‑Toolkit validiert wird; das Bundle als Graph konvertiert/bereitgestellt wird; .lokf/ auf Korrektheit, Lücken oder Fehler geprüft wird; eine LOKF‑Änderung für die Überprüfung durch einen menschlichen Maintainer vorbereitet wird; oder die geplante LLM‑Librarian‑Aufgabe ausgeführt wird, die .lokf/ aktuell hält (Karpathy‑Regel).
