ينشئ XML لسياسة إدارة Azure API جاهزة للإنتاج للمصادقة (OAuth 2.0، والتحقق من صحة JWT، ومفاتيح الاشتراك)، وتحديد المعدل، وتكوين CORS، ومعالجة الأخطاء، وتحويلات API. يُستخدم عند تنفيذ أمان واجهة برمجة التطبيقات (API)، أو التحكم في الوصول، أو منطق معالجة الطلب/الاستجابة.
