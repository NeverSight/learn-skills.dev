Disciplina de caché para cualquier repositorio que sirva un valor desde la memoria o un servidor de caché en lugar de volver a calcularlo a partir del almacén duradero, en cualquier lenguaje — una unión de adaptador, sin anotación ni decorador de caché, solo lectura a través, nada que garantice corrección en la caché, claves que transportan el alcance del llamador, un techo de obsolescencia comprometido, invalidación solo mediante eliminación tras el commit, ciclos de serialización exactos, sin valor sustituido ante un error de caché, y la puerta diferencial de tres configuraciones. Lleva el veredicto sobre cada forma en que un repositorio se construye a partir de estos primitivos — dos niveles de caché, una colección almacenada en caché junto a sus miembros, una caché compartida por dos desplegables, un precalentamiento en segundo plano, un cargador que lee otro servicio. SIEMPRE cargue antes de añadir una caché, una memo, una caché de carga, una anotación de caché o una expiración, antes de decidir si un repositorio necesita una caché en absoluto, antes de colocar una caché detrás de un proxy o una red de entrega de contenido, y antes de almacenar en caché un valor regido por otro conjunto de reglas. Indica el tipo de comprobación que necesita cada regla; la herramienta lleva el nombre en la habilidad del stack correspondiente (caching-java).
