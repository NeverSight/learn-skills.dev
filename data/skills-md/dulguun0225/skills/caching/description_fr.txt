Discipline de cache pour tout dépôt qui fournit une valeur depuis la mémoire ou un serveur de cache au lieu de la recomposer à partir du stockage persistant, dans n’importe quel langage — une seule jonction d’adaptateur, aucune annotation ou décorateur de mise en cache, lecture‑à‑la‑volée uniquement, aucune garantie de correction dans le cache, des clés qui portent le périmètre de l’appelant, un plafond d’obsolescence engagé, une invalidation en suppression uniquement après validation, des allers‑retours de sérialisation exacts, aucune valeur substituée en cas d’erreur de cache, et la porte différentielle à trois configurations. Porte le verdict sur chaque forme qu’un dépôt assemble à partir de ces primitives — deux niveaux de cache, une collection mise en cache à côté de ses membres, un cache partagé par deux déployables, un pré‑chauffage en arrière‑plan, un chargeur qui lit un autre service. TOUJOURS charger avant d’ajouter un cache, un mémo, un cache de chargement, une annotation de mise en cache ou une expiration, avant de décider si un dépôt a besoin d’un cache, avant de placer un cache derrière un proxy ou un réseau de diffusion de contenu, et avant de mettre en cache une valeur régie par un autre jeu de règles. Indique le type de vérification requis pour chaque règle ; l’outil est nommé dans la compétence de pile correspondante (caching‑java).
