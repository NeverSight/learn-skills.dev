一个代码仓库因维护者是大型语言模型代理而非人类而必须遵循的架构与过程规则——允许启动时的高调魔法，禁止运行时的无声魔法，摒弃全程序推理，运行状态存放于仓库，模块规模限定为一次会话，拓扑结构按独立意志的数量而非服务划分，强制使用单一惯用法，指挥人不是审阅者，使用新上下文的对抗性检查代替审阅，前置系统充当清单而非预言机，零重试并设有不可腐烂的隔离区，依赖项以失效形状计价，并且每个手工构建的细微部件旁都有安全论证。它包含了一个仓库的已完成案例——该语料库最爱却被它拒绝的案例，每个案例都以其失去的依据命名——以及对仓库从这两种原语组合而成的每种形态的判决——一个由两个意志推动的模块，一个带有自身惯用法的依赖，一个存在于控制台的隔离区，一个仍在提供流量的前置系统。始终在加载模块边界、选择运行时拓扑、决定构建闸门可放宽的条件、采用框架、数据库、托管服务或供应商 API、编写重试或错误处理、编写需要安全论证的细微代码、引入仓库已具备的第二种实现方式、从现有系统迁移或编写仓库宪章之前进行加载。
