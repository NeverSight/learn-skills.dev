针对使用 Spring Boot Web MVC、jOOQ 与 PostgreSQL 的 Java 后端的平台宪章——约束仓库中每一行代码的规则，每条规则都有一个命名的构建门。Reactive WebFlux、JPA 与 Hibernate、Spring Data JDBC 及 JdbcTemplate 系列、jOOQ 的 attached‑record CRUD、纯 SQL 字符串、可注入的 DSLContext、用于请求处理的固定线程池、StructuredTaskScope、连接池之上的额外信号量、领域代码中的实时时钟读取、迁移中的 `octet_length` 与 `varchar(n)` 长度限制、迁移中的 `create trigger` 与 `create function`，以及运行时静默的 Spring 注解——字段注入、@Transactional、@Scheduled、@Async、@Cacheable——均已列名禁用，并在每条规则中加入导致构建失败的检查。始终在编写查询、事务、请求内的 fan‑out、Flyway 迁移、定时任务或对该栈的测试之前加载它，在为其挑选持久化、并发、JSON 或空值检测库之前，以及在为新仓库创建构建文件或固定 Java 或 Spring Boot 版本之前，均必须先加载。
