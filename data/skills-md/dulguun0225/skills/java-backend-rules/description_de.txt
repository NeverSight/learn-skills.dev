Die Plattformkonstitution für ein Java‑Backend auf Spring Boot Web MVC, jOOQ und PostgreSQL — die Regeln, die jede Codezeile im Repository binden, jeweils mit einem benannten Build‑Gate. Reactive WebFlux, JPA und Hibernate, Spring Data JDBC und die JdbcTemplate‑Familie, jOOQs Attached‑Record‑CRUD, plain‑SQL‑Strings, ein injizierbarer DSLContext, feste Thread‑Pools für Request‑Arbeit, StructuredTaskScope, ein zusätzliches Semaphore über dem Connection‑Pool, Echtzeituhr‑Lesezugriffe im Domain‑Code, `octet_length`‑ und `varchar(n)`‑Längenbeschränkungen in Migrationen, `create trigger` und `create function` in Migrationen sowie die zur Laufzeit stillen Spring‑Annotationen — Feldinjektion, @Transactional, @Scheduled, @Async, @Cacheable — sind namentlich verboten, jeweils mit dem Check, der den Build fehlschlagen lässt. ALWAYS laden Sie vor dem Schreiben einer Abfrage, einer Transaktion, einem In‑Request‑Fan‑Out, einer Flyway‑Migration, einer geplanten Aufgabe oder einem Test auf diesem Stack, bevor Sie eine Persistenz‑, Concurrency‑, JSON‑ oder Null‑Sicherheits‑Bibliothek dafür auswählen, und bevor Sie eine Build‑Datei erstellen oder die Java‑ bzw. Spring‑Boot‑Version für ein neues Repository festlegen.
