行的标识方式以及该标识绝不可用于的场景——按照标识落在的使用场景来排序键候选，而不是按索引大小；把外部可以枚举的键视为容量泄露和强制改造；计算每种键机制在数据迁移时的手工步骤，因为序列不会复制而计数行会复制；使用计算出的表分类来应用单一机械规则，而不是对每个表单独判断；将反 UUID 成本的传说与自己的引擎对照，因为经典数字实际上是 UUIDv4，胖键乘以每个索引的说法是 InnoDB 的传说；禁止对任何 id 列使用 ORDER BY，因为时间有序键在单个生成器内是单调的，但在整个池中并非如此；保持不透明键和面向人的业务编号作为两个命名空间互不交叉的标识符；在模式中放置生成器默认值，并在旁边标记被禁止的生成器；为纯子表提供父表键加上一个序列；并记住键是唯一能在擦除后存活的。键在仓库中以各种形态出现时都必须遵循这一判决——键作为分区键、缓存键、对象存储路径、关联 ID、由其他标识符派生——以及某仓库将 UUIDv7‑全局使用作为成功案例，而 bigint 标识、bigint 加外部 ID 的混合以及 TSID 则被记录为失败者，并说明它们各自失败的原因。始终在创建表、选择或更改主键、在应用代码中生成 ID、设计面向人的编号格式、编写对象存储键模板或日志字段集、在 URL、日志行、负载或导出中放入 ID、在任何语言的查询中对 ID 列使用 ORDER BY，或在数据库之间迁移租户数据之前先进行加载。
