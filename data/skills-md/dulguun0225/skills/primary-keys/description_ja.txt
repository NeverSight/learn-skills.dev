行がどのように識別され、その識別子が決して使用されてはならないか——ID が着地するサーフェスでキー候補をランク付けし、インデックスサイズでランク付けしない、列挙可能な外部キーをボリューム開示および強制的なレトロフィットとして扱い、シーケンスは複製されずカウンタ行は複製されるため、各キー機構がデータ移動に要する手動ステップ数をカウントし、テーブルごとの判断ではなく計算されたテーブル分類で一つの機械的ルールを適用し、古典的な数字は UUIDv4 の数字であり、fat‑key‑multiplies‑every‑index の主張は InnoDB の伝承であることから、アンチ‑UUID コストのフォークロアを自分のエンジンで検証し、任意の ID カラムに対する ORDER BY を禁止する。時間順キーはジェネレータごとに単調増加するがプール全体では単調ではないため、オブジェクト的キーと人間が目にするビジネス番号を別個の名前空間を持つ二つの識別子として保持し、スキーマにデフォルトジェネレータを置き、隣に禁止されたジェネレータを配置し、純粋子テーブルには親のキーにシーケンスを付与し、キーは削除に耐える唯一のものだと覚えておく。リポジトリがキーから組み立てるあらゆる形状—パーティションキーとして、キャッシュキー内部で、オブジェクトストレージのパスとして、相関 ID として、別の識別子から派生したものとして—に対して判定を下す。あるリポジトリの「UUIDv7‑everywhere」判定は、bigint アイデンティティ、bigint‑plus‑external‑id ハイブリッド、TSID を敗者として記録し、失われた各基盤に対する判定である。テーブル作成前、主キーの選択または変更時、アプリケーションコードで ID を生成する際、人間が使用する番号形式を設計する際、オブジェクトストレージのキー・テンプレートやログフィールドセットを書く際、URL・ログ行・ペイロード・エクスポートに ID を入れる際、任意の言語で ID カラムに対して ORDER BY を書く際、テナントデータをデータベース間で移動する際は、必ず事前にロードすること。
