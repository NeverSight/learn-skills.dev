Cómo se identifica una fila y para qué nunca debería usarse esa identidad — clasifique los candidatos a clave por las superficies donde aterriza el id en lugar de por el tamaño del índice, trate una clave que un externo pueda enumerar como una divulgación de volumen y una adaptación forzada, cuente los pasos manuales que cada mecanismo de clave cuesta en una transferencia de datos porque las secuencias no se replican y las filas contrarias sí, aplique una regla mecánica con una clasificación de tabla calculada en lugar de un juicio por tabla, verifique el folklore del costo anti‑UUID contra su propio motor porque los números clásicos son números UUIDv4 y la afirmación de que una clave grande multiplica cada índice es leyenda de InnoDB, prohíba ORDER BY en cualquier columna id porque una clave ordenada por tiempo es monótona por generador y no a través de un pool, mantenga la clave opaca y el número de negocio visible para el humano como dos identificadores con espacios de nombres disjuntos, coloque el generador predeterminado en el esquema y el generador prohibido a su lado, proporcione a las tablas puras‑hijo la clave de su padre más una secuencia, y recuerde que la clave es lo que sobrevive a la eliminación. Lleva el veredicto en cada forma que un repositorio arma a partir de la clave — la clave como clave de partición, dentro de una clave de caché, dentro de una ruta de almacenamiento de objetos, como id de correlación, derivada de otro identificador — y el veredicto de un repositorio de “UUIDv7 en todas partes” como su caso trabajado, con identidad bigint, el híbrido bigint‑más‑id‑externo y TSID registrados como perdedores con el terreno que cada uno perdió. SIEMPRE cargue antes de crear una tabla, elegir o cambiar una clave primaria, generar un id en el código de la aplicación, diseñar un formato de número visible para el humano, escribir una plantilla de clave de almacenamiento de objetos o un conjunto de campos de registro, colocar un id en una URL, una línea de registro, una carga útil o una exportación, escribir un ORDER BY sobre una columna id en cualquier lenguaje, o mover datos de inquilinos entre bases de datos.
