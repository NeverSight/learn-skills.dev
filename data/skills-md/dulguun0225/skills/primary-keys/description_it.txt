Come una riga è identificata, e a cosa quell’identità non dovrebbe mai servire — classifica i candidati chiave in base alle superfici su cui l’ID atterra piuttosto che per dimensione dell’indice, tratta una chiave che un esterno può enumerare come una divulgazione di volume e una retrofit forzata, conta i passaggi manuali che ciascun meccanismo chiave comporta in uno spostamento di dati perché le sequenze non si replicano e le righe contatore sì, applica una regola meccanica con una classificazione della tabella calcolata anziché un giudizio per tabella, verifica il folklore dei costi anti‑UUID rispetto al tuo engine perché i numeri classici sono numeri UUIDv4 e l’affermazione “la chiave pesante moltiplica ogni indice” è leggenda di InnoDB, vieta ORDER BY su qualsiasi colonna ID perché una chiave ordinata temporalmente è monotona per generatore e non attraverso un pool, mantieni la chiave opaca e il numero business rivolto all’umano come due identificatori con namespace disgiunti, inserisci il valore predefinito del generatore nello schema e il generatore vietato accanto, assegna alle tabelle figlio pure la chiave del genitore più una sequenza, e ricorda che la chiave è ciò che sopravvive alla cancellazione. Porta il verdetto su ogni forma che un repository costruisce a partire dalla chiave — la chiave come chiave di partizione, dentro una chiave di cache, in un percorso di storage di oggetti, come ID di correlazione, derivata da un altro identificatore — e il verdetto “UUIDv7 ovunque” di un repository come suo caso di studio, con identità bigint, ibrido bigint‑plus‑external‑id e TSID registrati come perdenti con il terreno che ciascuno ha perso. CARICA SEMPRE prima di creare una tabella, scegliere o modificare una chiave primaria, generare un ID nel codice dell’applicazione, progettare un formato di numero rivolto all’umano, scrivere un template di chiave per lo storage di oggetti o un set di campi di log, inserire un ID in un URL, in una riga di log, in un payload o in un’esportazione, scrivere un ORDER BY su una colonna ID in qualsiasi linguaggio, o spostare dati di tenant tra database.
