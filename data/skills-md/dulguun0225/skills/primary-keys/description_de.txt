Wie eine Zeile identifiziert wird und wofür diese Identität niemals verwendet werden darf — bewerten Sie Schlüssel‑Kandidaten nach den Bereichen, auf denen die ID landet, und nicht nach der Indexgröße, behandeln Sie einen Schlüssel, den ein Außenstehender aufzählen kann, als Offenlegung von Volumen und als erzwungenes Nachrüsten, zählen Sie die manuellen Schritte, die jeder Schlüsselmechanismus bei einem Datenumzug kostet, weil Sequenzen sich nicht replizieren, Gegenreihen jedoch schon, wenden Sie eine einheitliche mechanische Regel mit einer berechneten Tabellenklassifizierung anstatt einer Einzeltabellen‑Entscheidung an, prüfen Sie die anti‑UUID‑Kosten‑Folklore gegen Ihre eigene Engine, weil die klassischen Zahlen UUIDv4‑Zahlen sind und die Behauptung, ein fetter Schlüssel multipliziere jeden Index, InnoDB‑Legende ist, verbieten Sie ORDER BY auf jeder ID‑Spalte, weil ein zeitlich geordneter Schlüssel pro Generator monoton ist, jedoch nicht über einen Pool hinweg, behalten Sie den undurchsichtigen Schlüssel und die für Menschen lesbare Geschäftsnummer als zwei Identifier mit getrennten Namensräumen, setzen Sie den Standard‑Generator in das Schema und den verbotenen Generator daneben, geben Sie reinen Kind‑Tabellen den Schlüssel ihres Elternteils plus einer Sequenz, und denken Sie daran, dass der Schlüssel das ist, was das Löschen überlebt. Trägt das Urteil über jede Form, die ein Repository aus dem Schlüssel zusammenstellt — den Schlüssel als Partitionsschlüssel, in einem Cache‑Schlüssel, in einem Object‑Storage‑Pfad, als Korrelations‑ID, abgeleitet von einem anderen Identifier — und das „UUIDv7‑überall“-Urteil eines Repositories als dessen Praxisbeispiel, mit bigint‑Identität, dem bigint‑plus‑external‑id‑Hybrid und TSID, die als Verlierer mit einem Grund, warum sie verloren haben, verzeichnet werden. LADEN Sie IMMER, bevor Sie eine Tabelle erstellen, einen Primärschlüssel wählen oder ändern, eine ID im Anwendungscode generieren, ein für Menschen lesbares Nummernformat entwerfen, ein Objekt‑Storage‑Schlüssel‑Template oder ein Log‑Feld‑Set schreiben, eine ID in eine URL, eine Log‑Zeile, eine Nutzlast oder einen Export einfügen, ein ORDER BY über eine ID‑Spalte in irgendeiner Sprache schreiben oder Mandantendaten zwischen Datenbanken verschieben.
