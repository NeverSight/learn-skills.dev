行如何被識別，以及該識別永遠不應被用於——根據識別碼所落腳的使用場景（而非索引大小）來排序鍵候選者，將外部人員能枚舉的鍵視為資訊洩露與被迫的改造，計算每種鍵機制在資料搬移時所需的人工步驟，因為序列不會被複製而計數列會，採用單一機械規則，利用計算出的表格分類取代對每張表的個別判斷，對照自己的引擎檢驗反 UUID 成本的傳說，因為傳統的數字實際上是 UUIDv4，且「大鍵會乘算每個索引」的說法是 InnoDB 的傳說，禁止在任何 ID 欄位上使用 ORDER BY，因為時間序列鍵僅在單一產生器內單調遞增，無法跨池保持單調，將不透明鍵與面向使用者的業務編號保留為兩個命名空間互不重疊的識別子，在模式中放置產生器的預設設定，並將被禁用的產生器並排列出，為純子表提供父表的鍵加上一個序列，並且記住，鍵是唯一會在資料抹除後仍然存在的東西。它決定了儲存庫從鍵所衍生的每一種形態——鍵作為分割鍵、作為快取鍵、作為物件儲存路徑的一部份、作為關聯 ID、或由其他識別子衍生——以及某個儲存庫的 UUIDv7 全面使用判決作為實作案例，其中 bigint 身份、bigint 加外部 ID 的混合以及 TSID 均被記錄為失敗者，且每一次失敗都有其根本原因。務必在建立資料表、選擇或變更主鍵、於應用程式碼中產生 ID、設計面向使用者的編號格式、編寫物件儲存鍵範本或日誌欄位集合、在 URL、日誌行、資料負載或匯出檔案中放置 ID、於任何語言的查詢中對 ID 欄位使用 ORDER BY，或在資料庫之間搬移租戶資料之前，先載入相關設定。
