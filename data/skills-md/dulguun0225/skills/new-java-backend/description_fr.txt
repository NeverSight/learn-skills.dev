Prenez un répertoire vide jusqu’au point où la première commande de spec‑kit peut s’exécuter, pour un backend Java sous Spring Boot Web MVC, jOOQ et PostgreSQL — un script épinglé qui instancie dulguun0225/java‑backend‑template avec chaque porte de construction déjà câblée, puis spécifiez `init`, puis transférez à `/speckit.constitution` uniquement pour les articles propres au projet. Rien ici n’est une décision et rien ici n’est écrit à la main. CHARGEZ TOUJOURS avant de créer un nouveau dépôt ou répertoire de projet backend Java, avant d’écrire un `pom.xml` ou tout fichier de construction pour celui‑ci, et avant d’exécuter `/speckit.constitution` dans un projet qui n’a pas encore de `backend/`.
