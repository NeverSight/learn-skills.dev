Nimm ein leeres Verzeichnis bis zu dem Punkt, an dem der erste Befehl von spec-kit ausgeführt werden kann, für ein Java‑Backend auf Spring Boot Web MVC, jOOQ und PostgreSQL – ein festes Skript, das dulguun0225/java-backend-template instanziiert, wobei jedes Build‑Gate bereits verkabelt ist, dann `init` spezifizieren und anschließend an /speckit.constitution nur für die eigenen Artikel des Projekts übergeben. Nichts hier ist eine Entscheidung und nichts hier ist von Hand geschrieben. LÄDSTE IMMER, bevor du ein neues Java‑Backend‑Repository oder Projektverzeichnis erstellst, bevor du eine pom.xml oder irgendeine Build‑Datei dafür schreibst und bevor du /speckit.constitution in einem Projekt ausführst, das noch kein backend/ hat.
