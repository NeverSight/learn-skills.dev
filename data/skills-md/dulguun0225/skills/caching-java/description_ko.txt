빌드가 실패하도록 캐시 규칙을 위반시키는 Java 검사 — Java, Spring Boot, ArchUnit, Error Prone, Jackson, Maven, JUnit, Testcontainers, Toxiproxy 각각에 대한 지시문을 적용하는 도구 — 그리고 캐시 엔진 선택(이름으로 금지된 Redis 7.4부터 7.8까지를 제외한 Valkey), 일회성 게이트 연결, 그리고 이 툴체인이 검사를 호스팅할 수 없는 명명된 구멍들. 캐시, 메모, 로딩 캐시, 캐싱 어노테이션, 만료 또는 캐시 엔진을 추가하기 전에 반드시 캐싱 스킬과 함께 Java 저장소에 로드하십시오. 여기서 엔진 선택을 제외한 모든 검사는 캐싱 스킬에 존재하는 지시문 ID에 매핑됩니다.
