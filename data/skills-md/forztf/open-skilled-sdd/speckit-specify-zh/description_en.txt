Chinese Functional Specification Creation Tool for converting natural-language feature descriptions into structured functional specification documents. Automatically generates branch names, creates Git branches, initializes spec files, and performs quality verification. Trigger phrases: "speckit spec", "functional spec", "create spec", "convert feature description", "speckit-specify". Use this skill when users need to turn feature ideas into structured specifications.
