Mantiene i contratti d'azione locali al repository e i repository di harness dove gli adapter CLI del prodotto e MCP rimangono sottili rispetto alle librerie di base. Da utilizzare quando si adottano o migliorano le azioni steward.yaml, le prove di adozione a livello di capacità, i loop di prova a freddo, i probe, i benchmark, la parità CLI/MCP/core, le ristrutturazioni degli adapter, i confini pacchetti/core o la disposizione dei harness fratelli; utilizzare prima repo-quality-system-lifecycle per i baseline generali di stewardship di app/librerie/tool.

Rispondi sempre nella lingua dell'utente.
