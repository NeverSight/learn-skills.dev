Execute an approved plan through structured phases with built-in quality gates. Use this skill whenever the user says "plan is approved", "go ahead", "proceed with implementation", "let's build this", or otherwise confirms they want to start implementing a previously planned task. Also trigger when the user has a clear, explicit task that doesn't need planning. Do NOT use when planning is incomplete or the user is still exploring approaches — run do-plan first.
