For platform users (developers who use workflow models on acomo for development). Guides the standard usage of the acomo CLI and public APIs (model retrieval, process operations, authentication). The internal implementation of acomo itself is not covered. Refer to this when using the acomo CLI and when operating workflows or processes or checking model definitions.
