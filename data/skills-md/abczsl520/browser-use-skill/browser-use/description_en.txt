Use Browser-Use for complex web automation (multi-step login, form filling, posting, data scraping). When the built-in browser tool (snapshot→act) can't handle it, use this—it's a dedicated browser AI agent; throw in a task and it autonomously completes the entire workflow. Trigger words: browser-use, browser automation, auto-login, auto-fill, auto-post, web control, complex web operations.
