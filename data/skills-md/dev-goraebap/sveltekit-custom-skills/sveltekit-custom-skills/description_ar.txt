بنية الخادم وقواعد كتابة التعليمات البرمجية لمشروع SvelteKit. يحدد طبقة الخادم (Active Record، وQuery Service، وREST API)، وسياسة إعادة استخدام المكونات، ودليل إمكانية قراءة التعليمات البرمجية. ارجع إليها عند كتابة أو مراجعة كود SvelteKit.
