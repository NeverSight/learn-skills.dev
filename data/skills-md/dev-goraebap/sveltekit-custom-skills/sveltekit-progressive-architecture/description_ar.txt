هندسة مشروع SvelteKit · قواعد الكود. الإجراءات: كتابة، تنفيذ، مراجعة، إعادة بناء، إصلاح، فحص، إضافة، تصميم، تعديل، كتابة، تنفيذ، مراجعة، إعادة بناء، إصلاح. القواعد الأساسية: إعادة استخدام المكونات، $lib، وضع المسار، عدم وجود أنواع مضمنة، الواجهة، النوع، قسم البرنامج النصي، التعليقات، سهولة القراءة، نمط التعليمات البرمجية، TypeScript. بنية الخادم: بنية الخادم، Active Record، خدمة الاستعلام، REST API، Drizzle، +server.ts، +page.server.ts، الخادم/المجال، الخادم/الأشعة تحت الحمراء، نموذج العرض، نموذج العرض، إجراءات النموذج، ORM، المخطط، فصل الطبقة، CUD، التحميل.
