Da utilizzare durante la configurazione o l'aggiornamento del file config.yaml di OpenSpec per un progetto o quando il flusso di lavoro OpenSpec non rileva le discipline di sviluppo (TDD, fasi progressive, convenzioni FP). Trigger: "configura openspec", "imposta openspec", "openspec config", "perché openspec non utilizza TDD", "openspec non richiama le competenze", "grounding config". Genera config.yaml con il contesto del progetto e le regole di chiamata delle competenze tra le sessioni. NON utilizzare per progetti non OpenSpec o per la configurazione generale di CLAUDE.md.
