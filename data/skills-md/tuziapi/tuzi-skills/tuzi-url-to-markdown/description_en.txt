Use Chrome CDP to scrape any URL and convert it to Markdown. Supports two modes—automatic scraping after page load, or waiting for a user signal (suitable for pages that require login). Use when the user asks to save a webpage as Markdown.
