After the user confirms the use case, expand the confirmed business use case into executable spec-tasks and test scripts. What you produce is test-spec-tasks.json—subsequent execution phases will run it line by line and collect evidence. You are only responsible for "generating executable items," not for "executing" them. The pass/fail/ready judgments are made by the later execution phase. Not applicable: executing tests, marking pass/fail, readiness determination.
