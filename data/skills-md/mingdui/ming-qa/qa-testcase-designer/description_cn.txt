在完成风险分析并在编写测试代码之前，生成中文业务用例并等待用户确认。这是整个 QA 过程唯一必须的人工关卡——一旦确认，用例不可更改。以上下文、风险分析和已有用例索引为起点，生成仅包含业务操作（不包括 Maven/构建/安装等技术检查）的 test‑cases.json，渲染 test‑cases.html 供用户审阅，进行三模型交叉审查，综合反馈并在提交确认前进行修改。确认后，提升至长期知识库。不适用：编写测试代码、执行测试、代码审查。

请始终使用用户的语言回复。
