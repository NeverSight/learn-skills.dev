Create commits following the commit convention (automatically classifying and splitting changes). Used when the user says "commit," "commit changes," "/commit," etc. Follows a convention based on Conventional Commits, automatically generating appropriate type/scope/subject. Changes that should be split into multiple logical units are automatically detected and split into multiple commits.
