Utilizza OpenWord end-to-end per sessioni di avventure dal vivo. Da utilizzare quando è necessario scaricare/installare/avviare OpenWord, guidare un giocatore umano nel browser o giocare in modo autonomo tramite l'API REST (crea/carica gioco, ciclo do_action, recupero stato/immagine), inclusa la configurazione di GEMINI_API_KEY e la condivisione di scene e scelte interessanti durante il gioco.
