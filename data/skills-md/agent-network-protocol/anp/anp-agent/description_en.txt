The ANP protocol enables cross-Agent skill invocation. Using the decentralized identity did:wba, it calls any Agent in the ANP network (such as Amap, hotel booking, express delivery inquiry, etc.). This skill is triggered when users mention ANP, invoke an Agent, book a hotel, check express delivery, look up maps, or plan routes.
