自动激活 litestar_mcp、LitestarMCP、MCP、MCPConfig、mcp.app、mcp.run()、@mcp.tool/resource/prompt、MCPAuthConfig、MCPAuthBackend、mcp_tool=、mcp_resource=、Streamable HTTP、stdio 或 OIDC MCP 端点。不适用于非 Litestar MCP 服务器或客户端 — 请改用官方 MCP Python SDK。
