自動啟動 litestar_mcp、LitestarMCP、MCP、MCPConfig、mcp.app、mcp.run()、@mcp.tool/resource/prompt、MCPAuthConfig、MCPAuthBackend、mcp_tool=、mcp_resource=、Streamable HTTP、stdio 或 DC MCP 到端點。不適用於非 Litestar MCP 伺服器或用戶端 — 請改用官方 MCP Python SDK。
