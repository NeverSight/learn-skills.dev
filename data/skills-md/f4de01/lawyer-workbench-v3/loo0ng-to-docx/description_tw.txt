要產生文件版本，需要將其轉換為 DOCX，通過佈局門，並在撰寫審閱報告時使用它：模型在 Markdown 中寫出最小的手稿集後，使用其轉換器以該節點的官方模板為載體將其轉換為 DOCX，然後運行其門檢查（靜態檢查加置信佈局估計；當有真實渲染可用時，使用渲染加置信度）。結論分為三層；只有經過並需要人眼觀察後才進入工作區域。當律師說「檢查此 docx」或「將此手稿轉換為 Word」時，他們也會使用它；門可以在任何 DOCX 上單獨運行。它提供了兩個CLI（轉換器僅依賴python-docx，gate本身俱有零第三方依賴，PyMuPDF可選），並且它們不互相引用；審查報告的格式、最小集和合併單元約定存儲在其參考文獻中。生成版本和最終批准的整個過程是由“loo0ng-doit”技能驅動的，然後調用轉換和門技能；將生成的條目添加到圖表中屬於“loo0ng-graph”技能；律師陳述和歸檔屬於“歸檔”技能；原來的官方模板和域名目錄屬於“loo0ng-domain”技能；關於下一步去哪裡不寫正文，不補槽，不修改律師寫的案卷，對結論沒​​有權威；缺少Word不會阻止文檔製作，但不確定的測量並不算通過。
