指导合作伙伴在其自有网站上构建 EMAP 商户注册表单。涵盖集成方式 1（完整表单——所有 6 步注册流程均托管在合作伙伴站点上，每一步通过代理调用 EMAP 的 API）、集成方式 2（重定向交接——浏览器将 step‑1 数据通过 URL 参数重定向至 EMAP，EMAP 预填并自动提交表单）以及集成方式 3（基于邮件的注册——合作伙伴后端向 EMAP 的 REST API POST step‑1 数据，EMAP 创建记录并通过邮件向商户发送安全链接，以完成完整申请）。提供字段对应关系、代码模板及安全检查清单。
