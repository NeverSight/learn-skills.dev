We triage PR changes, categorize them, perform surface checks, and determine required references. The results are output to `.pr-triage.json` and used in the subsequent code-review phase. Use it for cost optimization in CI environments.
