将现有代码重新对齐至 Tenets 规则集：审计指定范围，将发现聚合为有序的切片计划，等待批准，然后在项目接受门为绿灯的前提下，一次应用一个已批准的切片，每个切片对应一次提交。

仅在用户明确要求根据规则重新对齐、重构或修复代码库时使用——例如“将此包重新对齐至 Tenets”，或“修复 apps/api 中的规则违规”。不用于普通代码工作、单次编辑或只读评估：这些场景请使用 Tenets 技能或 Tenets‑audit。

请始终使用用户的语言回复。
