PostgreSQL 작업, 최적화 및 관리. 용도: 스키마 설계, 인덱스 선택, EXPLAIN ANALYZE를 사용한 쿼리 조정, postgresql.conf 구성, 백업 및 복원(pg_dump, pg_basebackup, WAL, PITR), Vacuum 및 Autovacuum 조정, 연결 풀링(pgBouncer, pgPool), 복제(스트리밍, 논리), 파티셔닝, 모니터링(pg_stat_statements, pg_stat_activity), JSONB 작업, 전체 텍스트 검색(tsVector, tsquery), 행 수준 보안(RLS), 확장(PostGIS, pg_trgm, timescaledb), GiST/GIN/BRIN 인덱스, 구체화된 뷰, 외부 데이터 래퍼, LISTEN/NOTIFY.
