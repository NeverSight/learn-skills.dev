Develop QuickBI AI Pro dashboard custom charts: scaffold creation, local debugging, build packaging, upload and release.

Trigger conditions: the user explicitly wants to develop a custom chart, extend an existing custom chart, create a chart project from scratch, locally debug a custom chart, upload the custom chart to the QuickBI platform, or troubleshoot issues such as the custom chart being blank, loading failures, missing external dependencies, or field mapping errors.

Non-trigger conditions: the user only wants to use the platform’s built-in components/widgets to create ordinary dashboards; only performs data Q&A, data retrieval, or generates analytical reports; modifies dataset structures, data source configurations, or permissions; and any general coding, documentation writing, or casual chat tasks unrelated to custom chart development.
