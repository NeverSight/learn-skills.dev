Modelli di progettazione REST API per .NET / ASP.NET Core (net8.0–net10.0) includendo la denominazione delle risorse, i codici di stato, le risposte di errore ProblemDetails, la paginazione, il filtraggio, il versionamento e il rate limiting per API di produzione. Usa questa competenza ogni volta che progetti o rivedi endpoint ASP.NET Core (API minime o controller), aggiungi endpoint di elenco, restituisci errori da un'API, scegli i codici di stato, o quando l'utente menziona REST, endpoint, Web API, paginazione, versionamento dell'API o limiti di velocità — anche se non dice esplicitamente "progettazione API". Rispondi sempre nella lingua dell'utente.
