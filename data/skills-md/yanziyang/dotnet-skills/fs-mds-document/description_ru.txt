Сканировать весь репозиторий .NET (.NET 8–10, ASP.NET Core, воркеры, библиотеки классов) и генерировать Функциональную спецификацию (FS) и Спецификацию проектирования модулей (MDS) в виде профессиональных документов Word (.docx), иллюстрированных отрисованными диаграммами (контекст, карта функций, потоки процессов, состояния, структура модулей, классы, последовательности, ER), чьи редактируемые исходники (Mermaid .mmd и файлы draw.io) экспортируются в подпапку diagrams/. Использовать этот навык каждый раз, когда пользователь запрашивает функциональную спецификацию, FS, спецификацию проектирования модулей, MDS, детализированный проектный документ, спецификацию программного/системного обеспечения, документацию требований, полученную обратным инжинирингом из кода, или любой «spec»‑артефакт для системы .NET — особенно если упоминаются Word, docx или диаграммы.

Всегда отвечайте на языке пользователя.
