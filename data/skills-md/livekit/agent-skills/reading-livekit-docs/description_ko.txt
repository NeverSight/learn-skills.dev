메모리에서 답변하는 대신 문서에서 현재 LiveKit 사실(예: API 시그니처, CLI 플래그, 설정 옵션, 모델 및 제공자 지원, SDK 변경 로그, 가격)을 조회합니다. LiveKit 구체 사항에 관한 질문이 있을 때마다 사용하십시오: “LiveKit이 X를 지원하나요”, “agents 1.8에서 무엇이 바뀌었나요”, “Y의 인자는 무엇인가요”, “LiveKit 비용은 어떻게 되나요”, “LiveKit 저장소에서 Z 예제를 찾아 주세요” 등, 혹은 LiveKit 코드를 작성하기 전에 사용합니다. 다른 LiveKit 스킬들은 이 스킬을 먼저 로드합니다. LiveKit Docs MCP 서버와 `lk docs` CLI를 포함합니다.

항상 사용자의 언어로 답변해 주세요.
