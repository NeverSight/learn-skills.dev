从文档中检索当前的 LiveKit 信息（API 签名、CLI 参数、配置选项、模型和提供商支持、SDK 更新日志、价格），而不是凭记忆回答。每当问题涉及 LiveKit 细节时使用，例如 “LiveKit 是否支持 X”、 “agents 1.8 有哪些更改”、 “Y 的参数是什么”、 “LiveKit 的费用是多少”、 “在 LiveKit 仓库中查找 Z 的示例”，或在编写任何 LiveKit 代码之前。其他 LiveKit 技能会先加载此功能。涵盖 LiveKit Docs MCP 服务器和 `lk docs` CLI。

请始终使用用户的语言作答。
