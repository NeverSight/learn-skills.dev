从文档中查找当前的 LiveKit 信息（API 签名、CLI 标志、配置选项、模型和提供商支持、SDK 更新日志、定价），而不是凭记忆作答。每当问题涉及 LiveKit 细节时使用，例如：“LiveKit 支持 X 吗”“agents 1.8 有哪些变化”“Y 的参数是什么”“LiveKit 的费用是多少”“在 LiveKit 仓库中找到 Z 的示例”，或在编写任何 LiveKit 代码之前。其他 LiveKit 技能会先加载此技能。涵盖 LiveKit Docs MCP 服务器和 `lk docs` CLI。

请始终使用用户的语言回复。
