Cerca le informazioni attuali su LiveKit (firmate API, flag della CLI, opzioni di configurazione, supporto per modelli e fornitori, changelog SDK, prezzi) nella documentazione invece di rispondere di memoria. Usalo ogni volta che una domanda riguarda specifiche di LiveKit: "LiveKit supporta X", "cosa è cambiato negli agenti 1.8", "quali sono gli argomenti di Y", "quanto costa LiveKit", "trova un esempio di Z nei repository di LiveKit", o prima di scrivere qualsiasi codice LiveKit. Altre competenze LiveKit caricano prima questa. Copre il server MCP dei Docs di LiveKit e la CLI `lk docs`.

Rispondi sempre nella lingua dell'utente.
