Cerca i fatti attuali di LiveKit (firma delle API, flag della CLI, opzioni di configurazione, supporto per modelli e provider, changelog SDK, prezzi) nella documentazione invece di rispondere dalla memoria. Usalo ogni volta che una domanda riguarda specifici di LiveKit: “LiveKit supporta X?”, “cosa è cambiato in agents 1.8”, “quali sono gli argomenti di Y”, “quanto costa LiveKit”, “trova un esempio di Z nei repository di LiveKit”, o prima di scrivere qualsiasi codice LiveKit. Altre competenze LiveKit caricano questa per prima. Copre il server MCP della documentazione LiveKit e la CLI `lk docs`.

Rispondi sempre nella lingua dell'utente.
