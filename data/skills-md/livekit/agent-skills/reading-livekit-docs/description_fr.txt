Recherche les faits actuels sur LiveKit (signatures d’API, drapeaux CLI, options de configuration, prise en charge des modèles et des fournisseurs, journaux des modifications du SDK, tarification) dans la documentation au lieu de répondre de mémoire. Utilisez‑le chaque fois qu’une question porte sur des spécificités de LiveKit : « LiveKit prend‑t‑il en charge X », « quels changements dans agents 1.8 », « quels sont les arguments de Y », « quel est le coût de LiveKit », « trouvez un exemple de Z dans les référentiels LiveKit », ou avant d’écrire du code LiveKit. Les autres compétences LiveKit chargent celle‑ci en premier. Couvre le serveur MCP des documents LiveKit et l’outil en ligne de commande `lk docs`.

Veuillez toujours répondre dans la langue de l’utilisateur.
