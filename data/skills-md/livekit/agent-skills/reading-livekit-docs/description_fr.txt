Recherche les faits actuels de LiveKit (signatures d’API, drapeaux CLI, options de configuration, prise en charge des modèles et des fournisseurs, journaux de modifications du SDK, tarification) dans la documentation au lieu de répondre de mémoire. Utilisez-le chaque fois qu’une question porte sur des spécificités de LiveKit : « LiveKit prend‑il en charge X », « qu’est‑ce qui a changé dans agents 1.8 », « quels sont les arguments de Y », « quel est le prix de LiveKit », « trouvez un exemple de Z dans les dépôts LiveKit », ou avant d’écrire du code LiveKit. Les autres compétences LiveKit chargent celle‑ci en premier. Couvre le serveur MCP des docs LiveKit et le CLI `lk docs`.

Veuillez toujours répondre dans la langue de l’utilisateur.
