從文件中查找當前的 LiveKit 事實（API 簽名、CLI 旗標、設定選項、模型與供應商支援、SDK 變更日誌、定價），而不是依靠記憶回答。當問題涉及 LiveKit 具體細節時使用，例如：「LiveKit 支援 X 嗎？」、「agents 1.8 有哪些變更？」、「Y 的參數是什麼？」、「LiveKit 的費用是多少？」、「在 LiveKit repositories 中找尋 Z 的範例」或在編寫任何 LiveKit 程式碼之前。其他 LiveKit 技能會先載入此技能。涵蓋 LiveKit Docs MCP 伺服器以及 `lk docs` CLI。

請始終以使用者的語言回覆。
