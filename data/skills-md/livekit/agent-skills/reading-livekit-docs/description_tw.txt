從文件中查詢當前的 LiveKit 資訊（API 簽名、CLI 旗標、設定選項、模型與供應商支援、SDK 更新日誌、價格），而不是憑記憶作答。當問題涉及 LiveKit 細節時使用，例如「LiveKit 是否支援 X」、「agents 1.8 有什麼變更」、「Y 的參數有哪些」、「LiveKit 的費用是多少」、「在 LiveKit 倉庫中找尋 Z 的範例」或在撰寫任何 LiveKit 程式碼之前。其他 LiveKit 技能會先載入此技能。涵蓋 LiveKit Docs MCP 伺服器與 `lk docs` CLI。
