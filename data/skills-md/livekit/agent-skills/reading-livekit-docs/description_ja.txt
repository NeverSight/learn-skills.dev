ドキュメントから現在の LiveKit の事実（API シグネチャ、CLI フラグ、設定オプション、モデルとプロバイダーのサポート、SDK 変更履歴、価格）を参照し、記憶だけで回答しないようにします。LiveKit に関する具体的な質問、例えば「LiveKit は X をサポートしていますか」「agents 1.8 で何が変わったか」「Y の引数は何か」「LiveKit の費用はいくらか」「LiveKit リポジトリ内の Z の例を探す」や、LiveKit のコードを書く前には必ず使用してください。他の LiveKit スキルはまずこのスキルを読み込みます。LiveKit Docs MCP サーバーと `lk docs` CLI をカバーします。

必ずユーザーの言語で回答してください。
