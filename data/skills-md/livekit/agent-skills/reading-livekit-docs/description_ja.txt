現在の LiveKit の事実（API シグネチャ、CLI フラグ、設定オプション、モデルおよびプロバイダーのサポート、SDK 変更履歴、料金）をドキュメントから取得し、記憶に頼って回答しないようにします。LiveKit の具体的な質問がある場合は必ず使用してください：「LiveKit は X をサポートしていますか」「agents 1.8 で何が変わったか」「Y の引数は何か」「LiveKit の料金はいくらか」「LiveKit リポジトリで Z の例を探す」あるいは LiveKit のコードを書く前に使用します。他の LiveKit スキルはこのスキルを最初にロードします。LiveKit Docs MCP サーバーと `lk docs` CLI を対象としています。

必ずユーザーの言語で回答してください。
