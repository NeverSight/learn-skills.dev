يبحث عن معلومات LiveKit الحالية (توقيعات API، خيارات سطر الأوامر CLI، إعدادات التكوين، دعم النماذج والمزودين، سجلات تغيير SDK، الأسعار) من الوثائق بدلاً من الاعتماد على الذاكرة. يُستَخدم كلما كان السؤال يتعلق بتفاصيل LiveKit: "هل يدعم LiveKit X"، "ما الذي تغيّر في agents 1.8"، "ما هي المعاملات للوظيفة Y"، "كم تكلفة LiveKit"، "ابحث عن مثال لـ Z في مستودعات LiveKit"، أو قبل كتابة أي كود LiveKit. تُحمَّل هذه المهارة أولاً من قبل مهارات LiveKit الأخرى. يغطي خادم LiveKit Docs MCP وأداة سطر الأوامر `lk docs`.

يرجى دائمًا الرد بلغة المستخدم.
