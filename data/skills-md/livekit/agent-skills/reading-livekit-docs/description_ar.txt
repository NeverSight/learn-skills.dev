يقوم بالبحث عن حقائق LiveKit الحالية (تواقيع API، خيارات سطر الأوامر، خيارات التكوين، دعم النماذج والمزودين، سجلات تغيّر SDK، التسعير) من الوثائق بدلاً من الإجابة من الذاكرة. يُستخدم كلما كان السؤال يلمس تفاصيل LiveKit: «هل يدعم LiveKit X»، «ما الذي تغيّر في agents 1.8»، «ما هي الوسائط لـ Y»، «كم تكلفة LiveKit»، «اعثر على مثال Z في مستودعات LiveKit»، أو قبل كتابة أي كود LiveKit. تُحمَّل مهارات LiveKit الأخرى هذا أولاً. يغطي خادم LiveKit Docs MCP وواجهة الأوامر `lk docs`.
