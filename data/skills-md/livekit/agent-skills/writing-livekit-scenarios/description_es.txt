Crea y mantiene los escenarios que ejecuta una simulación de agente LiveKit, y conecta el agente para que los consuma. Úsalo cuando el usuario pregunte “what should I test”, “generate simulation scenarios”, “write scenarios for my agent”, “add a scenario for X”, “organize my scenario files”, “my simulations are flaky”, “the scenario hits my real database”, “seed state per scenario”, “it passed but booked the wrong thing”, “grade the final state”, o quiera stress-testear un flujo antes de enviarlo. Cubre la generación de una línea base con el generador de escenarios LiveKit y su refinamiento, escribir los casos que la generación omite, redactar instrucciones de usuario simulado para que guíen de forma fiable, dividir los escenarios en conjuntos a través de archivos, y el código del lado del agente que inicializa un estado determinista a partir de un escenario y falla una ejecución si el estado final no es correcto. Para ejecutarlos usa running-livekit-simulations.
