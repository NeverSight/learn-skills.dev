Crea y mantiene los escenarios que ejecuta una simulación de agente LiveKit, y conecta al agente para que los consuma. Úsalo cuando el usuario pregunte "qué debería probar", "generar escenarios de simulación", "escribir escenarios para mi agente", "añadir un escenario para X", "organizar mis archivos de escenarios", "mis simulaciones son inestables", "el escenario accede a mi base de datos real", "estado semilla por escenario", "pasó pero reservó lo incorrecto", "calificar el estado final", o quiera someter a prueba un flujo antes de lanzarlo. Cubre la generación de una línea base con el generador de escenarios LiveKit y su refinamiento, la escritura de los casos que la generación omite, la formulación de instrucciones de usuario simulado para que guíen de manera fiable, la división de escenarios en conjuntos a través de archivos, y el código del lado del agente que siembra estado determinista a partir de un escenario y falla una ejecución según su estado final. Para ejecutarlos, usa running-livekit-simulations.
