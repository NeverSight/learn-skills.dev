إرشادات الخبراء لـ AI SDK Core: إنشاء النص، والبيانات المنظمة، واستدعاء الأدوات (أداة/أداة ديناميكية)، وتكامل MCP (createMCPClient، وExperimental_StdioMCPTransport)، والتضمينات/إعادة الترتيب، وإعداد الموفر، والبرامج الوسيطة، والقياس عن بعد، ومعالجة الأخطاء. يُستخدم عند الإنشاء باستخدام generatorText/streamText، أو generatorObject/streamObject، أو الأدوات (needsApproval، الصارم، inputExamples، activeTools، toolChoice، experience_context)، التضمينات (embed/embedMany/rerank)، أو أدوات/موارد/مطالبات/استنباط MCP.
