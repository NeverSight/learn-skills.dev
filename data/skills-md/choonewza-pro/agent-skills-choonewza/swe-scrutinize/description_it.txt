Revisione end-to-end da prospettiva esterna di un piano, PR o modifica al codice. Prima interroga l’intento e se un approccio più semplice o più elegante potrebbe raggiungere lo stesso obiettivo, poi traccia il percorso reale del codice (non solo il diff) per verificare che la modifica faccia ciò che afferma. L’output è conciso, azionabile e ogni chiamata contiene la sua motivazione. Si attiva con /scrutinize e in modo proattivo ogni volta che l’utente chiede di revisionare, auditare, fare un sanity‑check o ottenere una seconda opinione su un piano, PR, diff, documento di design o modifica proposta al codice.

Per favore rispondi sempre nella lingua dell’utente.
