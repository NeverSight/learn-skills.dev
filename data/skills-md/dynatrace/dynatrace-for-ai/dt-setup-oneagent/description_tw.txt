參考知識，用於在 VM/伺服器、Kubernetes（Operator + DynaKube）、AWS Lambda（層 + 環境變數，外加可選的新函式佈署）、Windows 主機、透過 SSH 的遠端 Linux 主機 / EC2 實例，以及 Azure VM / VM Scale Sets 上安裝、解除安裝或建立並安裝 Dynatrace OneAgent。提供已記錄的端點、環境變數、令牌範圍，以及可直接複製貼上的指令。返回要執行的精確指令，而不是執行它們。當使用者要求安裝或解除安裝 OneAgent，或詢問「我該如何…」、 「我需要什麼 URL/範圍/變數…」或「說明 X 的 OneAgent 部署」時使用。
