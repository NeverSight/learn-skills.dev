用于在 VM/服务器、Kubernetes（Operator + DynaKube）、AWS Lambda（层 + 环境变量，另可选全新函数部署）、Windows 主机、通过 SSH 的远程 Linux 主机/EC2 实例，以及 Azure VM/VM Scale Sets 上安装、卸载或创建并安装 Dynatrace OneAgent 的参考知识。返回已记录的端点、环境变量、令牌作用域，以及可直接复制粘贴的命令。返回准确的运行命令而不是执行它们。当用户请求安装或卸载 OneAgent，或询问“我该如何…”“我需要什么 URL/作用域/变量…”，或“解释 X 的 OneAgent 部署”时使用。

请始终使用用户的语言进行回复。
