Conoscenza di riferimento per l'installazione, la disinstallazione o la creazione‑e‑installazione di Dynatrace OneAgent su VM/server, Kubernetes (Operator + DynaKube), AWS Lambda (layer + variabili d'ambiente, più eventuale provisioning di una nuova funzione), host Windows, host Linux remoti / istanze EC2 tramite SSH e VM Azure / VM Scale Sets. Restituisce endpoint documentati, variabili d'ambiente, ambiti token e comandi pronti per il copy‑paste. Restituisce i comandi esatti da eseguire anziché eseguirli. Da usare quando l'utente chiede di installare o disinstallare OneAgent, o chiede “come faccio a…”, “qual è l'URL/ambito/variabile di cui ho bisogno per…”, o “spiega il deployment di OneAgent per X”.
