Gestisci un intero workspace Cargo come codice — dichiara connettori, modelli, play, strumenti, agenti, server MCP, segmenti, contesto, cartelle, file, worker e app in TypeScript, quindi riconciliali con `cargo-ai project` (init → types → plan → deploy), come faresti con Pulumi o l'AWS CDK. Trigger: "as code", "in git", "version-controlled", "reproducible", "Terraform for Cargo", "set up a whole workspace", "staging and production", "deploy the workspace from CI", "review this in a PR", "cargo.state.json", "scaffold from a template", "is there a cookbook for this", "start from a cookbook". Le skill con un esempio CDK (costruzione TAM, scoring account, sourcing contatti, routing, AI SDR, cockpit del rappresentante) vivono in gtm-skills; menu in references/cookbooks.md. Salta quando: è un'operazione una tantum, una lettura o una query ad‑hoc — usa la skill di capacità corrispondente.
