Verwalte einen kompletten Cargo‑Workspace als Code — deklariere Connectoren, Modelle, Plays, Tools, Agenten, MCP‑Server, Segmente, Kontext, Ordner, Dateien, Worker und Apps in TypeScript und synchronisiere sie anschließend mit `cargo-ai project` (init → types → plan → deploy), so wie du Pulumi oder das AWS CDK ausführen würdest.  

Auslöser: „as code“, „in git“, „versionskontrolliert“, „reproduzierbar“, „Terraform für Cargo“, „einen kompletten Workspace einrichten“, „Staging und Produktion“, „den Workspace aus CI deployen“, „dies in einem PR prüfen“, „cargo.state.json“, „aus einer Vorlage generieren“, „gibt es ein Cookbook dafür“, „aus einem Cookbook starten“.  

Fähigkeiten mit einem CDK‑Beispiel (TAM‑Building, Account‑Scoring, Contact Sourcing, Routing, AI SDR, Rep Cockpit) befinden sich in gtm‑skills; das Menü steht in references/cookbooks.md.  

Überspringen, wenn es sich um einen einmaligen Vorgang, eine Leseoperation oder eine Ad‑hoc‑Abfrage handelt — nutze die entsprechende Capability‑Skill.
