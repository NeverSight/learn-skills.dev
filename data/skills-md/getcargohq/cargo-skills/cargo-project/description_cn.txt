以代码的形式管理整个 Cargo 工作区——在 TypeScript 中声明连接器、模型、plays、工具、代理、MCP 服务器、细分、上下文、文件夹、文件、工作者和应用程序，然后使用 `cargo-ai project`（init → types → plan → deploy）进行调和，就像使用 Pulumi 或 AWS CDK 一样。触发词：“as code”“in git”“version‑controlled”“reproducible”“Terraform for Cargo”“set up a whole workspace”“staging and production”“deploy the workspace from CI”“review this in a PR”“cargo.state.json”“scaffold from a template”“is there a cookbook for this”“start from a cookbook”。带有 CDK 示例的技能（TAM 构建、账户评分、联系人获取、路由、AI SDR、rep cockpit）位于 gtm‑skills；目录在 references/cookbooks.md。以下情况跳过：一次性操作、读取或临时查询——使用匹配能力技能。
