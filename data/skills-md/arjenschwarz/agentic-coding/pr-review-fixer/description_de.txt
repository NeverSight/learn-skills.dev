Rufen Sie ungelöste PR-Kommentare ab (sowohl auf Code- als auch auf PR-Ebene), validieren Sie Probleme und beheben Sie sie. Überprüft außerdem den CI-Status und behebt fehlgeschlagene Tests, Lint-Fehler und Build-Probleme. Verwenden Sie diese Option, wenn Sie GitHub-PR-Feedback prüfen und bearbeiten. Filtert gelöste Kommentare heraus, behält nur den letzten claude[bot]-Kommentar pro Thread, validiert Probleme, veröffentlicht den Überprüfungsbericht als PR-Kommentar und behebt dann validierte Probleme.
