运行手动 QA 会话，以针对 PR、issue 或分支——发布交互式运行手册，测试人员从计划出现的那一刻起并行进行，结束时加入 AI 判定和 bug。永不直接触碰 tracker 本身。不同于 `om-auto-qa-pr` 的自动签署，这是一种 human‑in‑the‑loop 方式，并会建立本地 QA 知识库。用户说“帮我测一下”“QA 这个 PR”“找出此更改中的 bug”“逐步检查此 issue 的 bug” 时使用。
