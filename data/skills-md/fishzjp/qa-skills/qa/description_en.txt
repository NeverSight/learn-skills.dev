End-to-end QA entry: “test this feature fully” orchestrates requirements, strategy, cases, review, execution, bugs, regression, report; resumable. Single-stage tasks use their stage skill. The sole entry point for end-to-end testing: when you say “Help me test this requirement/feature,” it orchestrates requirements → strategy → test cases → review → execution → bug analysis → regression → report, with results persisted for resumable runs; single-stage requests directly use the corresponding stage skill.
