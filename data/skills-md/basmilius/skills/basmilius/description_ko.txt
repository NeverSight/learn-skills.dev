프로젝트가 `@basmilius` npm 스코프(베스 밀리우스) 내 패키지에 의존할 때 사용합니다: `@basmilius/utils`(Luxon 기반 독립 날짜, 숫자, 색상, 다운로드, DOM, 타이밍, 지리 및 PRNG 도우미), `@basmilius/http-client`(타입이 지정된 DTO 데이터 레이어) 및 `@basmilius/common`(Pinia와 Vue Router를 사용하는 Vue 3 앱 기본 요소). 모든 `@basmilius/*` 임포트에 대해 트리거됩니다; `formatDate`/`formatNumber`, `hexToRGB`/`hslToRGB`, `debounce`/`waitFor`, `downloadBlob`, `mulberry32`, `getSeason`/`getMoonPhase`에 대해 트리거됩니다; `@dto`/`@adapter`, `HttpClient`, `RequestBuilder`, `BaseService`, `BaseResponse`, `Paginated`, `.runAdapter`, `RequestError`/`ValidationError`, `serialize`/`deserialize`, `dtoNames` Vite 플러그인에 대해 트리거됩니다; ref를 반환하는 `defineStore`, `useService`/`guarded`, `useDataTable`/`useDataReport`, `useDtoForm`, `useUrlState`, `persistentRef` 및 그 컴포저블(`useClickOutside`, `useHotKey`, …)에 대해 트리거됩니다; 라우터 헬퍼(`useRouteParam`, `useNavigate`, …) 및 예외(`ForbiddenException`, `HandledException`, …)에 대해 트리거됩니다. `@flux-ui/*`(`flux-ui` 참조)에는 적용되지 않으며, `@basmilius/vite-preset`은 의도적으로 범위에서 제외되었습니다.
