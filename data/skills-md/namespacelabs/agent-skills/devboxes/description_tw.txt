建立、執行指令、分享與拆除 Namespace 開發盒（devboxes）──由 `devbox` CLI 驅動的遠端 Linux/amd64 或 macOS/arm64 機器。可用於隔離的 Linux 或 macOS 環境、遠端開發環境、一次性 Linux Docker 主機、執行超出本機能處理的工作（包括將測試套件分割至多個 devboxes 並行執行），或公開 `devbox.so` URL 讓團隊成員在瀏覽器中開啟工作。
