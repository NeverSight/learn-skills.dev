创建、运行命令、共享并拆除 Namespace devbox——由 `devbox` CLI 驱动的远程 linux/amd64 或 macos/arm64 机器。可用于隔离的 Linux 或 macOS 环境、远程开发环境、一次性 Linux Docker 主机、运行超出本地机器承载能力的任务（包括将测试套件在多个 devbox 上并行分片运行），或暴露 `devbox.so` URL，以便团队成员在浏览器中打开工作。
