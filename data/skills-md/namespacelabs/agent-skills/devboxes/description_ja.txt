Namespace の devbox を作成、コマンドを実行、共有、破棄できます — `devbox` CLI で制御されるリモートの linux/amd64 または macos/arm64 マシンです。隔離された Linux または macOS 環境、リモート開発環境、使い捨ての Linux Docker ホスト、ローカルマシンが処理しきれない作業（複数の devbox に分散して並列に走らせるテストスイートを含む）を実行するため、または `devbox.so` の URL を公開してチームメンバーがブラウザで作業を開くことができるように使用します。
