Public account article pipeline · The final state is **a formatted public‑account HTML** or **the article saved into your own public‑account draft box**; use it then (only save drafts, never mass‑send). The input is **already written** Markdown/HTML; this pipeline does not write the body, it only performs deterministic post‑processing: render → pre‑upload images → cover → save draft. ⚠️ It will **write into the user’s own public‑account backend**, so when the manuscript is finished, the user should not arbitrarily run it. Saving drafts requires binding an account + DOUBAOYA_API_KEY; only local rendering / theme switching does not need it. Trigger words: how to publish when the main text is ready / need formatted public‑account HTML / then format and send draft / write public account / convert to public‑account formatting / push public‑account draft / re‑push draft / publish with cover to draft box / save article into public‑account draft box / public‑account article pipeline / dby‑publish / save public‑account draft / public‑account draft box / proxy publish to public‑account draft box / addDraft / draft/add / article push to public‑account / send manuscript to public‑account backend.
