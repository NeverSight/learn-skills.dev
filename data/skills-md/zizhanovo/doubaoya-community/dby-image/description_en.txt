AI generating and editing images (both are exploding) — If you need a picture with just one sentence, use it: text‑to‑image, image‑to‑image, image editing, main visual, a standalone illustration. Provide a description and it will create an image; give a reference image (a local file works too) and it will edit based on that. **Slow operation**: usually takes 1–2 minutes, up to 4 minutes; during the wait do not interrupt or retry (retry means paying twice for the same image). After generation the image is saved as a local file and you get its path. Trigger words: draw a picture, help me draw, generate image, generate a picture, AI generate image, generate image, text‑to‑image, image‑to‑image, edit image, edit this image, Photoshop it, add illustration, add a picture, add an illustration, give a main visual, make a visual, draw according to this description, cover, cover image, public‑account cover, make a cover, add a cover image, first‑image inspiration. Not done: only cover **patterns and reference data** (no final image) go through dby‑api; insert the image into an article and save as a draft via dby‑publish.
