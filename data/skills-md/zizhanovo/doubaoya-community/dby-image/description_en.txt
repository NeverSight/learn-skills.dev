AI-generated and edited images (both are awesome) — if you need a picture with just a sentence, use it: text‑to‑image, image‑to‑image, editing, main visual, or a single accompanying illustration. Provide a description and it will produce an image; give a reference image (a local file works too) and it will edit based on that. **Slow operation**: usually wait 1–2 minutes, up to 4 minutes; do not interrupt or retry during the wait (retry means paying twice for the same image). After generation, the image is saved locally and you receive its path. Trigger phrases: “draw a picture,” “help me draw,” “generate image,” “generate a picture,” “AI generate image,” “produce image,” “text‑to‑image,” “image‑to‑image,” “edit image,” “edit this image,” “photoshop it,” “provide illustration,” “provide a picture,” “provide an illustration,” “give a main visual,” “create a visual,” “draw according to this description,” “cover,” “cover image,” “official account cover,” “make a cover,” “provide a cover image,” “first‑image inspiration.” Not handled: only cover **templates and reference data** (no final image) goes through dby‑api; inserting the image into an article and saving as a draft uses dby‑publish.
