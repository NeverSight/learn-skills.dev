AI 生成和编辑图像（两者都在爆炸）——如果你只需要一张图片，只要一句话即可使用：text‑to‑image、image‑to‑image、image editing、main visual、standalone illustration。提供描述，系统会生成图片；提供参考图片（本地文件也可），系统会基于此进行编辑。**慢速操作**：通常需要 1–2 分钟，最多 4 分钟；等待期间请勿中断或重试（重试意味着为同一图片付两次费）。生成后，图片会保存为本地文件并返回其路径。触发词：draw a picture、help me draw、generate image、generate a picture、AI generate image、generate image、text‑to‑image、image‑to‑image、edit image、edit this image、Photoshop it、add illustration、add a picture、add an illustration、give a main visual、make a visual、draw according to this description、cover、cover image、public‑account cover、make a cover、add a cover image、first‑image inspiration。未完成：仅覆盖 **patterns and reference data**（无最终图片）通过 dby‑api；将图片插入文章并通过 dby‑publish 保存为草稿。
