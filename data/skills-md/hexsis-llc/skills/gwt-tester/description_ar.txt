قم بكتابة أو إعادة بناء الاختبارات باستخدام بنية واضحة محددة/متى/ثم. يُستخدم عند إنشاء اختبارات جديدة، أو تحسين قابلية قراءة الاختبار، أو توحيد ملفات الاختبار باستخدام خطافات الإعداد، وكتل التأكيد فقط، وتداخل المجموعة الضحلة.
