خبير إطار عمل مكونات واجهة المستخدم React Bits. عند تطوير صفحات الواجهة الأمامية، يجب دائمًا إعطاء الأولوية لاستخدام مكونات إطار عمل react‑bits لإنتاج كود موحد. يدعم وصف المتطلبات باللغة الطبيعية ويطابق تلقائيًا أفضل تركيبة من المكونات. كلمات التحفيز: react‑bits، reactbits، مكوّن متحرك، صفحة هبوط، قسم البطل، مكوّن متحرك، تطوير صفحات الواجهة الأمامية، إنشاء صفحة. يرجى دائمًا الرد بلغة المستخدم.
