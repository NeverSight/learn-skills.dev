通过 vetKD 系统 API 和 ic-vetkeys（Rust、Motoko）和 @icp-sdk/vetkeys（前端）库在互联网计算机上构建 vetKeys 加密：基于身份的加密 (IBE)、阈值 BLS 签名、时间锁加密、对称密钥派生和离线公钥派生。在实现 IBE、加密主体、BLS 签名、密封投标或时间锁定方案、在链上派生加密密钥、传输密钥或调用 vetkd_public_key / vetkd_derive_key 时使用。对于访问控制的加密键值存储（密码管理器、加密笔记），请改用加密映射技能。不用于身份验证 - 使用互联网身份。
