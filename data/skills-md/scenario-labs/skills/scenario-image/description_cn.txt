在通过 MCP 使用 Scenario 生成或编辑图像时使用：文本到图像、图像到图像、指令编辑、带掩码的修补（inpainting）或延伸绘制（outpainting）、背景控制、宽高比或分辨率尺寸、每次运行产生多个输出，或在 Scenario 图像模型之间进行选择。也适用于因提示长度、计划受限模型或被悄然忽略的参考图像导致运行失败的情况。关键词：txt2img，img2img，图像编辑，修补，掩码，参考图像，宽高比。
