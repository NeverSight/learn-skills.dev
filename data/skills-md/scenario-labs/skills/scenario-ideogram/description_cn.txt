在 Scenario 通过 MCP 使用 Ideogram 模型生成或编辑图像时使用：海报、标志、菜单、包装或带有精确图内文字的标牌，原生透明 PNG 生成并带有 alpha 通道，保留头发和玻璃边缘的背景移除，将平面图形拆分为可编辑的文本层以进行本地化，或在单张参考照片中保持同一角色的一致性，使用修补（inpainting）。关键词：Ideogram V4 与 V3，排版，层化，角色参考，换脸。

请始终使用用户的语言作答。
