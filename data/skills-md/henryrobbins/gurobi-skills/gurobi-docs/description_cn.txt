当用户询问有关 Gurobi 或 gurobipy 的任何信息时使用 - API 方法（addConstr、addVar、addMVar、optimize、setParam、getAttr）、参数（TimeLimit、MIPGap、Threads、Presolve、PoolSolutions）、属性（ObjVal、Status、X、Pi、RC、ObjBound）、求解器功能（回调、IIS/不可行性、解决方案池、多目标、非线性）、状态/错误代码或特定于版本的更改（Gurobi 11、12、13）。始终使用此技能，而不是凭记忆回答 - Gurobi API 详细信息在版本之间会发生变化，并且需要文档验证。
