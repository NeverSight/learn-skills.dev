قم بتكوين إعدادات مشروع OpenSpec وتكوين واجهة سطر الأوامر العامة باستخدام أوامر `openspec/config.yaml` و`openspec config`. يُستخدم عندما يقول المستخدم "تكوين OpenSpec" أو "تكوين المشروع" أو "إضافة سياق المشروع" أو عندما يريد تعيين قواعد لكل قطعة أثرية.
