Usare solo quando l'utente richiede esplicitamente TDD, un test che fallisce o un test di regressione, O quando il bug ha un obiettivo di test locale ovvio e poco costoso. Saltare quando il percorso del test è poco chiaro, costoso, pesante in integrazione o non richiesto.

Rispondere sempre nella lingua dell'utente.
