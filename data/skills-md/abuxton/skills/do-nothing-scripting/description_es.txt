Obtenga un script bash que no haga nada a partir de un archivo asciinema .cast, un archivo de texto sin formato, un resultado del historial del shell o una entrevista de usuario, codificando cada comando observado como un paso manual que avisa al operador antes de continuar.
