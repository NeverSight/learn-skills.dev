Garantisce che esista una cartella .docs, crea project-log.md e registra lo stato corrente del progetto per l'handoff. Da utilizzare quando si termina una sessione, prima che il contesto cambi, quando si passa a un'altra persona o agente o quando l'utente chiede di salvare lo stato del progetto o creare un registro del progetto.
