Progetta e implementa endpoint API RESTful seguendo i principi REST, convenzioni di denominazione coerenti e metodi HTTP adeguati. Utilizza questa competenza durante la creazione o la modifica di route API, controller o gestori nel codice backend. Quando si implementa la logica dell'endpoint che gestisce le richieste HTTP (GET, POST, PUT, PATCH, DELETE). Quando si lavora con il controllo delle versioni dell'API, i parametri di query o la formattazione della risposta. Quando si definiscono strutture URL per endpoint basati su risorse. Quando si imposta la limitazione della velocità o la gestione del codice di stato HTTP. Quando si lavora con file come definizioni di percorso, file di controller, moduli di gestione API o file di configurazione del framework Web.
