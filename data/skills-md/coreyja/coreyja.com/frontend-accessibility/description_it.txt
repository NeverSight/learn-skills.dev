Crea interfacce utente accessibili con HTML semantico, attributi ARIA corretti, navigazione tramite tastiera e compatibilità con lo screen reader. Utilizza questa competenza durante la creazione o la modifica di markup HTML, componenti dell'interfaccia utente, moduli o elementi interattivi. Quando si implementa la navigazione tramite tastiera, la gestione dello stato attivo o l'ordine delle schede. Quando si aggiunge testo alternativo per immagini o etichette per gli input del modulo. Quando si garantiscono rapporti di contrasto cromatici sufficienti. Quando si lavora con menu di navigazione, modalità, pulsanti o contenuto dinamico. Quando si strutturano le pagine con una corretta gerarchia delle intestazioni (h1-h6). Durante il test delle modifiche all'interfaccia utente per la compatibilità dello screen reader.
